# Local socket connections (the image's own setup and health check) are trusted.
# Every TCP connection must use TLS and authenticate with a client certificate whose
# common name is the database user.
# TYPE   DATABASE  USER  ADDRESS  METHOD
local    all       all            trust
hostssl  all       all   all      cert
