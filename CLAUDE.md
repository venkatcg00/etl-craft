# CLAUDE.md — etl-craft

## What this is

`etl-craft` is a standalone, metadata-driven ETL orchestration engine. It is barebones by design, ships as a PyPI package (uv-compatible), and is meant to be adoptable by any team, independent of which orchestrator — if any — they use. This repo is **Repo 1** of a two-repo split: the engine itself, with zero domain knowledge and no team-specific pipelines baked in. A separate `metadata-etl-implementation` repo (not this one) will hold a reference implementation ("Support Insights") that proves the engine works at scale, and evolves independently of the engine.

The whole pipeline/task/step/dependency structure is modeled as **rows in Postgres**, not as Python DAG files or model files. The engine reads that metadata and executes it; it never writes logic it wasn't explicitly given.

## Where things stand / where to start

**Schema is signed off (2026-09-19).** `sql/schema.sql` is reconciled against two pasted schema snapshots plus every verbal correction made after them. It makes a real number of judgment calls translating ambiguous or conflicting source material into concrete SQL — every one of them is flagged inline with `[DEVIATION]`, `[ADDITION]`, or `[CHOICE]` tags (explained in the file's header) and summarized in a block at the end of the file. Every flag was walked through individually and confirmed as written — none required a change. **Read that summary anyway before touching the schema further** — it's the map of every non-obvious decision baked into it.

Build order, per the natural sequencing agreed once schema was confirmed — resolver → CLI (`run` first) → `generate-yml` → connector/auth layer:

- **Resolver — done.** `src/etl_craft/resolver.py`. Pure, DB-free dependency-graph logic scoped to same-pipeline `CFG_TASK_DEPENDENCY` edges only (cross-pipeline edges are explicitly out of scope here — those resolve via the tracker-table polling described above, not via this module). Covers: cycle/self-dependency validation (`build_graph`), static topological waves for `graph`/`generate-yml` (`DependencyGraph.waves`), and the retry-aware `ready()` computation implementing the SUCCESS/FAILURE/ALWAYS/HAS_DATA edge semantics plus "retry resumes, not restarts" (skip SUCCESS/SKIPPED, re-attempt FAILED/never-run, never re-dispatch an already-IN-PROGRESS task). 21 tests in `tests/test_unit.py`, all passing.
- **Engine DB access layer — done (2026-09-19).** The plumbing `run` needs, built ahead of the CLI itself since it has the most unresolved ambiguity to flag:
  - `src/etl_craft/config.py` — loads and validates `craft-connector.yml`. **[CHOICE]**: the file's own example renders section names as bracketed INI-style headers (`[Execution]`, `[Postgres]`, ...) inside a yaml fence, which isn't literal YAML — the loader expects plain nested top-level keys instead (`Execution:`, `Postgres:`, ...). **[ADDITION]**: each profile stores a `jdbc_url` (per "JDBC URL is the preferred storage format") plus `user`/`auth_mode`; secret material (password/token/passphrase) is never in the file — it's looked up via an env var name resolved through `[Source]`'s file-or-environment mechanism, defaulting to `ETL_CRAFT_{SECTION}_{PROFILE}_SECRET` unless a profile sets `secret_var:` explicitly. The Data DB section was deliberately not parsed at this point in the build (open question #1 was still unresolved) — now covered below under "Data DB / warehouse connector layer."
  - `src/etl_craft/db.py` — `AUTH_REGISTRY` keyed by `auth_mode`, each entry building a `creator` callable handed to `create_engine(creator=...)` uniformly (per CLAUDE.md, not just for token/sso). `password` and `key_file` are fully implemented; `token` and `sso` raise `NotImplementedError` — CLAUDE.md establishes *that* they mint short-lived credentials but not which provider/SDK, which is necessarily team-specific.
  - `src/etl_craft/runlog.py` — implements "Run-id resolution" above as two distinct entry points, not one: `find_or_create_active_run` (for whatever mints the run — the local orchestrator before spawning task subprocesses, or the Airflow DAG's synthetic first step) versus `resolve_run_for_task` (what a plain `run --task_code` binds to: the active run if one exists, else CLAUDE.md point 5's dev/ad-hoc fallback to the latest logged run). **[CHOICE]**: "updates its dates" in that fallback is implemented as touching only `END_DATE` — `STATUS` and `START_DATE` are left alone, since reopening a terminal run's status could collide with `ux_pipeline_run_one_active` the next time that pipeline genuinely kicks off. Also includes `find_or_create_task_run` (the per-task binding, short-circuits on an existing `SUCCESS`) and `update_task_run` (in-place update, never a second row per retry).
  - Tested against an in-memory SQLite stand-in (50 tests, part of `tests/test_unit.py`) for the control-flow logic, plus a real-Postgres integration suite (part of `tests/test_integration.py`, 5 tests) that includes an actual multi-thread concurrency test proving `ux_pipeline_run_one_active` — not application code — is what makes concurrent run creation race-safe. See "Local Postgres (Docker)" below for how to run it.
- **Single-task execution core (`run --task_code`) — done (2026-09-19).** `src/etl_craft/runner.py`'s `run_task()` is what that invocation actually does — dependency check, short-circuit, bind, dispatch, finalize — built and verified against real Postgres.
  - `src/etl_craft/cfg.py` — read-only `CFG_PIPELINES`/`CFG_TASKS`/`CFG_TASK_DEPENDENCY` queries: `resolve_pipeline_id`/`resolve_task_id` (code → id, active rows only), `fetch_pipeline_graph` (active tasks + same-pipeline edges shaped for `resolver.build_graph`, plus `cross_pipeline_task_ids` — surfaced but not yet acted on, since cross-pipeline polling isn't built), `fetch_task_handler`.
  - `src/etl_craft/handlers.py` — a stub `HANDLER_REGISTRY`/`dispatch()` seam; every handler body (`PYTHON`/`SQL`/`BUSINESS_RULES`/`EMAIL_ALERT`) raises `NotImplementedError` — each is its own substantial unbuilt piece (closed SQL action vocabulary, `$$pipeline_id` substitution, Data DB connection, script invocation, business-rule sequencing).
  - `src/etl_craft/runner.py`'s `run_task()` sequence (as first built; **superseded 2026-09-19, see the "Cross-pipeline dependency polling" entry below** — an unmet dependency is no longer a raised exception): refuse `--force` under `Mode=orchestrator` first (before any DB access); resolve pipeline/task ids; resolve `pipeline_run_id` via `resolve_run_for_task` (never mints a fresh run itself — see runlog.py above, so a single-task invocation against a pipeline that's *never* run at all correctly raises `RunLogError`, not silently minting one); unless `--force`, short-circuit on an existing `SUCCESS` binding, then live-check same-pipeline dependencies by building the full pipeline graph via `resolver.build_graph`/`ready()` fed with current `AUD_TASK_RUN_LOG` state (`runlog.fetch_run_state`) — if unmet, raised `DependenciesNotMetError` without writing anything to `AUD_TASK_RUN_LOG` at all; only then bind via `find_or_create_task_run` and dispatch to the (stub) handler, marking `FAILED`/`SUCCESS` on the way out.
  - Deliberately **not** included yet (at this point in the build — see the later entry): cross-pipeline dependency polling.
  - 19 integration tests against real Postgres (part of `tests/test_integration.py`), all passing — 74 total across the whole suite. Caught the same identifier-case-folding pitfall noted above, this time in a test assertion, not production code.
- **CLI entry point — `run`, `list`, `graph`, `set-execution-mode`, `configure --env` done.** `src/etl_craft/cli.py`'s `main(argv)` wires up `etl-craft run --pipeline_code X --task_code Y [--force]` (matching the CLI surface table below exactly — `--pipeline_code` required, `--task_code` optional). `--task_code` given dispatches to `runner.run_task`; omitted dispatches to `orchestrator.run_pipeline` (below). Every exception either `run_task` or `run_pipeline` can raise for a non-bug reason (`CfgError`, `RunLogError`, `ConfigError`/`ConnectionError_`, `ForceNotAllowedError`, `DependenciesNotMetError`) is caught and printed as a clean one-line `error: ...` rather than a raw traceback. `src/etl_craft/__init__.py`'s `main()` (the `[project.scripts]` target) and `__main__.py` (for `python -m etl_craft`) both delegate to it.
  - `list` prints every active pipeline (`cfg.fetch_all_pipelines`) — code, name, refresh type, tab-separated.
  - `graph --name X` prints `X`'s same-pipeline task waves (via `resolver.build_graph`/`waves()`), its own `CFG_PIPELINE_DEPENDENCY` edges, and any task-level cross-pipeline edges. **[CHOICE]**: CLAUDE.md's CLI surface table literally writes this as `graph --name <pipeline>`, unlike every other command's `--pipeline_code` — an inconsistency in the source doc, not a deliberate difference here. `--name` is resolved as a `PIPELINE_CODE`, same as everywhere else, since that's the one designated CLI lookup key (open question #2). Flagging in case `--name` was actually meant to match `PIPELINE_NAME`.
  - **`set-execution-mode {local,orchestrator}`** (2026-09-20, `src/etl_craft/configure.py`) and **`configure --env <path>`** (same file) are the first commands that write `craft-connector.yml` rather than read it — both handled in `cli.py`'s `main()` *before* the usual `load_config`/`build_engine` setup, since neither needs (or can assume) a working Engine DB connection. Per sign-off: both do a full parse + re-serialize via PyYAML (`sort_keys=False`), never a surgical text edit — simple and robust, but every write touches the whole file, so comments/exact formatting in a hand-edited file don't survive a call. **[ADDITION]**: `configure --env`'s env-var names (`ETL_CRAFT_MODE`, `ETL_CRAFT_POSTGRES_JDBC_URL`, `ETL_CRAFT_POSTGRES_PROFILE`, `ETL_CRAFT_POSTGRES_USER`, `ETL_CRAFT_POSTGRES_AUTH_MODE`, `ETL_CRAFT_SOURCE_TYPE`/`_PATH`, `ETL_CRAFT_CLONING_ENABLED`/`_SCOPE`, `ETL_CRAFT_ORCHESTRATOR_NAME`) aren't specified anywhere in CLAUDE.md, only that the command exists — confirm this naming before anything else (docs, the reference-implementation repo) comes to depend on it. **[CHOICE]**: re-running `configure --env` against an existing file *merges* — adds/updates just the one Postgres profile named in the env file (preserving any others already there) and makes it `Active_profile`, but wholesale-replaces `Execution`/`Source`/`Cloning` since those are singular/global. Interactive `configure` (no `--env`) is not built — refused with a clear "not implemented yet" message and exit code 2.
  - Manually verified end-to-end against Docker Postgres: `configure --env` generating a fresh, `load_config`-round-trippable file; `set-execution-mode` updating just `Mode`; a second `configure --env` call correctly merging a `uat` profile alongside `dev` and switching `Active_profile`.
- **`run --init-only`, and `run_pipeline` refused under `Mode=orchestrator` — done (2026-09-20).** A real design gap surfaced while starting `generate-yml`: a generated Airflow DAG needs a synthetic first task to mint the run (CLAUDE.md: "an additional pipeline id creation step that starts in step 1 before all named steps"), but the existing `orchestrator.run_pipeline()` — a full local wave-spawning scheduler — would be redundant with (and race against) Airflow's own native per-task scheduling if invoked that way. **[CHOICE], confirmed:** `run_pipeline()` is now refused outright under `Mode=orchestrator`, *regardless of `--force`* (a real behavior change from before — it used to only refuse `--force` specifically; a bare no-`--task_code` call under orchestrator mode used to silently run the full local scheduler, which the design gap shows was always wrong). `orchestrator.py`'s own `[CHOICE]` comment has the full reasoning. The generated DAG's synthetic first step instead uses the new `--init-only` flag on `run` (`orchestrator.init_pipeline_run`) — mints/reuses the active run via `find_or_create_active_run`, does *not* spawn any task subprocesses, and is legal under both modes (`--task_code` and `--init-only` are mutually exclusive at the argparse level). Cross-pipeline dependency polling wasn't wired into it yet at this point in the build — done later, see the "Cross-pipeline dependency polling" entry below.
  - Manually verified end-to-end against Docker Postgres: `--init-only` mints on first call, reuses the same `pipeline_run_id` on a second call, and `--init-only --task_code` is rejected by argparse itself.
  - 144 tests total (up from 137), 100% coverage maintained.
- **Pipeline-level orchestration (`run --pipeline_code X`, `--task_code` omitted) — done (2026-09-19).** `src/etl_craft/orchestrator.py`'s `run_pipeline()` — CLAUDE.md's "the engine becomes its own tiny scheduler": mints/reuses the run via `find_or_create_active_run` (unlike `run_task`, which never mints), then loops — compute the ready wave via `resolver.build_graph`/`ready()` fed live `AUD_TASK_RUN_LOG` state, spawn one genuine `python -m etl_craft run --task_code` **subprocess** per ready task (not an in-process call — this is what makes local execution mirror Airflow-side execution), wait for the whole wave, repeat — until every task is settled (`SUCCESS`/`SKIPPED`) or none are ready (stuck: some task's dependency will never be satisfied this pass), then finalizes `AUD_PIPELINES_RUN_LOG` `SUCCESS`/`FAILED` accordingly.
  - **[Bug caught and fixed]**: the first version re-selected and re-spawned a permanently-FAILED task forever within one `run_pipeline()` call — real infinite loop, reproduced as an actual test hang (~150s before it was killed), not a theoretical concern. `resolver.ready()` correctly treats `FAILED` as retry-eligible *in general* (point of the whole "retry resumes" design), but that's for a **separate, later** `run` invocation to pick up — not for the orchestrator to retry within a single pass. Fixed by tracking an `attempted` set scoped to one `run_pipeline()` call. Also added `pytest-timeout` (60s, `[tool.pytest.ini_options]`) project-wide as a safety net against this whole bug class recurring, since a hang fails a lot less legibly than an assertion.
  - `--force`: since it bypasses dependency checks entirely, `ready()` can't be used to gate anything — falls back to `graph.waves()` (static topological order) instead, spawning every task in each wave regardless of current status.
  - Deliberately **not** included yet, at this point in the build: cross-pipeline dependency polling (the self-check/poll step CLAUDE.md describes before run-id minting — done later, see below), and orchestrator-level crash detection for a subprocess that dies without writing its own terminal status (CLAUDE.md's crash detection is specifically about `run_task`'s own internal fork/monitor, still deferred — a task stuck that way just can't become ready again, so the existing stuck-detection already keeps the pipeline from hanging on it).
  - 6 real end-to-end tests spawning actual subprocesses against Docker Postgres (independent tasks, blocked-downstream/stuck, retry-skip-already-SUCCESS, `--force`, mode refusal, empty pipeline) plus manual CLI smoke tests, all passing — 80 total across the whole suite (up from 74).
- **`generate-yml` — done (2026-09-20).** `src/etl_craft/generate_yml.py`'s `generate_pipeline_dag()` builds a plain dict (the CLI `yaml.safe_dump`s it, to stdout or `--output <path>`) for one pipeline's DAG. **[ADDITION]**: CLAUDE.md only says "hand-rolled, Airflow-YAML-inspired shape" — no field names or structure are specified anywhere, so every choice below is this module's own, flagged for confirmation before anything downstream depends on it:
  - `dag_id`/`description`/`schedule`/`sla_hours`/`refresh_type` from `CFG_PIPELINES` (new `cfg.fetch_pipeline_detail`), then a `tasks:` block (dag-factory-visually-inspired: `bash_command` + `depends_on`, not dag-factory's actual schema).
  - Every generated DAG gets a synthetic `__init__` task first (`etl-craft run --pipeline_code X --init-only`) per CLAUDE.md's old design-notes phrasing ("an additional pipeline id creation step that starts in step 1 before all named steps"). Each real task's `bash_command` is `etl-craft run --pipeline_code X --task_code Y` — the literal form CLAUDE.md's Execution section describes. `depends_on` is the task's own same-pipeline `CFG_TASK_DEPENDENCY` edges (task code + `dependency_type`, e.g. an `EMAIL_ALERT` task correctly shows up depending on its watched task via `FAILURE`); a task with none instead depends on `__init__` (with `ALWAYS`) rather than every task listing `__init__` redundantly — downstream tasks inherit the ordering transitively through their own chain, the same way a real Airflow `>>` DAG would be hand-written.
  - `build_graph` runs first and its `ResolverError` (cycle, self-dependency, unknown task) propagates as a clean CLI error — there's no meaningful YAML to emit for a graph that can't become a DAG.
  - Pipeline-level (`CFG_PIPELINE_DEPENDENCY`) and task-level cross-pipeline dependencies are surfaced under their own top-level keys (`pipeline_dependencies`, `cross_pipeline_task_dependencies`) purely informationally — CLAUDE.md: "one DAG can't natively depend on a task in a separate DAG" — never wired into `tasks:`, since nothing resolves them yet (see orchestrator.py's self-check/poll gap).
  - Manually verified end-to-end against Docker Postgres with a real 4-task pipeline (linear chain + an `EMAIL_ALERT` task gated on `FAILURE`) — output matches the design exactly; also verified `--output`, unknown-pipeline, and cycle-rejection error paths.
  - 12 new tests (7 for `generate_pipeline_dag` covering the structural cases above, 4 CLI-level, 1 for the new `cfg.fetch_pipeline_detail` query) — 156 total, 100% coverage maintained.
  - **Richer Airflow-facing fields, added later (2026-09-19) per explicit request to make the output easier to wire straight into a real Airflow DAG:** `catchup: false`, `tags` (`[REFRESH_TYPE.lower()]`), and a `default_args` block (`owner`, `retries`, `retry_delay_minutes`, `depends_on_past`, `email_on_failure`). **[ADDITION]**, every value grounded in real reasoning, not arbitrary: `owner` reads `CFG_PIPELINES.CREATED_BY` (new column on `cfg.fetch_pipeline_detail`/`PipelineDetail`, auto-stamped by `trg_set_audit_columns` — the closest real analog to "owner," since there's no dedicated column); `email_on_failure: false` because CLAUDE.md's own `EMAIL_ALERT` handler is the engine's alerting mechanism, and Airflow's native email-on-failure alongside it would double-alert on the same failure; `depends_on_past: false` because the run-id model has no notion of "depends on the previous DAG run" — all ordering comes from `CFG_TASK_DEPENDENCY`/`CFG_PIPELINE_DEPENDENCY`, and leaving Airflow's own past-run gating on would silently add ordering this design doesn't have; `catchup: false` because a run minted via `ux_pipeline_run_one_active` has no meaningful backfill semantics — leaving catchup on would let many missed-interval DAG runs all race to mint/reuse the same active run at once; `retries`/`retry_delay_minutes` are the one genuinely arbitrary pair (1 retry, 5 minutes — no CFG_ column backs them) but deliberately nonzero rather than Airflow's bare default, since `run --task_code`'s idempotent retry-resumes design (CLAUDE.md) makes an Airflow-level retry of the exact same `bash_command` safe by construction.
  - No new test functions needed — 3 existing ones strengthened instead (`fetch_pipeline_detail`'s two tests now assert `created_by`; `generate_pipeline_dag`'s linear-chain test now asserts the new dag-level keys), since the new fields are unconditional dict literals any existing call already exercised structurally. Still 188 total, 100% coverage maintained. Manually re-verified the full generated YAML by eye against real Docker Postgres.
- **Crash detection — done (2026-09-19).** `src/etl_craft/runner.py`'s `run_task()` now forks the actual handler dispatch (`handlers.dispatch(handler)` alone, not the dependency-check/binding work around it) into a child process via `multiprocessing.get_context("fork")` and waits on it, per CLAUDE.md's "Crash detection" section. A child that exits normally — `SUCCESS`, or a caught `HandlerError` → `FAILED` — writes its own terminal `AUD_TASK_RUN_LOG` row, same as always. A child that dies unannounced (non-zero/`None` exit code with no exception the parent ever saw — OOM-kill, segfault) gets `FAILED` written on its behalf by the still-alive parent, but only if the row is still `IN-PROGRESS`, so a child that wrote its real outcome and then hit unrelated exit-time trouble is never overwritten.
  - **[CHOICE]**: `fork`, not `spawn` or a genuine `subprocess.Popen` of a fresh `python -m etl_craft` invocation (unlike `orchestrator.py`'s task-level subprocesses, which deliberately are separate processes to mirror Airflow). Two reasons: the child always builds its own fresh `Engine` via `build_engine(config)` rather than reusing anything the parent has open, so fork's usual "never share an inherited DB connection across a fork" hazard doesn't apply; and fork duplicates the parent's already-patched in-memory state, which is what let the real crash-detection test (below) monkeypatch `handlers.dispatch` to call `os._exit(1)` and observe the parent react correctly — `spawn` would re-import everything fresh in the child and silently ignore any monkeypatch applied in the test process.
  - **[Bug caught and fixed]**: the forked child's `_dispatch_and_record()` calls `build_engine(config)` independently of the parent, which means it needs a real, resolvable secret even in tests that inject a bare `postgres_engine` fixture and otherwise never touch config/secret resolution at all. Two existing tests failed on `ConfigError: secret 'ETL_CRAFT_POSTGRES_DEV_SECRET' not found` until `tests/conftest.py`'s `postgres_engine` fixture set it via `os.environ.setdefault(...)` (never clobbering a real override) — a genuine gap this feature exposed, not a design flaw; the child is correct to insist on a real secret.
  - **[Bug caught and fixed, unrelated but found during this verification pass]**: `tests/conftest.py`'s `craft_connector_on_disk` fixture never checked Postgres reachability itself — with Docker down, any test depending on it alone (several CLI tests don't also depend on `postgres_engine`) errored with a raw `connection refused` traceback instead of skipping, contradicting this file's own documented guarantee that plain `pytest -q` never needs Docker. Fixed by making `craft_connector_on_disk` depend on `postgres_engine` (value unused, but its skip-if-unreachable check now runs first).
  - `[tool.coverage.run]` needed `"multiprocessing"` added to `concurrency` (alongside the existing `"thread"`) plus `parallel = true`, so the forked child's own execution is actually measured — each process writes its own data file, which `pytest-cov` combines automatically at session end. Without this the fork's code silently read as uncovered despite genuinely running, the same class of gap `"thread"` fixed earlier for the concurrency tests.
  - New test (`test_run_task_detects_a_child_that_dies_unannounced`, `tests/test_integration.py`) genuinely exercises the crash path against real Postgres: monkeypatches `dispatch` to `os._exit(1)`, then asserts the row still ends up `FAILED` with a "died unexpectedly" message — not simulated any other way, since this is exactly the scenario no in-process mock can prove.
  - 157 tests total (up from 156), 100% coverage maintained (all of `runner.py`, including the forked child's own lines, now shows 100%).
- **Data DB / warehouse connector layer — done (2026-09-19).** `src/etl_craft/warehouse.py`'s `build_data_engine()` — the last piece of the resolver → CLI → `generate-yml` → connector/auth layer build order.
  - **Open question #1, resolved: the Data DB section is called `[Warehouse]`.** It's the term CLAUDE.md itself already uses throughout ("Data DB / warehouse"), reads as a plain noun rather than the rejected Informatica-shaped names, and is a one-line rename in `config._parse_config` if a different name is preferred later. **[CHOICE]**: unlike `[Postgres]`, `[Warehouse]` is optional at parse time — `config.warehouse` is `None` when the section is absent, since most existing commands (`list`/`graph`/`set-execution-mode`/`configure`/`run --init-only`, and `run`/`run --task_code` until a real `HANDLER=SQL` implementation lands) never touch the Data DB at all. `build_data_engine` itself raises a clear `ConnectionError_` if called with no `[Warehouse]` configured and no profile passed explicitly.
  - **[ADDITION], the actual design problem this layer exists to solve:** per CLAUDE.md's Non-goals, the engine must never import a third-party SQLAlchemy dialect directly, yet the Data DB can be "any SQLAlchemy-supported relational engine" — so `db.py`'s existing approach (hand-written `psycopg.connect(...)` per `auth_mode`) can't generalize here; there's no single driver to hardcode. `warehouse.py`'s `_password_creator` solves this by building a real `sqlalchemy.engine.URL` from the profile, then — inside the `creator` callable `create_engine` invokes at each pool checkout — asking that URL's own resolved dialect to turn itself into raw DBAPI connect args via `dialect.import_dbapi()` / `dialect.create_connect_args(url)`, the exact mechanism SQLAlchemy's own `DefaultDialect.connect()` uses internally. This works against whatever dialect is actually installed (Postgres today, Snowflake/Databricks/etc. later as optional extras a team installs itself) without this module ever importing a specific driver — verified for real against Docker Postgres, standing in as "some installed dialect," not because the Data DB is expected to be Postgres in practice.
  - `translate_jdbc_url()` splits a `jdbc:<scheme>://host[:port]/database[?query]` URL into a SQLAlchemy dialect name + parts, via a deliberately small, non-exhaustive scheme→dialect map (`postgresql`→`postgresql+psycopg`, `mysql`→`mysql+pymysql`); an unmapped scheme passes through unchanged as the dialect name (correct whenever the two already match, e.g. `oracle`/`mssql`). **Flagging a real limit, not silently papering over it**: a vendor whose JDBC URL shape isn't `scheme://host[:port]/database[?query]` at all — Snowflake's account-identifier-as-host form, Oracle's `thin:@` form — isn't handled by this translator and would need its own parsing added once that vendor is actually chosen, not guessed at now.
  - `auth_mode=password` is the only one implemented for the Data DB, for the reason above. `key_file`/`token`/`sso` all raise `NotImplementedError` — same as `db.py`'s existing `token`/`sso` stubs, but `key_file` is unimplemented here too (unlike `db.py`, where it's fully built for Postgres): how a private-key/cert credential maps to DBAPI connect args is genuinely dialect-specific (Postgres SSL client certs and, say, Snowflake private-key auth share nothing), so there's nothing generic to build yet.
  - 12 new tests: 4 for `translate_jdbc_url`, 3 for the auth-registry creators (mocking the generic DBAPI-connect step, not a specific driver), 2 for `build_data_engine`'s own error paths, 3 for `config.py`'s new optional `[Warehouse]` parsing — plus one real-Postgres integration test (`test_build_data_engine_connects_for_real`) proving the generic mechanism, not just the mocked unit-level version, genuinely opens a connection.
  - **[Bug caught and fixed, same class as crash detection's]**: the new integration test initially had no `postgres_engine` fixture dependency (it never uses the fixture's own engine — `build_data_engine` opens its own), so it skipped the reachability guard entirely and hard-failed with a raw connection-refused traceback when Docker was down, same gap as `craft_connector_on_disk` above. Fixed the same way: depend on `postgres_engine` for its skip check alone.
  - 169 tests total (up from 157), 100% coverage maintained, stable across repeated runs.
  - **Not included**: `Cloning`'s actual copy mechanism (merge Engine DB tables into the Data DB) — `build_data_engine` is what it would connect with, but the copy logic itself isn't built.
  - **Real ClickHouse connectivity, added later (2026-09-19) per explicit request** to actually prove the generic mechanism against a dialect other than Postgres, not just assert it in theory. `clickhouse-sqlalchemy` is registered as `[project.optional-dependencies].clickhouse` (never a hard dependency of the base package, per CLAUDE.md's own Non-goals — it's also in the `dev` group, via a self-referential `etl-craft[clickhouse]` entry, so this repo's own test suite has it) — no code changes to `translate_jdbc_url`/`warehouse.py` were needed at all: `jdbc:clickhouse://host:port/db` already passes the `clickhouse` scheme through unchanged (nothing in the scheme map catches it), and that happens to be exactly `clickhouse-sqlalchemy`'s own registered plain-`clickhouse` dialect entry point (verified directly — `importlib.metadata.entry_points(group="sqlalchemy.dialects")` — before writing anything, not assumed), which resolves to its HTTP driver. First real proof this module's "never hardcode a driver" design actually holds up against a second, genuinely different dialect.
    - `docker-compose.yml` gets a `clickhouse` service (image `clickhouse/clickhouse-server:24`, host port `58123` not `8123` for the same reason Postgres uses `55432`) alongside Postgres; `.github/workflows/ci.yml` gets a matching native `services:` entry so CI exercises this for real too, not just locally. `tests/conftest.py`'s new `clickhouse_engine` fixture mirrors `postgres_engine` exactly — skips cleanly (not a hard failure) if ClickHouse is unreachable *or* the `clickhouse` extra isn't installed, since `_reachable`'s broad `except Exception` catches both the same way.
    - New test `test_build_data_engine_connects_to_real_clickhouse` — 1 more test, 188 total (up from 187 after the `validate` commit, since this landed before the `generate-yml` enrichment above), 100% coverage maintained.
- **`validate` — done (2026-09-19).** `src/etl_craft/validate.py`, wired into `cli.py` as the last unbuilt row in the CLI surface table. Per CLAUDE.md's own description — "Config integrity check — including the cross-database checks (e.g. single-column PK on a `TARGET_TABLE`) that no Engine DB constraint can enforce" — two checks make up the scope, both concrete and explicitly called for elsewhere rather than invented: **[ADDITION]** flagged since CLAUDE.md's "including" implies more could exist later; these two are simply the only ones anything currently calls for.
  - `validate_graphs()`: runs `resolver.build_graph` across *every* active pipeline in one pass (not just one named pipeline the way `graph`/`generate-yml` do), catching cycles/self-dependencies/unknown-task-ids — exactly the kind of thing no Engine DB `CHECK` constraint can catch, since `CFG_TASK_DEPENDENCY` rows are only meaningful in aggregate.
  - `validate_business_rule_keys()`: the literal check `sql/schema.sql`'s own `CFG_BUSINESS_RULES` table comment names — "every `TARGET_TABLE` is required to have a single-column primary key ... enforce it at `validate` time via introspection, not here." Uses SQLAlchemy's `Inspector.get_pk_constraint()` against the Data DB (same dialect-agnostic spirit as `warehouse.py`, never a hardcoded driver call), comparing case-insensitively against `BUSINESS_RULE_KEY_COLUMN` (Postgres folds unquoted identifiers to lowercase — same pitfall class noted earlier in this file). Reports, per active business rule: the table doesn't exist, the table has zero or >1 PK columns, or the PK column doesn't match `BUSINESS_RULE_KEY_COLUMN`. If active business rules exist but no `[Warehouse]` is configured, that absence is itself reported as an issue rather than silently skipped.
  - Both functions collect every issue found into a flat `ValidationIssue` list rather than raising on the first one — one bad pipeline or rule doesn't hide the rest of the report, and it's why `cfg.py` gained one new query (`fetch_business_rule_targets`, the `TARGET_TABLE`/`BUSINESS_RULE_KEY_COLUMN` read for every active business rule across all pipelines).
  - `cli.py`'s `_validate_command`: prints `validate: OK` and exits 0 if the issue list is empty; otherwise prints each `[category] message` and exits 1. A `ConfigError` while building the Data DB engine (e.g. an unresolvable secret) or a `SQLAlchemyError` while actually checking against it (e.g. connection refused) are each caught and printed as a clean one-line `error: ...` with exit code 2, distinct from a genuine validation failure (exit 1) — matching the rest of the CLI's error-handling convention.
  - 18 new tests (2 for `fetch_business_rule_targets`, 3 for `validate_graphs`, 7 for `validate_business_rule_keys` covering every branch — missing table, no PK, composite PK, mismatched column, case-insensitive match, no warehouse configured, empty — 6 CLI-level tests covering the OK path with and without a configured warehouse, both error-status codes, and the missing-warehouse-with-active-rule path) — 187 total (up from 169), 100% coverage maintained, stable across repeated runs.
  - Manually smoke-tested end-to-end against Docker Postgres with a real pipeline/task/business-rule/table: clean state → OK; matching single-column PK → OK; `TARGET_TABLE` pointed at a nonexistent table → correctly reported and non-zero exit.
- **Cross-pipeline dependency polling — done (2026-09-19).** `src/etl_craft/crosspipe.py`, wired into both `runner.py` and `orchestrator.py`. The last piece of "Dependency resolution & polling" — resolving `CFG_PIPELINE_DEPENDENCY` edges and genuinely cross-pipeline `CFG_TASK_DEPENDENCY` rows (`DEPENDS_ON_PIPELINE_ID != PIPELINE_ID`) at runtime, since neither has a DAG-native equivalent in any orchestrator. Two open questions were resolved directly with the user before writing any code (not guessed at): **open question #4 is resolved** — the percentage-based poll schedule (70%/80%/+10% per poll of the dependency's average run duration, capped at 30 polls or a 1-hour wall clock) is the only real mechanism; the "exponential backoff of 60 minutes" mention elsewhere in this file is stray/superseded. And: pipeline-level `HAS_DATA` (schema-allowed on `CFG_PIPELINE_DEPENDENCY` but never defined at that grain, since `AUD_PIPELINES_RUN_LOG` has no `TARGET_COUNT` of its own) means "the run succeeded AND at least one of its own tasks reported `TARGET_COUNT > 0`" via `AUD_TASK_RUN_LOG` — the pipeline-grain analogue of `resolver.py`'s existing task-level rule.
  - `check_pipeline_dependencies()`/`check_task_cross_pipeline_dependencies()`: for each active edge, poll only while the dependency's own latest run is genuinely `IN-PROGRESS` (CLAUDE.md: "if it isn't, go straight to the tracker comparison rather than polling blindly"); then compare against `AUD_PIPELINE_DEPENDENCY_TRACKER`/`AUD_TASK_DEPENDENCY_TRACKER` — a candidate run satisfies the edge only if it's newer than whatever that specific edge already consumed (no tracker row yet == never consumed == any qualifying run counts) *and* matches the edge's own `DEPENDENCY_TYPE`, mirroring `resolver.py`'s SUCCESS/FAILURE/ALWAYS/HAS_DATA semantics exactly. `consume_pipeline_dependency_edges()`/`consume_task_dependency_edges()` advance the tracker — called only from the gated party's own finalize point, per CLAUDE.md ("the tracker only updates *after* the gated task/pipeline completes"), so a run that dies before finishing never burns a qualifying upstream run it didn't actually get to use.
  - **[CHOICE], found during review before anything shipped, not after:** every public function in `crosspipe.py` takes an `Engine`, not a `Connection`, and opens a fresh short-lived connection per query rather than holding one open across a poll's real `sleep()` calls (which can span most of an hour). An earlier version took a bare `Connection` throughout — functionally correct in tests, but wrong for a real deployment: many concurrent Airflow-triggered tasks each polling for up to an hour while holding one pooled connection idle is a real path to connection-pool exhaustion, not a theoretical one. Caught and fixed before wiring it into `runner.py`/`orchestrator.py`, which meant rewriting ~20 already-passing tests to use genuinely committed data (`tests/conftest.py`'s new `two_committed_pipelines` fixture plus `insert_committed_pipeline_run`/`insert_committed_task_run`/`insert_committed_pipeline_dependency`/`insert_committed_cross_pipeline_task_dependency`) instead of the rolled-back `pg_conn`, since each function now opens its own connections and can't see uncommitted work.
  - **[Bug caught and fixed, real not theoretical]**: `CAST(:after_run_id AS BIGINT) IS NULL OR ... > CAST(:after_run_id AS BIGINT)` was needed in the "newer than last consumed" query — a bind parameter compared only via `IS NULL` (the other side of the `OR` is what compares it against a real column) leaves Postgres unable to infer its type on its own (`AmbiguousParameter`). The first fix attempt, `:after_run_id::bigint`, silently failed a different way: SQLAlchemy's `text()` treats a literal `::` right after a bind name as an escaped colon, not a cast, so the substitution never happened at all and the literal text reached psycopg unparsed. Found by actually running it against real Postgres, not assumed.
  - **Testability**: both poll loops (`_wait_for_pipeline_dependency_to_settle`/`_wait_for_task_dependency_to_settle`) take injectable `sleep`/`now` callables (defaulting to `time.sleep`/`datetime.now(UTC)`), so tests prove real behavior — poll-then-settle (a fake `sleep` flips the DB row's status on its second call), poll-cap exhaustion, and wall-clock-deadline-hit-mid-loop (a fake clock that jumps hours per "sleep") — without ever actually waiting. `_next_poll_delay()` (the interval math itself) is a pure function, tested directly with plain assertions in `test_unit.py`.
  - **[ADDITION]**: `DEFAULT_ASSUMED_DURATION_SECONDS` (300s) and `MIN_POLL_INTERVAL_SECONDS` (1s) are this module's own engineering choices, nowhere specified. The former seeds the percentage schedule for a pipeline/task pairing with no run-duration history yet to compute 70%/80%/etc. of. The latter stops an "already overdue" check (elapsed already past the target fraction) from hot-looping through all 30 polls near-instantly with an effectively-zero delay between them.
  - **Wired into `runner.py`'s `run_task()`** — per direct, detailed instruction on exactly what should happen on every invocation, regardless of who triggers it (Airflow or a human): resolve the pipeline and its dependency graph; resolve the task within it; check same-pipeline *and* cross-pipeline dependencies (poll if the checked dependency is itself running); if a dependency will never be satisfied, don't error — record the task `SKIPPED` and still exit 0, since "correctly gated off by design" isn't a failure Airflow's own retry/alerting should react to. **[DEVIATION], a real, deliberate behavior change to already-shipped code**: this replaces the previous same-pipeline-only `DependenciesNotMetError` (raised, exit 1, nothing written to `AUD_TASK_RUN_LOG`) — same-pipeline and cross-pipeline unmet dependencies are now handled identically, both ending in a `SKIPPED` row and exit 0. `run_task` also short-circuits to `SKIPPED` immediately if the `pipeline_run_id` it resolves is itself already `SKIPPED` (see below), without attempting any dependency check of its own. After dispatch (success or failure), `consume_task_dependency_edges()` runs unconditionally — completion, not success specifically, is what the tracker cares about.
  - **Wired into `orchestrator.py`'s `init_pipeline_run()` and `run_pipeline()`** — both gate on `check_pipeline_dependencies()`, but *only* on the path that's about to mint a genuinely new run (an already-`IN-PROGRESS` run's own gate already passed when it started — checked via a new `runlog.fetch_active_pipeline_run_id`). `--force` bypasses this gate entirely in `run_pipeline`, per CLAUDE.md ("`--force` bypasses all dependency/state checks"); `init_pipeline_run` has no `--force` concept to begin with (pre-existing, unchanged). **[CHOICE]**: when the gate finds an unmet dependency, the run is still minted (so any task invocation already waiting on it has a real `pipeline_run_id` to bind to) but immediately finalized `SKIPPED` rather than left `IN-PROGRESS` — this is exactly what `run_task`'s new short-circuit above reacts to. `run_pipeline` also now calls `consume_pipeline_dependency_edges()` after every finalize (the empty-pipeline early-return, and the normal wave-loop completion), matching CLAUDE.md's "after the gated pipeline completes."
  - **[Known gap, surfaced but not fixed here at the time]**: once a run's own gate passes and it genuinely starts under `Mode=orchestrator`, nothing ever marked `AUD_PIPELINES_RUN_LOG` `SUCCESS`/`FAILED` — `finalize_pipeline_run` was only called from `run_pipeline()`, refused under that mode. **Closed later the same day — see "`--finalize-only`, closing the orchestrator-mode finalize gap" below.**
  - 25 new tests (5 pure poll-math in `test_unit.py`; ~20 in `test_integration.py` covering both `crosspipe.py` directly and its integration into `run_task`/`init_pipeline_run`/`run_pipeline`) — 219 total (up from 188 before this and the two entries above), 100% coverage maintained, stable across repeated runs.
  - Manually verified end-to-end against Docker Postgres with two real pipelines and a `SUCCESS`-typed pipeline dependency: `--init-only` before the upstream ever ran → minted + immediately `SKIPPED`; a task run under that `SKIPPED` run → `SKIPPED`, exit 0; upstream marked `SUCCESS`; a fresh `--init-only` call → a genuinely new, `IN-PROGRESS` run (not reusing the `SKIPPED` one); the same task run again → proceeded past the gate and failed on the stub handler, same as any other task — proving it was the gate itself that mattered.
- **Configurable Airflow-facing settings + optional global DAG — done (2026-09-19).** Per explicit request, the `default_args`/`catchup`/`tags`/`email_on_failure` fields `generate-yml` previously hardcoded (see the "Richer Airflow-facing fields" entry above) became real, three-tier-resolved settings, plus a wholly new optional cross-pipeline trigger DAG.
  - **[ADDITION, post-signoff schema change]** `CFG_PIPELINES` gains 7 nullable columns: `CATCHUP`/`DEPENDS_ON_PAST`/`EMAIL_ON_FAILURE` (boolean), `RETRIES`/`RETRY_DELAY_MINUTES` (integer, `CHECK >= 0` when set), `TAGS`/`EMAIL_RECIPIENTS` (`VARCHAR[]`). `EMAIL_RECIPIENTS` exists specifically because Airflow's own `email_on_failure` is inert without a recipient list to send to — confirmed with the user this is needed alongside the boolean toggle, not guessed at. Flagged in a new "POST-SIGNOFF CHANGES" block at the end of `sql/schema.sql`, separate from the original sign-off summary, since the schema was already signed off (2026-09-19) before this — a real, tracked departure from that state, not folded in silently. `sql/schema_test.sql` gained a round-trip test (all 7 columns, including the array types) and two `EXPECT FAIL` cases for the new `CHECK` constraints; both verified against real Docker Postgres.
  - **`craft-connector.yml` gains a new `[Orchestrator]` section** (`config.OrchestratorConfig`): `Global_dag` (bool, **defaults to `false`** per explicit instruction), plus a global default for each of the 7 fields above (all optional, `None` when unset).
  - **Three-tier resolution**, implemented as `generate_yml._resolve()` and used for every one of the 7 fields: (1) the pipeline's own `CFG_PIPELINES` column, if not `NULL`; (2) `craft-connector.yml`'s `[Orchestrator]` section, if set; (3) the same final hardcoded default `generate-yml` already used (1 retry, 5 min delay, `depends_on_past`/`catchup`/`email_on_failure` all `false`, `tags` <- `[REFRESH_TYPE.lower()]`). `owner` stays derived from `CREATED_BY`, unchanged — not part of this resolution, no per-pipeline/global override exists for who registered a pipeline. `default_args["email"]` is only emitted when `email_on_failure` resolves `true` — never a spurious empty list when notifications are off.
  - **The optional global DAG** (`generate_yml.generate_global_dag()`, `etl-craft generate-yml --global`, mutually exclusive with `--pipeline_code` at the argparse level, matching `run`'s `--task_code`/`--init-only` pattern): refused with a clear error unless `[Orchestrator].Global_dag: true`. **[CHOICE], confirmed with the user**: each node represents triggering that pipeline's own already-generated DAG — Airflow's `TriggerDagRunOperator`, described via a `trigger_dag_id` key in this hand-rolled shape, never imported (same spirit as `bash_command` in the per-pipeline DAG) — gated on the `CFG_PIPELINE_DEPENDENCY` edge's own `DEPENDENCY_TYPE`. This was the one part of the request needing real confirmation: `run --pipeline_code X` (bare, no `--task_code`) is already refused under `Mode=orchestrator`, so a global DAG's node literally cannot shell out to it the way per-pipeline DAG nodes shell out to `run --task_code` — `TriggerDagRunOperator`-shaped nodes are the only mechanism that actually works under real Airflow. Every pipeline touched by at least one active edge (either side) gets a node; a pipeline with no pipeline-level dependencies at all isn't included, since it has no cross-DAG ordering to represent. `GLOBAL_DAG_ID = "etl_craft_global_orchestration"` — this module's own naming choice, changeable before anything depends on it.
  - **[Bug found and fixed, environmental not code]**: the new `cfg.fetch_all_pipeline_dependency_edges()` is genuinely global (unlike every other `cfg.py` query, which is scoped to one `pipeline_id`) — the first query in the whole test suite to be affected by leftover rows from `sql/schema_test.sql` (`PL_A`/`PL_B`, ...), which `make db-schema-test` had been leaving permanently in the shared dev Postgres database (`etl_craft`) rather than a disposable one, contradicting `schema_test.sql`'s own header instructions. Fixed the Makefile target itself: it now creates/applies-schema-to/drops a genuinely disposable `etl_craft_schema_test` database inside the same container, never touching the persistent dev database `make test` and everything else uses.
  - 15 new tests (4 in `test_unit.py` for `[Orchestrator]` parsing; 11 in `test_integration.py` — the new global query, three-tier resolution including the pipeline-override-wins and global-fallback cases, `email` key presence/absence, `generate_global_dag`'s node inclusion/exclusion rules, and CLI-level `--global` coverage including the disabled-by-default and mutual-exclusivity error paths) — 234 total (up from 219), 100% coverage maintained, stable across repeated runs.
  - Manually verified end-to-end against Docker Postgres with two real pipelines: a pipeline-level `RETRIES` override correctly beat a conflicting global default; a pipeline with no override correctly picked up the global default instead; `email_on_failure`/`email` correctly propagated from `[Orchestrator]` to both pipelines' `default_args`; `generate-yml --global` correctly emitted both pipelines with the right `trigger_dag_id`/`depends_on` shape.
- **`--finalize-only`, closing the orchestrator-mode finalize gap — done (2026-09-19).** The gap flagged in the cross-pipeline-polling entry above (nothing ever marked `AUD_PIPELINES_RUN_LOG` `SUCCESS`/`FAILED` under `Mode=orchestrator`, since `finalize_pipeline_run` only lived in `run_pipeline()`, refused under that mode) is closed with a synthetic *last* step, mirroring `--init-only`'s synthetic first one.
  - `orchestrator.finalize_active_run(engine, config, pipeline_code)`: finds `pipeline_code`'s currently active (`IN-PROGRESS`) run — raising a clean `RunLogError` if there isn't one (nothing to finalize) — computes `SUCCESS`/`FAILED` from every active task's own current status (the exact same rule `run_pipeline()`'s own finalize step already used), writes it, and calls `consume_pipeline_dependency_edges()`. **Refactored, not just added**: extracted the shared logic (`_finalize_from_task_states`) so `run_pipeline()` and `finalize_active_run()` share one implementation rather than two copies that could drift — `run_pipeline()`'s own finalize step is now a thin caller of it, unchanged in behavior (verified: same tests, same outcomes). Legal under both modes, like `--init-only` — its only real use case is `Mode=orchestrator`, but nothing about it depends on mode.
  - **CLI**: `run --pipeline_code X --finalize-only`, added to the existing `--task_code`/`--init-only` mutually-exclusive group (now three-way). Exits 0 on `SUCCESS`, 1 on `FAILED`, matching every other `run` outcome's exit-code convention.
  - **`generate-yml`**: every generated DAG now gets a synthetic `__finalize__` task (`etl-craft run --pipeline_code X --finalize-only`), depending via `ALWAYS` on every *leaf* task — one nothing else depends on (computed as: not the `depends_on_task_id` of any same-pipeline edge). A pipeline with no tasks at all falls back to depending directly on `__init__`, same pattern as any ordinary task with no dependencies of its own. Verified against a diamond-shaped graph (two parallel branches converging on nothing further) that `__finalize__` waits on *both* leaves, not just one.
  - 8 new tests (4 in `test_integration.py`'s orchestrator.py section — success, failure-from-an-unsettled-task, the no-active-run error, and legal-under-orchestrator-mode; 2 in generate_yml.py's section for the diamond-leaf case and the no-tasks fallback; 3 CLI-level covering both exit codes and the new mutual-exclusivity case) plus the 3 pre-existing `generate-yml` structural tests updated to account for the new `__finalize__` task in every DAG's task set — 242 total (up from 234), 100% coverage maintained, stable across repeated runs.
  - **Unrelated, found and resolved during this same session**: an in-progress `git merge origin/main` (started outside this session, likely via an IDE's own Git integration) was sitting with an unresolved conflict on `.github/workflows/ci.yml` — origin/main had independently added the same `PGHOST`-style psql fix already on this branch (see the CI bug fix noted earlier in this file). Resolved by keeping this branch's version, a confirmed strict superset (byte-for-byte diffed against the pre-merge branch state) that also includes the ClickHouse service/health-check/env-var additions absent from origin/main; completed with an ordinary merge commit, nothing lost from either side.
  - Manually verified end-to-end against Docker Postgres under `Mode=orchestrator`: `--init-only` minted an `IN-PROGRESS` run; a task's `SUCCESS` was stamped; `--finalize-only` correctly resolved and wrote `SUCCESS`; a second `--finalize-only` call (nothing left `IN-PROGRESS`) correctly produced a clean error instead of silently doing nothing or crashing; `generate-yml`'s own output confirmed `__finalize__` correctly depends only on the pipeline's real leaf task.
- **The execution engine itself — SQL actions, business rules, Python scripts — done (2026-09-19).** Per detailed, explicit instruction, the last major unbuilt piece: every `HANDLER` body except `EMAIL_ALERT` now genuinely executes, closing out `handlers.py`'s stub registry. Four new modules — `execution.py` (shared `TaskExecutionContext`/`HandlerError`/`HandlerResult`, split out purely to avoid a circular import between `handlers.py` and the three it dispatches to), `sql_actions.py`, `business_rules.py`, `scripts.py` — plus `handlers.py`/`runner.py` rewritten to build and thread a `TaskExecutionContext` through the crash-detection fork instead of a bare `(config, task_run_id, handler)` tuple.
  - **[DEVIATION, post-signoff] `CFG_TASK_PARAMS` renamed to `CFG_TASK_PARAMETERS`, `VARIABLE_NAME`/`VARIABLE_VALUE` → `PARAMETER_NAME`/`PARAMETER_VALUE`** (and `TASK_PARAM_ID` → `TASK_PARAMETER_ID` to match) — corrected per explicit instruction mid-build, before any other tooling could come to depend on the old names. `cfg.fetch_task_parameters`/`fetch_sibling_target_sql_action` and every SQL string referencing the table were updated together with the schema change.
  - **`sql_actions.py`** wraps the closed action vocabulary CLAUDE.md names but never defined: `CREATE_TABLE`, `SETUP_TABLE`, `OVERWRITE_TABLE`, `SCD1_MERGE`, `SCD2_MERGE`, `DROP_TABLE`, `DELETE_ROWS`. **[ADDITION]** `CFG_TASK_PARAMETERS.PARAMETER_NAME` vocabulary (the module's own module docstring is the authoritative reference, mirrored in `schema.sql`'s `COMMENT ON TABLE`): `SQL_ACTION`, `TARGET_OBJECT` (`schema.table`, no database prefix), `SOURCE_SQL` (the bare read-only SELECT), `MERGE_KEY`/`MERGE_COMPARE_COLUMNS` (pipe-separated column lists), `HARD_DELETE`.
    - **Audit columns, per explicit instruction**: the author's own SELECT must never project an engine-managed column — the engine appends exactly the set each action calls for, positioned after the SELECT's own business columns. Every action always appends `PIPELINE_RUN_ID` (CLAUDE.md's pre-existing auto-stamp rule); `CREATE_TABLE` adds nothing else; `OVERWRITE_TABLE` adds `UPDATE_DATE`; `SCD1_MERGE` adds `CREATE_DATE`/`CREATED_BY`/`UPDATE_DATE`/`UPDATED_BY`/`DELETE_FLAG`; `SCD2_MERGE` adds those plus `ACTIVE_FLAG`. `SETUP_TABLE` — which only ever establishes a target's *shape* ahead of the real writer — infers its own audit-column set by finding whichever other active task in the same pipeline actually writes that `TARGET_OBJECT` (`cfg.fetch_sibling_target_sql_action`) and mirroring that writer's set; falls back to `CREATE_TABLE`'s (none) if no sibling is found. **[CHOICE]** `CREATED_BY`/`UPDATED_BY` are stamped with the active `[Warehouse]` profile's `user`, not a SQL `current_user()` call — the Data DB has no "one Postgres role per human" convention the way the Engine DB does, so a literal bind value is the only thing that means the same thing across every dialect.
    - **Schema check / evolution, per explicit instruction, with no separate schema-registry table**: the task's own `SOURCE_SQL` is materialized into a uniquely-named temp table (which also supplies row counts and drives every merge statement — the SELECT only ever executes once), then compared via `information_schema.columns` on both sides against the target's business columns (its full column set minus `PIPELINE_RUN_ID` and the action's own audit columns). A staged SELECT missing a column the target already has is always a hard failure, evolvable or not — this only ever adds columns, never drops one. A genuinely new column fails with a clear reason when `CFG_TASKS.SCHEMA_EVOLUTION` is false (the default); when true, the target is rebuilt with the column added at the position the SELECT's own column order implies, existing rows backfilled `NULL`. **[CHOICE]**: Postgres has no `CREATE OR REPLACE TABLE` (unlike some other dialects) — "use CREATE OR REPLACE" (as literally instructed) is implemented as the portable three-statement equivalent every ANSI-ish engine supports instead: `CREATE TABLE <new-shape> AS SELECT ... FROM <target>`, `DROP TABLE <target>`, `ALTER TABLE <new-shape> RENAME TO <target>`.
    - **`qualify()`**: `CFG_TASK_PARAMETERS.TARGET_OBJECT`/`CFG_BUSINESS_RULES.TARGET_TABLE` are always just `schema.table`, deliberately environment-agnostic per explicit instruction — the database/catalog prefix always comes from the active `[Warehouse]` profile, producing the literal ANSI `catalog.schema.table` form. The same `schema.table` pair means a different real object in dev/uat/prod without any `CFG_` row changing across a promotion.
    - **Row counts, per explicit instruction ("implement ways to easily get impacted row counts... we correct impact counts")**: never trusts a DBAPI cursor's own `rowcount` — every count is computed with its own `COUNT(*)`/`EXISTS` query, evaluated *before* the mutating statement it describes runs.
    - **`DROP_TABLE`** is refused unless some other active task in the same pipeline creates that exact `TARGET_OBJECT` via `SQL_ACTION=CREATE_TABLE` — "drop tables created by this pipeline only using create_table," read as a safety guard against pointing a drop at an unrelated table, not literal permission to drop everything the pipeline has ever created.
    - **`DELETE_ROWS`** takes a `HARD_DELETE` param (default false/soft): `true` issues a real `DELETE`; anything else instead does `UPDATE ... SET DELETE_FLAG = 'Y'`, per explicit instruction.
    - Atomicity/idempotency, per explicit instruction: every action's statements run inside the one Data DB transaction `handlers.py` already opens — a failure partway rolls back everything, so a retry always starts from the target's last genuinely-committed state. Idempotency follows for free from each action re-deriving its effect from current state every run.
    - **[Bug caught and fixed before shipping]**: the schema-evolution rebuild's final `ALTER TABLE <new> RENAME TO <name>` used the *new* table's own bare name as the rename target instead of the *original* target's name — Postgres correctly rejected renaming a table to its own already-existing name (`DuplicateTable`). Found by actually exercising the evolution path end-to-end against Docker Postgres (a real `new_col` insertion), not assumed from reading the code.
  - **`business_rules.py`** drives `CFG_BUSINESS_RULES` per explicit instruction: `SELECT DISTINCT t.<key_column> FROM <target_table> t WHERE <scope> AND EXISTS (<business_rule_sql>)` flags rows (a new `AUD_BUSINESS_RULES_RESULTS` row, unless already actively flagged); the same query with `NOT EXISTS` finds rows that stopped violating the rule and deactivates their existing active flag. **[ADDITION]** `BUSINESS_RULE_SQL` is a correlated condition, not standalone — the outer target row is aliased `t`; nowhere else pins this convention down, and there's no reliable way to validate a rule follows it without a real SQL parser (ruled out per Non-goals).
    - **[CHOICE]** "a manual br task execution will run for all data" is read as: `--force` (the CLI's existing "bypass normal gating" signal) switches the scope predicate from `t.PIPELINE_RUN_ID = <this run>` to unconditional `1=1`. A pipeline-triggered run always scopes to `PIPELINE_RUN_ID`, which for a FULL-refresh target still covers every row anyway, since a full refresh re-stamps that column on every row it rewrites.
    - **[ADDITION, post-signoff schema change]** `CFG_BUSINESS_RULES.BUSINESS_RULE_TYPE` gains a third value, `REPORT`, alongside `INCOMPLETE`/`REJECT` — "if the team wants reportability, they should be able to do it as well." `AUD_BUSINESS_RULES_RESULTS.STATUS` gets the same third value (it mirrors the flagged row's rule type). Execution is identical for all three types; only the vocabulary widened.
    - **Cross-database mechanics**: target data lives in the Data DB, `AUD_BUSINESS_RULES_RUN_LOG`/`AUD_BUSINESS_RULES_RESULTS` live in the Engine DB — no single SQL statement can join them, so flagged/passing keys are always pulled into Python first, then written to the Engine DB as a second step, the same shape `crosspipe.py` already uses for its own cross-connection comparisons.
    - **[Bug caught and fixed before shipping, not after]**: the first version shared one Engine DB transaction (`cfg_conn`, opened by `handlers.py`) across every rule in a task. When a later rule's Data DB query raised, the enclosing transaction rolled back on the way out of `handlers.py`'s `with engine.begin()` block — taking the "FAILED" status this module had just written for the *broken* rule down with it, along with any earlier rules' genuinely-succeeded results in the same task. Found by asserting `AUD_BUSINESS_RULES_RUN_LOG.STATUS == 'FAILED'` after a deliberately malformed rule and watching the row not exist at all. Fixed by having `business_rules.execute()` take the Engine DB `Engine` directly (not a shared `Connection`) and open its own independently-committed transaction per rule, so one rule's failure never erases another rule's — or its own — already-written outcome.
  - **`scripts.py`** (`HANDLER=PYTHON`) is the one piece of this round with no detailed spec to work from beyond CLAUDE.md's single existing rule ("the engine does not auto-inject the run id... the team's own script is responsible for fetching and including `pipeline_run_id`") — **[ADDITION]**, flagged for confirmation before a real script is written against it: `CFG_TASKS.SCRIPT_NAME` runs as `<python> <script>`, inheriting this process's environment plus `ETL_CRAFT_PIPELINE_CODE`/`ETL_CRAFT_TASK_CODE`, in the current working directory (so the script can resolve its own `pipeline_run_id`, e.g. via `etl-craft`'s own `craft-connector.yml` in the same directory). A nonzero exit becomes a `HandlerError` built from stderr (or stdout if stderr is empty). The script may optionally print one trailing JSON line reporting `ingestion_count` (→ `HandlerResult.source_count`) and/or `latest_offset_value`/`latest_offset_type` (→ upserts `AUD_TASK_OFFSET_TRACKER` for that task) — both offset keys must be present together or neither is applied; an unrecognized `latest_offset_type` is a clean `HandlerError`, not a silent write.
  - **`handlers.py` rewritten**: `dispatch(engine, ctx)` (was `dispatch(handler)`) now actually calls into the three real modules, builds the Data DB engine only for `SQL`/`BUSINESS_RULES` (disposed after the one task's use, same lifecycle as `validate.py`'s own pairing), and — since this all runs inside `runner.py`'s crash-detection fork, where anything that isn't a `HandlerError` crashes the child outright and loses its real message to the parent's generic "died unexpectedly" fallback — wraps `ConfigError`/`SQLAlchemyError` into a `HandlerError` with the original message preserved. **[Bug caught and fixed before shipping]**: without that wrapping, a missing `[Warehouse]` section (`ConnectionError_`, a `ConfigError` subtype, not a `HandlerError`) crashed the forked child instead of failing the task cleanly — caught by a test asserting the real message appears, not the generic crash fallback.
  - **4 pre-existing tests broke and were updated**, not regressions — they asserted the old "every `HANDLER` is a stub" behavior, now superseded: two `HANDLER='SQL'`-stub-message assertions became `[Warehouse]`-not-configured assertions (still true — `make_config()`'s test helper has no `[Warehouse]` section); the `dispatch` monkeypatch test's lambda signature updated from `(handler)` to `(engine, ctx)`; `test_dispatch_unknown_handler_rejected` updated to the new `dispatch(engine, ctx)` call shape.
  - 34 new tests (242 → 276): pure-logic unit tests for `substitute_pipeline_id`/`qualify`/`active_database`/`_parse_trailing_json`, plus real-Postgres integration tests (Postgres standing in as *both* the Engine DB and the Data DB, `make_config(warehouse=True)`, same spirit as `warehouse.py`'s own tests) covering every action's success and failure paths, `SETUP_TABLE`'s three-way audit-column inference, a full multi-run `SCD2_MERGE` history scenario (insert new → deactivate-and-reinsert a real value change → leave an unrelated row alone), schema evolution's before/after column shape (proving the new column lands at the right *position*, not just that it exists), `BUSINESS_RULES`' flag/deactivate/force-all-data paths, and `scripts.py`'s success/failure/offset-tracker paths. 100% coverage maintained, stable across repeated runs.
  - Manually verified beyond the automated suite, against Docker Postgres standing in as the warehouse: a 3-run `SCD2_MERGE` lifecycle (bootstrap via `SETUP_TABLE` → all-new insert → a genuine value change producing a deactivated-old/active-new pair → soft then hard `DELETE_ROWS`); `SCD1_MERGE` across two runs (one update, one no-op, one insert); `OVERWRITE_TABLE` truncate-and-replace across runs; `DROP_TABLE`'s refusal and success paths; schema evolution disabled (clean failure) vs. enabled (column added mid-schema, existing data preserved) on the same starting shape; a `BUSINESS_RULES` task with a `REPORT`-typed rule flagging and correctly classifying a row.
- **Execution engine refinements — hash-key change detection, smarter `$$pipeline_id`, universal lineage, parallel BR waves, `lineage`/`migrate` CLI — done (2026-09-20).** A second, very detailed round of explicit instruction on top of the execution engine above, refining several mechanisms and closing two more open items.
  - **[ADDITION] `HASH_KEY`, per explicit instruction ("scd tables should also have hashkey created by merge_compare columns")**: `SCD1_MERGE`/`SCD2_MERGE` gain `HASH_KEY` as a new engine-managed audit column (`sql_actions.AUDIT_COLUMNS`), an `MD5` hash of `MERGE_COMPARE_COLUMNS`, NULL-safely concatenated (`_hash_expression`). Change detection (`t.HASH_KEY IS DISTINCT FROM s.HASH_KEY`) replaces the old per-column `OR`-chained `IS DISTINCT FROM` comparison entirely. **[Bug caught and fixed]**: `_add_hash_key` computes it as a follow-up `ALTER TABLE ... ADD COLUMN` + `UPDATE`, deliberately *after* the schema-shape check, not baked into the staging `CREATE TABLE AS SELECT` itself — doing it earlier made the staged shape carry a column the target-side comparison (which correctly excludes engine-managed columns) never expects, breaking the "shapes agree" check on every single SCD run. Caught immediately by re-running the existing SCD tests, not left for later.
  - **`substitute_pipeline_id` gained two more cases, per explicit instruction**: (1) if `$$pipeline_id` is absent from a task's `SOURCE_SQL` and it has *no* `WHERE` clause at all, the engine now appends one itself (`WHERE 1=1` / `WHERE pipeline_run_id = X`) rather than requiring the author to always remember the token; (2) if `$$pipeline_id` is absent but a real `WHERE` clause already exists, the SQL is left **completely untouched** — "you may need to enforce it on static tables as well, which is wrong": a query against a small reference/lookup table with its own filter and no `PIPELINE_RUN_ID` column must never get one silently `AND`-ed on. **[CHOICE]** the "has a WHERE clause" check is a plain `\bWHERE\b` regex, not a real parse (ruled out elsewhere in this module for the same Non-goals reason) — a WHERE that exists only inside a subquery, none at the outer level, would be mis-detected; flagged, not solved further. This broke essentially every existing SQL-action test using inline `VALUES (...)` fixtures with no real `PIPELINE_RUN_ID` column — fixed by adding an explicit `WHERE 1=1` to each (correctly exercising the "leave a static-shaped query alone" case, not a workaround).
  - **`DROP_TABLE`'s ordering check strengthened, per explicit instruction ("created by this pipeline using create_table *before* this drop table step")**: previously only checked that a `CREATE_TABLE` sibling exists in `CFG_`; now also verifies (`runlog.fetch_task_run_status`) that sibling has genuinely reached `SUCCESS` under *this* `pipeline_run_id` before allowing the drop — a declared-but-not-yet-run `CREATE_TABLE` task no longer satisfies the guard. `cfg.fetch_sibling_target_sql_action` renamed to `fetch_sibling_target_writer`, now returning the sibling's `task_id` alongside its `SQL_ACTION` (needed for this new check).
  - **Universal lineage declaration + traceability, per explicit instruction ("every task should have atleast 1 source_table and target_table" / "one of our goals to maintain traceability")**: every active task, regardless of `HANDLER`, now declares `SOURCE_OBJECT`/`TARGET_OBJECT` in `CFG_TASK_PARAMETERS` (pipe-separated for more than one of either) — purely declarative bookkeeping for lineage, layered on top of (not replacing) `HANDLER=SQL`'s already-functional `TARGET_OBJECT` and `HANDLER=BUSINESS_RULES`' already-functional per-rule `CFG_BUSINESS_RULES.TARGET_TABLE`. **[CHOICE]** checked by `validate` (`validate_task_lineage_declarations`, a third check alongside the graph/PK ones), not hard-enforced at runtime inside the handler modules themselves — consistent with schema.sql's own "not enforceable as a constraint, check at validate time" precedent for `CFG_TASK_PARAMETERS` conventions in general. **[Bug caught and fixed before shipping]**: the first version of `cfg.fetch_tasks_missing_source_or_target` used a bare `bool_or(...)` in its `HAVING` clause — for a task with *zero* `CFG_TASK_PARAMETERS` rows at all, the `LEFT JOIN` produces one all-`NULL` row, and Postgres's `bool_or` over an all-`NULL` input returns `NULL`, not `FALSE`; `NOT NULL` is `NULL`, which `HAVING` silently treats as "excluded" — the exact tasks the check most needs to catch (declaring nothing) vanished from the result entirely. Reproduced directly (`SELECT bool_or(NULL::boolean)` → `NULL`) before fixing with `COALESCE(bool_or(...), FALSE)`.
  - **`lineage --table <schema.table>` — a new CLI command**, closing the "table-level lineage" read-only query verb CLAUDE.md's CLI surface section already anticipated but left unnamed/unbuilt. `cfg.fetch_table_lineage` reads only the `SOURCE_OBJECT`/`TARGET_OBJECT` convention above (not `CFG_BUSINESS_RULES.TARGET_TABLE` — **[CHOICE]** one canonical place lineage is read from, not two that could disagree for a multi-rule `BUSINESS_RULES` task), filtering pipe-separated values in Python rather than attempting a fragile SQL `LIKE`-based match against one element of a delimited list.
  - **`business_rules.py` gained genuine same-wave parallelism, per explicit re-instruction ("it sequence would be like a dense rank. run in waves. every rule sharing same number for a task can run parallel")**: rules are grouped by consecutive `SEQUENCE_NUMBER` (`itertools.groupby`, already ordered by `cfg.fetch_business_rules_for_task`) into waves; a wave with more than one rule runs them concurrently via a `ThreadPoolExecutor` (I/O-bound DB round trips, not CPU work — threads, not the fork-based approach `runner.py`'s crash detection uses for an unrelated reason), each thread opening its **own** Data DB connection (`data_engine.connect()`) since SQLAlchemy `Connection`s aren't safe to share across threads. One wave fully completes — every rule, success or failure — before the next wave starts; a same-wave failure doesn't cancel its wave-mates, only stops any *later* wave from starting. `execute()`'s signature changed to take `data_engine: Engine` instead of a shared `Connection`, for the same reason.
  - **[DEVIATION, applied retroactively] Pipe (`|`), not comma, is now the one multi-value separator convention project-wide**, per explicit instruction ("any column that has a need to store more than one value must use | as separator"). `CFG_TASKS.RETURN_VALUES` (added comma-separated earlier the same round) was switched to pipe-separated for consistency with `MERGE_KEY`/`MERGE_COMPARE_COLUMNS`/`SOURCE_OBJECT`/`TARGET_OBJECT`, all pipe-separated from the start — `ck_tasks_return_values`'s regex, `scripts._parse_return_values`, and every test fixture updated together.
  - **`scripts.py`'s return-variable contract fully specified, per explicit instruction** (superseding the earlier "no detailed spec to work from" placeholder): `HANDLER=PYTHON` must report at least `INGESTION_COUNT` (a plain number) and `LATEST_OFFSET_UPDATE` (a `value|datatype` pair, e.g. `"2023-01-01 00:00:00|timestamp"`) — both declared in `CFG_TASKS.RETURN_VALUES` and both actually present in the script's one trailing JSON line, or a clean `HandlerError` (not optional the way the rest of this file's docstring once implied). Any other declared name is logged if present, silently skipped if not. **"log all the variables... as rows with variable = value semantics"**: `execution.HandlerResult` gained a `variables: dict[str, object] | None` field and a shared `format_task_log()` helper — every declared-and-present script variable becomes one `"NAME = value"` `AUD_TASK_RUN_LOG.TASK_LOG` line (in `RETURN_VALUES`' own declared order); `sql_actions.py`/`business_rules.py` leave `variables` unset and get the same `"NAME = value"` shape for free, rendered from whichever typed count fields (`SOURCE_COUNT`, `INSERT_COUNT`, ...) are non-`None` — one shared formatter, not a bespoke one per handler. The old `HandlerResult.task_log` free-text field (raw stdout dump) is gone.
  - **Coverage gate relaxed from 100% to 80%, per explicit instruction** ("use only high quality tests... try to add only high quality tests if adding new") — existing tests untouched; new tests for this round were written for genuine new behavior (including two real bugs the tests themselves caught — the `HASH_KEY`-before-schema-check ordering and the `bool_or`/`NULL` lineage gap above) rather than chased line-by-line to a percentage. Actual coverage still lands at 100% as of this round, incidentally, not by design.
  - **Migration tooling — closes CLAUDE.md open question #7, per explicit permission ("you may implement the migration mechanism as well")**: a new `SCHEMA_MIGRATIONS(VERSION, APPLIED_AT)` bookkeeping table plus `src/etl_craft/migrate.py`/`etl-craft migrate`, deliberately a small Alembic-*lite* runner, not a full framework. **[CHOICE]** `sql/schema.sql` keeps its existing role as the single authoritative *full* definition for a brand-new install; `sql/migrations/*.sql` (see that directory's own `README.md`) is only for carrying an *already-deployed* database forward incrementally from here on — nothing already baked into `schema.sql` gets a retroactive migration file, since that would misstate when each change actually happened (every one is already flagged in `schema.sql`'s own "POST-SIGNOFF CHANGES" block). `sql/migrations/` starts genuinely empty, which is consistent by construction with a fresh `SCHEMA_MIGRATIONS` table having zero rows — no bootstrapping conflict, since no migration file has ever existed for anything already in `schema.sql`. Each pending file runs inside its own transaction together with its own bookkeeping row (a failure rolls both back and halts before any later file); a file's own statements are split on `;` and executed individually, since psycopg3's extended query protocol (what SQLAlchemy's `text()` normally goes through) doesn't reliably support more than one statement per call — a known, flagged limitation (a literal `;` inside a string literal would mis-split), not silently assumed safe.
  - 18 new tests (294 total, up from 276 at the end of the execution-engine round above — `pytest --collect-only` before/after, not estimated), plus several pre-existing SQL-action tests updated in place (not counted as new) to add an explicit `WHERE 1=1` to their inline `VALUES (...)` fixtures now that `substitute_pipeline_id` auto-scopes a bare query with no `WHERE` at all. 100% coverage maintained (naturally, not enforced), stable across repeated runs.
  - Manually verified via `make db-schema-test`/`make db-reset` against real Docker Postgres after every schema change this round (`SCHEMA_MIGRATIONS` table creation, `RETURN_VALUES`' pipe-separated regex, the two new `EXPECT SUCCEED`/`EXPECT FAIL` cases) — every failure still lands exactly on the line marked to produce it.
- **Data-modeling cleanup: CFG_TASKS/CFG_PIPELINES stripped down to what's true of every row — done (2026-09-20), per explicit instruction to "think like an data modelling architect."** Two real anti-patterns fixed: dedicated columns that only ever mattered for a *subset* of rows (`CFG_TASKS.SCRIPT_NAME`/`RETURN_VALUES` — `HANDLER='PYTHON'` alone; `SCHEMA_EVOLUTION` — `HANDLER='SQL'` alone), and seven separate typed columns on `CFG_PIPELINES` that were really one cohesive, optional settings bag.
  - **[DEVIATION, post-signoff, supersedes three earlier entries in this section] `CFG_TASKS.SCRIPT_NAME`/`RETURN_VALUES`/`SCHEMA_EVOLUTION` all removed as columns**, per explicit instruction ("why special treatment for ingestion task alone... make these as something we give as parameter values"). All three now live in `CFG_TASK_PARAMETERS` as ordinary `PARAMETER_NAME`/`PARAMETER_VALUE` rows (`'SCRIPT_NAME'`, `'RETURN_VALUES'`, `'SCHEMA_EVOLUTION'`), read at execution time via `ctx.task_params.get(...)` — the same mechanism `SQL_ACTION`/`TARGET_OBJECT`/`MERGE_KEY` already used. `CFG_TASKS` now holds only what's true of every task regardless of `HANDLER`. Their old `CHECK` constraints (`ck_tasks_script_required`, `ck_tasks_return_values`) are gone with them — the same "not a schema constraint once it's a `CFG_TASK_PARAMETERS` convention, enforce at the application layer" reasoning already established for every other parameter: `scripts.py` still enforces `SCRIPT_NAME` presence and `RETURN_VALUES`' mandatory tokens at runtime, just no longer backed by a `CHECK`. `execution.TaskExecutionContext` lost the three dedicated fields entirely (not just their source) — a direct, concrete expression of "no special treatment," not merely cosmetic.
  - **[DEVIATION, post-signoff] `CFG_PIPELINES`' 7 separate Airflow-facing columns (`CATCHUP`, `TAGS`, `RETRIES`, `RETRY_DELAY_MINUTES`, `DEPENDS_ON_PAST`, `EMAIL_ON_FAILURE`, `EMAIL_RECIPIENTS`) collapsed into one `PIPELINE_PARAMETERS JSONB` column**, per explicit instruction ("these all as one pipeline_parameters column"). Same three-tier resolution as before, unchanged in `generate_yml.py` itself — `cfg.fetch_pipeline_detail` now reads the JSONB blob (psycopg3 deserializes it into a plain `dict` automatically, confirmed directly — no `json.loads` needed) and pulls the same `UPPERCASE` keys back out into `PipelineDetail`'s own unchanged, individually-typed fields, so the only thing that changed is *where* the values are stored, not how any consumer reads them. **[CHOICE]**: JSONB keys match `CFG_TASK_PARAMETERS.PARAMETER_NAME`'s own `UPPERCASE` casing convention, not a second casing style. No `CHECK` constraint on shape (the removed `RETRIES >= 0` etc.) — same "enforce at the application layer" reasoning as above.
  - Both changes are additive-only from a *behavior* standpoint (every existing test passed once its own fixtures were updated to write through the new storage location) but real schema surgery: old columns are gone, not just deprecated. `sql/schema.sql`'s own "POST-SIGNOFF CHANGES" block keeps the three superseded entries in place rather than editing them away, plus a new entry explicitly marking them superseded — an accurate history of what happened and when, per this file's own established practice.
  - No new migration file was written for this in `sql/migrations/` (still genuinely empty) — this change landed as a direct `schema.sql` edit, same as every other post-signoff change so far; the migration mechanism exists for carrying an *already-deployed* database forward, which this repo's own dev/CI Postgres isn't.
  - Test fixtures updated, not test *counts* grown: `conftest.py`'s `insert_committed_task(..., schema_evolution=, script_name=, return_values=)` convenience kwargs kept their exact shape for ~30 existing call sites, but now write `CFG_TASK_PARAMETERS` rows under the hood instead of `CFG_TASKS` columns; one `CFG_PIPELINES` direct-`UPDATE` test fixture switched from setting individual columns to a JSONB literal. 294 tests, 100% coverage maintained, stable across repeated runs — no new tests were needed since existing ones already fully exercised every affected path once their fixtures spoke the new storage location.
  - Manually verified: a real `INSERT`/round-trip of `PIPELINE_PARAMETERS` and a `CFG_TASK_PARAMETERS`-based `SCRIPT_NAME` against Docker Postgres; `make db-schema-test` (8 `EXPECT FAIL` cases remain, down from 12 — the 4 removed were exactly the `CHECK` constraints deleted from `CFG_TASKS`/`CFG_PIPELINES` above, confirmed by name, not just count).
- **`$$pipeline_id` auto-append reverted; new audit-column-presence pre-flight check added — done (2026-09-20), per explicit correction ("this was a bad idea") plus a related new request.** No schema.sql changes — both are pure `sql_actions.py` application-layer fixes.
  - **[DEVIATION, post-signoff, supersedes the "gained two more cases" entry above] `substitute_pipeline_id` reverted to a strict two-case rule**: the token is present (substitute it) or it isn't (leave `sql` **completely untouched**, `WHERE` clause or not). The auto-append-a-`WHERE`-when-none-exists case (and its known regex blind spot for subqueries) is gone entirely, per explicit instruction. Reasoning walked through with the user, not just applied silently: auto-appending made a SELECT's real behavior depend on a hidden heuristic invisible to its own author, which is a bigger overreach than plain token substitution and inconsistent with CLAUDE.md's own model that the author supplies and owns "a bare, validated, read-only SELECT." The residual risk — a task author forgets the token on a genuinely incremental source and it over-scans — is accepted as a reviewable pipeline-definition mistake ("this will only result in failure in a pipeline where there is no pipeline id, which should not be the case for what we create"), not something the engine should silently rescue by guessing. The `WHERE 1=1` suffixes added to existing test fixtures in the prior round are now unnecessary (harmless either way — a real `WHERE` with no token was always left alone) and were left in place rather than churned back out.
  - **[ADDITION] New audit-column-presence pre-flight check, per explicit instruction ("fail the task if the target is already present and does not have the scoped audit columns for that sql action")**: `_check_or_evolve_schema` now verifies every one of an action's own engine-managed columns (`PIPELINE_RUN_ID` plus whatever `AUDIT_COLUMNS[action]` lists) is actually present on an already-existing target *before* comparing business-column shape at all — a stale or hand-built target missing e.g. `UPDATE_DATE`/`HASH_KEY`/`ACTIVE_FLAG` now fails with one clear `HandlerError` naming exactly what's missing, instead of blowing up later mid-statement with a confusing raw "column ... does not exist." Runs unconditionally, regardless of `SCHEMA_EVOLUTION` — that flag only ever governs new *business* columns the staged SELECT introduces, never repairing a target's own missing engine-managed columns.
  - **[ADDITION, scoped extension beyond the literal ask, stated to the user and not corrected]** `_delete_rows`' soft-delete path (`HARD_DELETE` not `"true"`) gets the equivalent check for `DELETE_FLAG` specifically — the same failure class (a target missing the one column that action's write path depends on), reached differently since `DELETE_ROWS` never goes through `_check_or_evolve_schema` at all (it only matches on `MERGE_KEY`, no shape comparison). **[CHOICE]** Presence-only, not column-*type* checking (e.g. not verifying `ACTIVE_FLAG` is really `VARCHAR(1)` and not, say, `BOOLEAN`) — deliberately out of scope per explicit instruction to address "highly possible data engineering possibilities," not "the infinite possibilities": a predates-this-convention/hand-edited target missing a whole column is a real, seen failure mode; a column present but subtly mistyped is a rarer one not worth chasing here.
  - **Confirmed during review**: the `DELETE_FLAG` check is scoped to the soft-delete branch only (`sql_actions.py`'s `_delete_rows`, inside `else: # not hard_delete`) — a `HARD_DELETE=true` task issues a real `DELETE FROM` and never touches `DELETE_FLAG` at all, so it correctly is never rejected for a target that lacks that column. Locked in with a dedicated regression test (`test_sql_delete_rows_hard_delete_ignores_missing_delete_flag`) rather than left as an implicit property of the `if hard_delete: / else:` branching, since it's exactly the kind of scoping a later refactor could accidentally widen.
  - 4 new integration tests against real Docker Postgres (`test_sql_overwrite_table_missing_audit_column_fails_clearly`, `test_sql_scd1_merge_missing_audit_column_fails_even_with_schema_evolution` — proving the check fires even with `SCHEMA_EVOLUTION=true` — `test_sql_delete_rows_soft_delete_missing_delete_flag_fails_clearly`, `test_sql_delete_rows_hard_delete_ignores_missing_delete_flag`), each hand-building a target table missing its required audit column(s) rather than going through `SETUP_TABLE` (which would never omit them). One existing unit test (`test_substitute_pipeline_id_appends_where_when_none_exists`) asserted the now-removed auto-append behavior and was rewritten to assert the new unconditional "leave alone" rule instead (renamed `..._leaves_a_tokenless_query_with_no_where_untouched`) — not just deleted, since the no-token/no-WHERE case is still worth a named regression test for the opposite behavior. 298 tests total (up from 294), 100% coverage maintained, `make check` and `make db-schema-test` both clean.
- **The remaining CLAUDE.md scope closed out in one large round — `steps`/`history` query verbs, `EMAIL_ALERT`+SMTP with an HTML status digest, interactive `configure`, `Cloning`'s copy mechanism, and `generate-docs` — done (2026-09-20), per explicit request to build everything still open.** No `sql/schema.sql` changes anywhere in this round — every piece is pure application code against the already-signed-off schema. 368 tests total (up from 298), 100% coverage maintained (save 4 lines in `cloning.py`'s own type-dispatch function, see its own entry below), `make check`/`make db-schema-test` both clean.
  - **`steps`/`history` — closing CLAUDE.md's last two "conceptually agreed but not yet named or built" read-only query verbs.** `cfg.fetch_pipeline_steps` (a pipeline's active tasks, each with its own `CFG_TASK_PARAMETERS` dict — deliberately generic, no per-`HANDLER` formatting in the read layer, same spirit as `fetch_table_lineage`) backs `etl-craft steps --pipeline_code X`. `cfg.fetch_pipeline_run_history`/`fetch_task_run_history` (newest-first, `--limit`, default 20) back `etl-craft history --pipeline_code X [--task_code Y] [--limit N]` — pipeline-level or task-level depending on whether `--task_code` is given. 10 new tests (3 `cfg.py`-direct, 7 CLI-level including both empty-result placeholders and both unknown-code error paths).
  - **`EMAIL_ALERT` + SMTP, closing CLAUDE.md's last open Handlers question — `src/etl_craft/email_alert.py`.** Transport resolved to SMTP per explicit instruction; gating stays entirely generic (a normal `CFG_TASK_DEPENDENCY`/`CFG_PIPELINE_DEPENDENCY` edge, same as any other task) — this module's only job is composing and sending the email once dispatch already decided the task runs.
    - **[ADDITION] New `craft-connector.yml` section, `[Email]`** (`config.EmailConfig`/`EmailProfile`) — shaped like `[Postgres]`/`[Warehouse]`'s own `Active_profile`/`Profiles` pattern (a team's dev/uat/prod relays can genuinely differ), not a single flat block. **[CHOICE]** its own smaller `auth_mode` vocabulary (`none`/`password` only — `VALID_EMAIL_AUTH_MODES`) since an OAuth2 SMTP flow is exactly as provider-specific as `db.py`'s existing `token`/`sso` stubs and no team has named one to build against; left out entirely rather than stubbed with a third `NotImplementedError` nothing currently asks for.
    - **CFG_TASK_PARAMETERS vocabulary**: `EMAIL_TO` (pipe-separated, required), `EMAIL_SUBJECT`/`EMAIL_BODY` (substitution tokens allowed; `EMAIL_BODY` required unless `EMAIL_PIPELINES` is set), `EMAIL_PIPELINES` (below). Substitution tokens — `$$pipeline_id`/`$$pipeline_code`/`$$task_code`/`$$error_message` — are plain-text, pre-driver replacements, the same spirit as `sql_actions.substitute_pipeline_id`, not a templating engine; `$$error_message` is derived from the task's own active `FAILURE`-typed `CFG_TASK_DEPENDENCY` edges (`cfg.fetch_failure_watch_messages`, latest logged `ERROR_MESSAGE` per watched task, same-or-cross-pipeline) rather than a second, parallel `WATCH_TASK_CODE` parameter that could drift out of sync with the real gating edge.
    - **[ADDITION, mid-round explicit follow-up request] Every email is HTML with an inline `<style>` block, and supports a status digest**: `EMAIL_PIPELINES` — "if it is all, send the status of all pipelines for its latest [run]. if the pipeline name is mentioned, then that alone. can contain 1, some, all. some = pipeline1|pipeline2" — parsed the same pipe-separated way as every other multi-value `CFG_TASK_PARAMETERS` value (`ALL` | one code | `CODE_A|CODE_B`). When set, the email gets an appended summary table (pipeline/status/run id/start/end) plus one collapsible `<details>`/`<summary>` section per pipeline (per explicit instruction — "each pipeline should have collapsible arrow") showing that pipeline's own latest run's per-task breakdown (`cfg.fetch_task_statuses_for_run`, `PENDING` for a task with no row yet under that run). **[CHOICE]** SUCCESS (green)/FAILED (red) are the two colors that matter most, per explicit instruction ("success, falire types"); `SKIPPED`/`IN-PROGRESS`/this module's own synthetic `PENDING`/`NEVER_RUN`/`UNKNOWN` get neutral grey. **[CHOICE]** "if pipeline should support any other type that too" is read as: never hard-fail the whole alert over one bad name in the list — an unresolvable pipeline code renders as its own `UNKNOWN` row instead of raising. **[CHOICE]** plain `<details>`/`<summary>`, no JavaScript — most clients render a real disclosure triangle; the rest just show it always-expanded, a safe fallback not a broken one. **[CHOICE]** "any other template, team should use airflow," per explicit instruction — this module's reporting shape stops at one intro paragraph plus this one digest shape; richer/bespoke reporting is out of scope by design, not an oversight.
    - 24 new tests (6 `config.py` `[Email]`-parsing, 2 pure substitution, 4 pure digest-rendering/status-color, 12 integration against real Postgres with `smtplib.SMTP` faked). **[Bug avoided, not caught after the fact]**: the fake SMTP client had to write its captured calls to a file, not an in-memory list — `runner.py`'s crash-detection fork means the actual `smtplib.SMTP(...)` call happens in a *forked child process*, and mutations to Python objects there are invisible back in the parent test process (fork's copy-on-write semantics, the same reason the project's own crash-detection test already reaches for a DB-visible effect instead of an in-memory one — see that test's own note in the "Crash detection" entry above).
  - **Interactive `configure` (no `--env`), closing the last "not implemented yet" CLI row — `configure.configure_interactive`.** Mirrors `configure_from_env`'s own section shape and merge semantics (`Execution`/`Source`/`Cloning` wholesale-replaced; the one `Postgres` profile entered merges in alongside any others already on disk), plus two `configure_from_env` never covered at all: optional `[Warehouse]` and `[Email]` blocks, each gated behind its own "configure this now? y/n" prompt so declining leaves any existing section for it untouched. **[CHOICE]** the 7 `[Orchestrator]` Airflow-facing global defaults are deliberately not prompted for — real edge-case tuning is better done by hand-editing the YAML than by walking through 7 more prompts most setups would just accept the fallback default for anyway. `input_fn`/`print_fn` are injectable (default: real `input`/`print`), the same "injectable I/O" testability convention `crosspipe.py`'s `sleep`/`now` already established — every prompt/re-prompt/merge path is tested with a canned answer list, no real terminal involved. 9 new tests (7 `configure_interactive`-direct, 2 CLI-wiring — mocking `configure_interactive` itself, the same "prove the CLI wires this in, the function's own behavior is proven elsewhere" pattern the `migrate` CLI tests already use).
  - **`Cloning`'s actual copy mechanism, closing the last piece of config plumbing that never did anything — `src/etl_craft/cloning.py`.** `tables_for_scope` is this module's own authoritative `cfg`/`aud`/`all` table list (CLAUDE.md names the scopes but never enumerated the tables). **[CHOICE]** "merge-style" is implemented as truncate-then-reinsert per table, not a genuine row-level upsert/delete-tracking merge — a real cross-dialect `MERGE` has no single portable ANSI form, and CFG_/AUD_ tables are metadata, not fact-table-scale data, so a current, correct mirror is what the stated goal ("so a team can query engine config/audit from inside their own warehouse") actually needs, not incremental change tracking; flagged as a real, deliberate scope limit. **[ADDITION]** the mirrored target table's own columns are generic (`BigInteger`/`Numeric`/`Boolean`/`DateTime`/`Text`), not a byte-for-byte copy of Postgres's own types — `PIPELINE_PARAMETERS` (`JSONB`) and any array column are JSON-serialized into `Text` at insert time instead, since neither has a portable equivalent across "any SQLAlchemy-supported relational engine."
    - **[ADDITION, a real safety guard, not a contrived one]** `run_cloning_if_enabled` refuses outright if `[Warehouse]` resolves to the same `(dialect, host, port, database)` as the Engine DB — since every mirrored table keeps its Engine DB name, cloning onto itself would mean truncating and reinserting the real `CFG_`/`AUD_` tables in place from their own reflection. **[Bug caught and fixed before shipping, via a real integration test built specifically to exercise this guard, not a review]**: the first version compared the *built* `Engine` objects' own `.url` attributes — always blank on both sides, since `db.build_engine`/`warehouse.build_data_engine` both deliberately use a blank-URL-plus-`creator` pattern (never rendering a secret into a logged engine URL) — so the guard silently never fired in practice and a genuinely same-database clone hit Postgres's own real `FeatureNotSupported: cannot truncate a table referenced in a foreign key constraint` error instead of being refused up front. Fixed by comparing the two profiles' own `jdbc_url` strings (config-level data, independent of how either `Engine` gets built) instead.
    - **[CHOICE, two real ClickHouse-specific gaps, verified directly against a real ClickHouse target, not assumed]**, the same "prove it for real" bar `warehouse.py`'s own ClickHouse test already set: (1) ClickHouse refuses any `CREATE TABLE` with no explicit table `ENGINE` clause — no other dialect touched so far needs this, and `clickhouse-sqlalchemy` only accepts it via its own `Engine` construct, which this module deliberately never imports (per Non-goals, no third-party dialect is a hard/direct dependency) — `_create_clickhouse_table` is a hand-built, literal `CREATE TABLE ... ENGINE = MergeTree() ORDER BY tuple()`, reached only via the dialect *name string* `data_engine.dialect.name == "clickhouse"`, never an import. (2) `clickhouse-sqlalchemy`'s own table-engine reflection resolves the target database from `connection.engine.url.database`, not the live DBAPI connection's real one — always blank for the same blank-URL-plus-`creator` reason above — so ClickHouse alone needs the real database name passed explicitly as `schema=` (resolved once via `warehouse.translate_jdbc_url`, the same parse `warehouse.py` itself already does). (3, found the same way) ClickHouse's own `DELETE` compiler refuses an unconditional `DELETE` outright ("WHERE clause is required") — switched the clear-a-table-for-a-full-rewrite step to `TRUNCATE TABLE`, this project's own established idiom for exactly that (`sql_actions.py`'s `OVERWRITE_TABLE`), which every dialect touched so far — Postgres included — supports.
    - Wired into `orchestrator.py`'s one shared finalize point (`_finalize_from_task_states`, called from both `run_pipeline()` and `finalize_active_run()` — the latter is Mode=orchestrator's *only* pipeline-level finalize path, so this is what makes Cloning actually reachable under both modes from one call site) via `_run_cloning_best_effort`, which deliberately catches every exception and prints a warning rather than letting a Data DB hiccup turn an otherwise-successful pipeline run into a reported failure — per CLAUDE.md's own framing of Cloning as "special-cased engine-internal machinery," secondary to the pipeline's own correctness. **[CHOICE]** deliberately *not* wired into the empty-pipeline early-return or either mode's SKIPPED-via-unmet-cross-pipeline-dependency short-circuit — one shared hook, not four, and neither path changes anything in the mirrored tables worth a clone.
    - 24 new tests: 3 pure (`tables_for_scope`, 3-way `_same_database`), 8 against real Postgres-as-Engine-DB + real ClickHouse-as-Data-DB (the actual copy mechanism — deliberately *not* tested with Postgres standing in for both roles, unlike every other dual-role test elsewhere in this project, since doing so for cloning specifically would mean truncating the real `CFG_`/`AUD_` tables — exactly what the same-database guard exists to refuse), 1 against a second, genuinely separate Postgres database on the same server (proving the *generic*, non-ClickHouse create-target-table path for real, not just in theory), 2 proving the `orchestrator.py` wiring (invoked-with-the-right-args, and a cloning failure not failing the pipeline).
  - **`generate-docs`, closing CLAUDE.md's planned documentation generator — `src/etl_craft/docs_generator.py`.** Reuses exactly the existing read-layer functions (`cfg.py`, `resolver.build_graph`) `list`/`graph`/`steps`/`lineage` already use — no new query logic, just assembling their results into pages, per CLAUDE.md's own "the same read-layer queries as above." Emits `index.html` (search box + every active pipeline) plus one `<PIPELINE_CODE>.html` per pipeline (task waves, steps, pipeline/cross-pipeline-task dependencies) plus `search-index.json` plus `style.css`/`search.js`, all to a local `--output` directory (default `./etl-craft-docs`) a team then serves or deploys however it likes. **[CHOICE]** the client-side search is a small, dependency-free vanilla-JS substring search over the generated JSON index, not a vendored copy of Fuse.js/Lunr.js — CLAUDE.md's own "Fuse.js/Lunr.js-class" phrasing reads as "a client-side index of this general shape," not a literal library requirement, and staying dependency-free keeps the site working even served from a network with no CDN access at all; flagged as a real trade-off (substring matching only, no fuzzy/ranked search) appropriate for a metadata site with at most a few hundred pipelines/tasks. 6 new tests (5 pure rendering/search-index in `test_unit.py`, 2 against real Postgres including the CLI wiring).

**Tooling (added 2026-09-19).** The package is uv-managed (`pyproject.toml` + `uv.lock`, src-layout under `src/etl_craft/`, Python ≥3.11). Dev tooling: `pytest`, `black` (formatter, line-length 100), `ruff` (lint-only — `E,F,I,UP,B,SIM`; deliberately excludes `D` so it never duplicates/conflicts with pydocstyle), and `pydocstyle` (`select=D` in `[tool.pydocstyle]`). One deviation worth knowing about: a bare `select=D` selects two internally contradictory rule pairs — `D203`/`D211` (blank line before a class docstring: required vs. forbidden) and `D212`/`D213` (multi-line summary on the first line vs. the second) — so *any* style choice leaves one side of each pair permanently violated. `add-ignore = "D203,D213"` resolves this the same way pydocstyle's own `pep257` convention does; the alternative (writing every docstring as a one-line summary with extended rationale kept as plain `#` comments underneath, as `resolver.py` now does) was adopted project-wide to sidestep the conflict rather than fight it file by file. `.github/workflows/ci.yml` runs `black --check`, `ruff check`, `pydocstyle`, and `pytest` via `uv sync --locked --all-groups` on every PR into `main`.

**Coverage gate: 80% (relaxed from 100% on 2026-09-20, per explicit instruction), added 2026-09-19.** `pytest-cov` + `[tool.coverage.run]`/`[tool.coverage.report]` in `pyproject.toml` (`fail_under = 80`, was `100`); CI's final step and `make check`/`make coverage` both run `pytest --cov=etl_craft --cov-report=term-missing`. **[DEVIATION]** the instruction accompanying the relaxation was explicit about *why*: "use only high quality tests... try to add only high quality tests if adding new" — the gate exists to catch real regressions, not to force line-by-line coverage-chasing on every future change; existing tests were left untouched, and actual coverage has stayed at 100% since incidentally (each new feature's own genuinely-new branches got real tests, not padding). Coverage is only meaningful against the **full** suite (unit + integration) — most of the source is exercised through the Postgres-backed tests, so measuring `test_unit.py` alone reports large, misleading gaps. The original push to 100% required real investigation, not just writing tests until a number hit it:
- `[tool.coverage.run] concurrency = ["thread"]` is required, or coverage.py silently doesn't trace background threads at all — without it, `test_integration.py`'s concurrency tests report 0% on the very race-handling code they exist to exercise, despite genuinely running it.
- Even with that fixed, the original 8-thread best-effort race test (`test_concurrent_find_or_create_active_run_converges_on_one_run`) turned out not to *reliably* force the `except IntegrityError` branch — depending on connection-pool/network timing, a "losing" thread's own SELECT can run after the winner already committed, so it takes the early-return path instead of ever attempting the conflicting INSERT. Fixed by adding two **deterministic** 2-thread race tests (`..._second_transaction_hits_integrity_error_path`, one per `find_or_create_*` function) that explicitly control the interleaving via a `threading.Event` and a timed hold, rather than relying on 8-way scheduling luck. The original best-effort test stays too, as a broader stress proof.
- A few branches are genuinely unreachable through any real path and are either exercised by directly calling the private function with hand-built invalid input (`resolver._edge_satisfied`, `db.build_engine` with a bad `auth_mode`, `config._load_dotenv_file(None)`, `handlers.dispatch("BOGUS")`) or, for `runlog.py`'s "unique_violation fired but the re-SELECT still finds nothing" defensive `RunLogError` (which *cannot* happen against a real Postgres — a unique_violation means a conflicting row exists by definition), simulated with a minimal fake `Connection` in `test_unit.py`. `runner.py`'s `SUCCESS`-finalizing path is unreachable through any real handler today (every `HANDLER` is still a stub that always raises) — exercised by monkeypatching `etl_craft.runner.dispatch` to return a real `HandlerResult` directly.
- `cli.py`'s final dispatch fallback (`return 2` after exhausting `run`/`list`/`graph`) is structurally unreachable — argparse's `required=True` on the subparsers already guarantees `args.command` is one of the three — and is the one line excluded via `# pragma: no cover`, with a comment explaining why.
- `runpy.run_module("etl_craft", run_name="__main__")` executes `__main__.py`'s `if __name__ == "__main__":` guard in-process (not a subprocess), which is what let it be measured at all — the orchestrator's own subprocess-spawning tests exercise the same line for real but in a child process, invisible to the parent's coverage collection.

109 tests total (up from 87).

**Local Postgres (Docker), added and verified end-to-end 2026-09-19.** `docker-compose.yml` runs Postgres 16 on host port `55432` (not `5432`, to avoid colliding with a locally-installed Postgres) with `sql/schema.sql` auto-applied via `docker-entrypoint-initdb.d` on first boot. `make db-up` / `make db-down` / `make db-reset` (tears down the volume too, since `schema.sql` is plain `CREATE TABLE` — not idempotent — by design, meant to run once against an empty DB) / `make db-schema-test` (runs `sql/schema_test.sql` inside the container) / `make test` / `make check` (mirrors CI). The Postgres-backed integration suite (`tests/test_integration.py`, via `tests/conftest.py`) skips itself with a clear message if nothing is reachable at `ETL_CRAFT_TEST_DATABASE_URL` (defaults to the compose file's own connection string), so `pytest -q` alone never requires Docker. CI runs the same integration suite for real, via GitHub Actions' native `services:` (no Compose needed there) rather than skipping.

**Real CI bug, found 2026-09-20 (first genuine GitHub Actions run) and fixed.** `.github/workflows/ci.yml`'s `psql -f sql/schema.sql` step failed with `connection to server on socket "/var/run/postgresql/.s.PGSQL.5432" failed` — it was passing `ETL_CRAFT_TEST_DATABASE_URL` (SQLAlchemy's `postgresql+psycopg://` dialect-qualified scheme) straight to `psql`, which speaks plain libpq and doesn't recognize `+psycopg`; rather than erroring on the bad scheme, libpq silently fell back to a local Unix socket that doesn't exist in that job. This had never been exercised: local testing always used `docker compose exec ... psql -U ... -d ...` (flag form, no URI at all), and there's no way to run GitHub Actions itself locally. Fixed by giving the `psql` steps libpq's own standard `PGHOST`/`PGPORT`/`PGUSER`/`PGPASSWORD`/`PGDATABASE` env vars instead, so they need no connection string; `ETL_CRAFT_TEST_DATABASE_URL` stays SQLAlchemy-flavored for the Python suite alone. Reproduced the exact failure and confirmed the fix with a throwaway `docker run --network host postgres:16 psql ...` against the local Docker Postgres before pushing, since that's the closest thing to CI's own environment available here.

Ran `make db-schema-test` and `make check` for real once Docker was available: `sql/schema_test.sql` passed line-for-line as documented against actual Postgres 16, and the integration suite immediately caught a real bug — `find_or_create_task_run` read a Row's `TASK_RUN_ID`/`STATUS` attributes by their as-written uppercase names, which works against the SQLite stand-in (preserves declared case) but not real Postgres (folds unquoted identifiers to lowercase), raising `NoSuchColumnError`. Fixed by adding explicit lowercase `AS` aliases to those `SELECT`s. **Lesson for any future raw-SQL code here**: always alias `SELECT` columns explicitly in lowercase (or another fixed casing) rather than relying on a Row's attribute-name matching an unquoted identifier's original casing — SQLite and Postgres disagree on what that casing even is.

## Core design principles

These aren't stylistic preferences — violating them undoes decisions made deliberately over a long design conversation:

- **Config vs. code, drawn by blast radius, not convenience.** A step that only reads/writes within the platform (a `CFG_` row) is config — low-friction, eventually CLI-driven. A new external connection, a new action type, or a destructive schema change is code — reviewed, deliberately harder to change. Creating a new pipeline is *always* manual (git-managed inserts/migrations), never a CLI verb.
- **Closed vocabulary of SQL actions.** `CREATE_TABLE`, `SETUP_TABLE`, `OVERWRITE_TABLE`, `SCD1_MERGE`, `SCD2_MERGE`, `DROP_TABLE`, `DELETE_ROWS` — implemented in `sql_actions.py` (see "Where things stand"), the authoritative reference for the exact vocabulary and mechanics now that it's built. Every SQL-handler task supplies a bare, validated, read-only `SELECT`; the engine wraps it in whatever statement the declared action calls for, and appends whichever engine-managed audit columns that action calls for — never present in the author's own SELECT. The engine owns every write — a step cannot touch the warehouse outside its declared action. Cover new needs by extending an existing action's metadata, not by adding a new action type casually.
- **Order comes from data.** `CFG_TASK_DEPENDENCY` / `CFG_PIPELINE_DEPENDENCY` rows are the DAG. Nothing enumerates order in code; the resolver just walks dependency rows. Same-pipeline order compiles into Airflow's native task chaining when generating YAML; cross-pipeline order has no structural equivalent in any orchestrator and is always resolved by self-check/polling at runtime.
- **Idempotent by construction, retry resumes.** Re-running a step must produce the same result whether it runs once or five times. A retry reads the log, skips what's already `SUCCESS` or `SKIPPED`, and only re-attempts what actually failed or never ran.
- **One run id, resolved, never passed.** `pipeline_run_id` is never handed between tasks — no XCom, no injected env var. Every task independently resolves the currently-active run by querying `AUD_PIPELINES_RUN_LOG`. See "Run-id resolution" below; this is probably the single most load-bearing mechanism in the whole design.
- **Full-refresh/derived state by default; incremental only where provably safe.** A table either accumulates every run (history) or holds exactly one run's truth (current state) — never a mix.

## Non-goals — things explicitly considered and rejected

Do not reintroduce these without a real reason; each was deliberately ruled out during design:

- REST API calls to any orchestrator, including Airflow, at execution time. The only hard runtime dependency, checked by every action, is a valid Engine DB (Postgres) connection.
- XCom, or any other orchestrator-specific mechanism, for passing `pipeline_run_id` between tasks.
- Importing `dag-factory` or any other orchestrator-specific library. `generate-yml` is entirely hand-rolled — the YAML shape takes visual inspiration from how Airflow/dag-factory-style YAML looks, for familiarity, but is not required to match any external tool's schema, and no such library is a dependency.
- Support for more than one Data DB (warehouse) connection per deployment. One warehouse, full stop — the explicit reasoning given for this was "else we will become Informatica."
- Any runtime detection scheme for whether execution mode is being spoofed (credential-probing, install-time locks were both considered and dropped). Final stance: trust the developer. `--mode` is never a runtime flag typed by a human for orchestrator runs — it's baked in once, at setup time, via `set-execution-mode`, and persists in `craft-connector.yml` until the environment is rebuilt.
- Sourcing credentials from an orchestrator's own connection store (e.g. reading Airflow Connections directly). All connections resolve through `craft-connector.yml` only, to stay orchestrator-agnostic — this includes the Data DB connection, "even on the Airflow side."
- Bundling third-party SQLAlchemy dialects (Snowflake, Databricks, BigQuery, Redshift, ClickHouse) as hard dependencies. They're optional extras a team installs itself; SQLAlchemy discovers an installed dialect automatically via setuptools entry points, so engine code never imports one directly.
- A heartbeat column plus an external "reaper" process for detecting dead tasks. Superseded by parent/child process monitoring — see "Crash detection" below.
- A staging/cross-connection copy path for ordinary tasks. Moot by construction once there's only one Data DB — `source_object`/`target_object` are always in the same place.

## Architecture at a glance

**Two databases, two very different rule sets:**
- **Engine DB** — always Postgres, no exceptions. Holds every `CFG_`/`AUD_` table. Required specifically for Postgres's constraint guarantees — most importantly, a partial unique index is what makes run-id creation race-safe (see below). Every engine action checks for a valid Engine DB connection first, before anything else.
- **Data DB / warehouse** — exactly one per deployment, any SQLAlchemy-supported relational engine. Because there's only one, `TARGET_OBJECT`/`SOURCE_SQL` in `CFG_TASK_PARAMETERS` always resolve against that same database — there is no cross-connection staging path to build for ordinary tasks. `TARGET_OBJECT` is stored as bare `schema.table`, deliberately environment-agnostic — the database/catalog name is always supplied at runtime from the active `[Warehouse]` profile (`sql_actions.qualify()`), so the same CFG_ row means a different real object in dev/uat/prod.

**Connections & auth** — all resolved through `craft-connector.yml` (below), never hardcoded, never sourced from an orchestrator. JDBC URL is the preferred storage format for a connection string wherever one is persisted; a small internal translator converts it to the right SQLAlchemy dialect URL at connection time. Auth is a small registry of functions keyed by each profile's `auth_mode` (`password`, `token`, `sso`, `key_file`), handed to `create_engine(creator=...)` — the `sso`/`token` modes mint or refresh short-lived credentials per connection. For those two specifically, `pool_recycle` should sit comfortably under the credential's real lifetime so a checked-out connection always has meaningful life left — that's a safety margin, not a guarantee; a connection dying mid-operation is just another failure the idempotent-retry design already absorbs. `pool_pre_ping` is a worthwhile optimization on top but not load-bearing.

**Execution — one primitive.** Everything runs through `etl-craft run --pipeline_code X [--task_code Y] [--force]`:
- `--task_code` given → runs exactly one task. This is the literal form Airflow's generated `BashOperator` tasks shell out to.
- `--task_code` omitted → runs the whole pipeline. The engine becomes its own tiny scheduler: resolve the ready wave from `CFG_TASK_DEPENDENCY` (the same resolver `graph` uses to walk the same data, just consumed differently), spawn one `run --task_code` subprocess per ready task, wait on the wave, repeat. Local runs are built to mimic exactly what an orchestrator-driven run does — sequential/parallel waves, subprocess-per-task, crash monitoring — unless `--force` is passed.
- `--force` bypasses all dependency/state checks entirely, for a pipeline or a single task. Only legal under `Mode = local`; refused outright under `Mode = orchestrator`.
- There is deliberately no separate `execute --local`/`execute --airflow` verb, and no REST-based trigger/status command — `run` is the only execution primitive, in both modes.

**Execution mode (`local` vs. `orchestrator`)** is set once per environment via `etl-craft set-execution-mode`, persisted in `craft-connector.yml`. It is **not** a per-invocation CLI flag — nothing in a generated DAG or a manual command line passes `--mode`. Changing it means re-running setup, which in a real deployment goes through the same versioned pipeline as any other environment change. This is intentionally a trust boundary, not a technical one: see the Non-goals entry above.

**Crash detection.** `run` forks the actual task logic as a child process and watches it. A child that exits normally — success, or a handled failure — writes its own final log row, as always. A child that dies unannounced (OOM-kill, segfault) gets `FAILED` written on its behalf by the still-alive parent. This is identical in local and Airflow modes, since Airflow-side execution is this same subprocess form — nothing orchestrator-specific to build. Known, accepted gap: if the *entire* process tree dies at once (container OOM-killed, pod evicted, node gone), nothing survives to write `FAILED`. Airflow's own zombie-task detection covers its UI in that scenario but never touches `AUD_TASK_RUN_LOG`, which is left permanently stale. Accepted as rare enough not to solve for now.

## Run-id resolution (read this before touching anything related to run state)

`pipeline_run_id` is never passed to a task — every task resolves it itself by querying `AUD_PIPELINES_RUN_LOG`:

1. The first task of a run does a **find-or-create**: reuse an existing `IN-PROGRESS` row for this pipeline if one exists; otherwise mint a new one.
2. This must be atomic at the database level — a trigger or application-level check alone cannot close the race between two near-simultaneous kickoffs of the same pipeline. The actual guarantee comes from a **partial unique index**: `UNIQUE (PIPELINE_ID) WHERE STATUS = 'IN-PROGRESS'`. The second concurrent insert fails outright rather than racing. This is implemented in `sql/schema.sql` as `ux_pipeline_run_one_active`.
3. Every later task queries for whatever run is currently active for its pipeline and binds itself to it (one `AUD_TASK_RUN_LOG` row per task per pipeline run, found-or-created and updated in place — never a new row per retry attempt).
4. If a task already has a binding under that run and it already shows `SUCCESS`, it returns success immediately without re-running. This is what makes retries — whether triggered locally or by an orchestrator — always *continue* a failed/in-progress run rather than starting fresh, and is why a run should never end in an intermediate state.
5. A single-task invocation with nothing currently active is a dev/ad-hoc convenience path: it binds to the latest logged run and updates its dates. Not an everyday scenario — don't over-engineer around it.

## Incremental / full-refresh mechanics

- `CFG_PIPELINES.REFRESH_TYPE` (`FULL`/`INCREMENTAL`) is a **pipeline-level** value — every task in one pipeline run shares one mode. Mixing an always-full dimension load with an incremental fact load means two pipelines, not one. **Note:** both pasted schema drafts still show `REFRESH_TYPE` on `CFG_TASKS` — that was superseded by a later, explicit decision to move it to the pipeline. `sql/schema.sql` implements the pipeline-level version; `CFG_TASKS` carries a comment noting the column is intentionally absent there.
- Every SQL step's `SELECT` may contain the literal token `$$pipeline_id` in its `WHERE` clause. The engine runs its own plain-text substitution pass *before* the query reaches the driver's own bind-variable handling, so `$$` tokens and real bind params (`:param`, `%s`) never collide.
- At substitution time: `$$pipeline_id` becomes `pipeline_run_id = <this run's id>` for incremental, or `1=1` for full refresh.
- **Refined 2026-09-20, per explicit instruction, to two further cases** (`sql_actions.substitute_pipeline_id`): if the token is absent and the `SELECT` has **no `WHERE` clause at all**, the engine appends one itself, so an author doesn't have to remember the token on every ordinary query; if the token is absent but a **real `WHERE` clause already exists**, the query is left completely untouched — "you may need to enforce it on static tables as well, which is wrong," since a small reference/lookup table with its own filter and no `PIPELINE_RUN_ID` column must never get one silently `AND`-ed on. The "has a `WHERE`" check is a plain regex, not a real parse — a `WHERE` that exists only inside a subquery would be mis-detected as present at the top level; a known, flagged heuristic limit, not solved further.
- **[SUPERSEDED, same day, 2026-09-20]** The auto-append bullet directly above was reverted per explicit correction ("this was a bad idea"). `substitute_pipeline_id` is back to a strict two-case rule: token present → substitute; token absent → leave `sql` **completely untouched**, `WHERE` clause or not — no heuristic `WHERE`-detection, no auto-append. A missing token on a genuinely incremental source is now treated as a reviewable pipeline-definition mistake, not something the engine tries to silently rescue. See "Where things stand"'s own dated entry for the full reasoning and the new audit-column-presence check added alongside it.
- Every table written by a SQL action carries a `pipeline_run_id` column that the engine auto-stamps. A step author writes `select col1, col2 from table where $$pipeline_id` — never manually selects or inserts the id.
- **`HANDLER = PYTHON` ingestion scripts are the one exception.** The engine does not auto-inject the run id into them; the team's own script is responsible for fetching and including `pipeline_run_id` in whatever it inserts.
- Runtime substitution, rather than trusting a stored value, matters specifically for full refresh: it's what lets a downstream table that maintains history, with no new data after change-detection, still correctly carry the *current* run's id instead of getting stuck showing some run from long ago.
- `HAS_DATA` (`TARGET_COUNT > 0`) is only a meaningful signal against an **incremental** upstream — there it genuinely reflects whether anything changed, and downstream tasks can legitimately skip on it. Against a full-refresh upstream, every run re-asserts the complete current state regardless of whether values actually moved, so `HAS_DATA` isn't a real change signal there — full refresh just proceeds and re-stamps.

## Dependency resolution & polling

- **Same-pipeline task deps**, when going through a generated DAG, compile into Airflow's own native task chaining (`>>`), built from `CFG_TASK_DEPENDENCY` — series, parallel, conditional shapes, as the data dictates.
- **Any path that bypasses the DAG's own ordering** — a manual single-task run, a backfill, a re-triggered task — has no structural ordering to lean on, so the engine still has to verify same-pipeline dependencies itself: a live query against `AUD_TASK_RUN_LOG` scoped by the shared `pipeline_run_id` (no staleness comparison needed, since both tasks already share one run).
- **Cross-pipeline dependencies** (`CFG_PIPELINE_DEPENDENCY`, and `CFG_TASK_DEPENDENCY` rows where `DEPENDS_ON_PIPELINE_ID` points elsewhere) have no DAG-native equivalent in Airflow or otherwise — one DAG can't natively depend on a task in a separate DAG. These are always resolved by a self-check/poll step inserted before step 1, alongside run-id minting.
- The naive version of this check — "look at the dependency's last logged run" — breaks the moment two pipelines run on different schedules (weekly vs. daily, say): most days the dependency isn't in-progress, so the check falls straight to a stale "last logged run" from days ago and wrongly proceeds. The fix: two watermark tables, `AUD_PIPELINE_DEPENDENCY_TRACKER` and `AUD_TASK_DEPENDENCY_TRACKER` (in `sql/schema.sql`, not in either pasted schema draft — designed later), recording the run last *consumed* for each dependency edge. A candidate run only satisfies the edge if it's newer than what was already consumed for that specific edge *and* matches that edge's own `DEPENDENCY_TYPE` — a `SUCCESS` edge tracks the last `SUCCESS` run, `FAILURE` tracks the last `FAILED` run, `HAS_DATA` tracks the last run with `TARGET_COUNT > 0`. Until a new qualifying run appears, the edge keeps resolving to "not yet satisfied," with no polling wasted on a pipeline that was never going to produce one today.
- The tracker only updates *after* the gated task/pipeline completes. Cross-pipeline task-level tracking only applies to actual cross-pipeline edges — same-pipeline task deps never touch it.
- Poll only while the dependency is genuinely `IN-PROGRESS`; if it isn't, go straight to the tracker comparison above rather than polling blindly.
- Poll cadence, as specified: a hard 1-hour timeout overall. The interval strategy is duration-aware rather than fixed — based on the dependency's average run duration and current elapsed time, first poll at 70% of that average, next at 80%, +10% each poll after, capped at 30 polls total. Whichever limit is hit first (poll count or the 1-hour wall clock) ends the wait. **Resolved (2026-09-19, open question #4): the percentage-based schedule alone governs.** The earlier "exponential backoff of 60 minutes" mention is stray/superseded, not a second real mechanism — confirmed directly, not guessed at. When no duration history exists yet for a new pipeline pairing (nothing to compute 70%/80%/etc. of), there's still no separate backoff fallback specified; see this build's own `[CHOICE]` on what fills that gap.

## Business rules

- `CFG_BUSINESS_RULES.BUSINESS_RULE_TYPE` (`INCOMPLETE`/`REJECT`) classifies the **row** a rule flags — it is not a pass/fail outcome. Rule *run* outcome (did the check itself execute successfully) is tracked separately, in `AUD_BUSINESS_RULES_RUN_LOG.STATUS`.
- Ordering is `SEQUENCE_NUMBER` (dense rank per task) directly on `CFG_BUSINESS_RULES` — same rank runs in parallel, different ranks run sequentially. There is no separate business-rule dependency table.
- Every target table is required to have a single-column primary key — an enforced framework convention, which is why `BUSINESS_RULE_KEY_COLUMN` can safely stay a single column rather than a list. This convention lives outside the Engine DB entirely (it constrains tables in the Data DB), so it cannot be checked by an Engine DB constraint — enforce it at `validate` time via schema introspection against the Data DB.
- A flagged row's classification is audit information, not a failure signal — a business-rule task that flags every row it checks is still a `SUCCESS` task run. Nothing here auto-blocks a pipeline; that would have to come from a separate, explicitly-declared task dependency on the BR task's own status.

## Handlers

Four, closed: `PYTHON` (ingestion scripts, `scripts.py`), `SQL` (engine-executed, wraps a bare `SELECT` per the closed action vocabulary, `sql_actions.py`), `BUSINESS_RULES` (drives `CFG_BUSINESS_RULES` rows for that task, `business_rules.py`), `EMAIL_ALERT` (added later than the other three — not in either pasted schema draft, but confirmed; SMTP send, `email_alert.py`, done 2026-09-20 — see "Where things stand" for the full design, including its HTML/CSS status-digest mode). An `EMAIL_ALERT` task is gated the same way any other task is, via a normal `CFG_TASK_DEPENDENCY`/`CFG_PIPELINE_DEPENDENCY` row — a `FAILURE` edge fires only when what it watches fails, `ALWAYS` fires regardless. No separate conditional mechanism needed.

## CLI surface

| Command | Purpose |
|---|---|
| `list` | List pipelines |
| `run --pipeline_code X [--task_code Y] [--force]` | The one execution verb (see Architecture above) |
| `configure` | (done 2026-09-20) Interactive setup chain — `configure.configure_interactive` |
| `configure --env` | Non-interactive setup from an env file |
| `set-execution-mode local\|orchestrator` | One-time-per-environment mode lock; persists in `craft-connector.yml` until rebuild/reconfigure |
| `graph --name <pipeline>` | Print dependency chain / lineage. Bare `graph` with no name errors. |
| `validate` | Config integrity check — including the cross-database checks (e.g. single-column PK on a `TARGET_TABLE`) that no Engine DB constraint can enforce, plus (added 2026-09-20) every active task declaring `SOURCE_OBJECT`/`TARGET_OBJECT` |
| `generate-yml` | Emit YAML (replaces the earlier working name `generate-dag`) — hand-rolled, Airflow-YAML-inspired shape, consumable by Airflow, another orchestrator, or a custom script |
| `lineage --table <schema.table>` | (added 2026-09-20) List every active task that declares `<schema.table>` as a `SOURCE_OBJECT` or `TARGET_OBJECT`, across all pipelines — closes the "table-level lineage" read-only verb this section used to list as conceptually agreed but unbuilt |
| `migrate` | (added 2026-09-20) Apply any pending `sql/migrations/*.sql` file to an already-deployed Engine DB — see `src/etl_craft/migrate.py` and `sql/migrations/README.md` |
| `steps --pipeline_code X` | (added 2026-09-20) List a pipeline's active tasks, each with its own `CFG_TASK_PARAMETERS` — closes the "steps-in-a-pipeline" read-only verb |
| `history --pipeline_code X [--task_code Y] [--limit N]` | (added 2026-09-20) Recent run history, pipeline- or task-level — closes the "run history" read-only verb |
| `generate-docs [--output <dir>]` | (added 2026-09-20) Emit the static, searchable documentation site described below — `src/etl_craft/docs_generator.py` |

Retired during design: `execute --local` / `execute --airflow` (collapsed entirely into `run`), any REST-based Airflow trigger/status commands, and a proposed `reap` command for stale-run cleanup (superseded by parent/child monitoring).

All of the "read-only query verbs conceptually agreed but not yet named or built" this section used to list — dependency graph (`graph`), table-level lineage (`lineage`), steps-in-a-pipeline (`steps`), run history (`history`) — are now built, closing that item out. Writing `CFG_` rows (registering a pipeline, adding a task, adding a dependency) stays outside the CLI — pipeline creation specifically is manual, git-managed migrations/inserts; how far *other* `CFG_` writes get their own verbs later is open ("most of it is fine," not fully enumerated).

The documentation generator (`generate-docs`, above) is also now built: the same read-layer queries as above (`cfg.py`, `resolver.build_graph`), rendered as a static, searchable site to a local output directory (deployable to GitHub Pages or any endpoint) rather than returned as CLI text. **[CHOICE]** "searchable" is a small, dependency-free vanilla-JS substring search over a generated JSON index, not a vendored copy of Fuse.js/Lunr.js — see `docs_generator.py`'s own module docstring for the full reasoning.

## craft-connector.yml

Versioned, no secrets stored directly in it — analogous to a dbt `profiles.yml`. Structure so far:

```yaml
[Execution]
Mode: local | orchestrator       # set via `set-execution-mode` at setup time only, persists until rebuild
Orchestrator name: <optional>    # informational only, engine doesn't act on it

[Source]
Type: file | environment         # where actual secret values live
Path: <path>                     # required when Type = file (a .env-style file)

[Postgres]                       # Engine DB — required, always Postgres
Active_profile: <name>
Profiles:
  dev:  { host, port, user, auth_mode, ... }
  sit:  { ... }
  uat:  { ... }
  prod: { ... }

[Warehouse]                      # Data DB — optional (see "Data DB / warehouse connector layer" above); resolved open question #1
Active_profile: <name>
Profiles:
  dev:  { host, port, user, auth_mode, ... }
  sit:  { ... }
  uat:  { ... }
  prod: { ... }

[Cloning]
Enabled: true | false            # opt-in, off by default — nothing is copied unless explicitly turned on
Scope: cfg | aud | all           # which table groups mirror into the Data DB, merge-style

[Orchestrator]                   # optional — global defaults/fallbacks for generate-yml's Airflow-facing fields
Global_dag: true | false         # emit the optional cross-pipeline trigger DAG — defaults to false
Catchup: true | false
Tags: [tag1, tag2]
Retries: <int>
Retry_delay_minutes: <int>
Depends_on_past: true | false
Email_on_failure: true | false
Email_recipients: [addr1, addr2]

[Email]                          # optional — required only if any active task uses HANDLER=EMAIL_ALERT; added 2026-09-20
Active_profile: <name>
Profiles:
  dev:  { host, port, from_address, auth_mode, user, use_tls, ... }
  sit:  { ... }
  uat:  { ... }
  prod: { ... }
```

`auth_mode` per profile selects one of a small registry of connection-creator functions (`password`, `token`, `sso`, `key_file`) — see Architecture above. `[Email]` profiles use their own, smaller `auth_mode` vocabulary (`none`, `password`) — see `email_alert.py`'s own module docstring for why `token`/`sso` aren't included there.

**Cloning**, specifically: a merge-style copy of selected Engine DB tables into the Data DB, so a team can query engine config/audit from inside their own warehouse without a separate Postgres connection. Runs after each pipeline run, only when enabled. This is special-cased engine-internal machinery — it does not reopen the "one Data DB, no general cross-connection support" rule that applies to ordinary tasks. **Done (2026-09-20)** — `src/etl_craft/cloning.py`; see "Where things stand" for the full mechanism (truncate-then-reinsert per table, generic cross-dialect column types, a same-database safety refusal, and the real ClickHouse-specific fixes this required).

## Database schema

Full DDL (complete first draft, needs review and sign-off) is at `sql/schema.sql`. It has been applied to a real local Postgres 16 and exercised against `sql/schema_test.sql`, which confirms — by actually triggering them, not just reading the DDL — that every load-bearing mechanism behaves as this file describes: the audit trigger stamps and preserves the right columns, the enum/conditional `CHECK` constraints reject what they should, the `DEPENDS_ON_PIPELINE_ID` default-from-sibling trigger fires, self-dependency is blocked, the partial unique indexes correctly allow a deactivated natural key to be reused while blocking a second active duplicate, and — the one that matters most — the `AUD_PIPELINES_RUN_LOG` partial unique index really does block a second concurrent `IN-PROGRESS` run for the same pipeline while leaving a second pipeline's own `IN-PROGRESS` row and any terminal-status row untouched. Re-run `sql/schema_test.sql` (instructions in its header) after any schema change that touches these mechanisms. Summary of tables:

**Config tables** — `CFG_PIPELINES`, `CFG_PIPELINE_DEPENDENCY`, `CFG_TASKS`, `CFG_TASK_DEPENDENCY`, `CFG_TASK_PARAMETERS`, `CFG_BUSINESS_RULES`.
**Audit tables** — `AUD_PIPELINES_RUN_LOG`, `AUD_TASK_RUN_LOG`, `AUD_BUSINESS_RULES_RUN_LOG`, `AUD_BUSINESS_RULES_RESULTS`, `AUD_TASK_OFFSET_TRACKER`, `AUD_PIPELINE_DEPENDENCY_TRACKER`, `AUD_TASK_DEPENDENCY_TRACKER`.
**Migration bookkeeping** — `SCHEMA_MIGRATIONS` (added 2026-09-20, see "Where things stand" and `sql/migrations/README.md`) — not a `CFG_`/`AUD_` table, purely for `etl-craft migrate`'s own use.

All primary keys use `GENERATED ALWAYS AS IDENTITY`. The pasted schema notes annotate almost every validated column as "(WILL BE ENFORCED BY THE ENGINE SETUP AS TRIGGER)" — `schema.sql` does **not** implement all of these as literal trigger functions; see its header and closing summary block for exactly which ones became real triggers (only where a `DEFAULT`/`CHECK` genuinely cannot do the job — namely `CREATED_BY`/`UPDATED_BY` capture, and `CFG_TASK_DEPENDENCY.DEPENDS_ON_PIPELINE_ID`'s default-from-sibling-column) versus which became plain `CHECK` constraints (all the enum-style value lists). This is a real, deliberate divergence from the literal pasted text — flagged prominently for sign-off, not silently decided.

## Open questions

Genuinely unresolved — don't guess silently on these, ask:

1. ~~What should the Data DB section in `craft-connector.yml` actually be called?~~ **Resolved (2026-09-19): `[Warehouse]`** — see "Data DB / warehouse connector layer" under "Where things stand" for the full reasoning; `"Data Db"`/`[Data Db 1]` stay rejected as too Informatica-shaped.
2. `CFG_PIPELINES` needs a stable code/slug for the CLI's `--pipeline_code` to resolve against, distinct from the numeric `PIPELINE_ID` — the same way `CFG_TASKS.TASK_CODE` works for tasks. Neither pasted schema draft has such a column. `sql/schema.sql` adds `PIPELINE_CODE` to close this gap; confirm the name and that this is really needed (vs., say, resolving `--pipeline_code` against `PIPELINE_NAME` instead).
3. ~~Email alert transport and `$$`-substitution-in-alert-bodies~~ **Resolved (2026-09-20)**: SMTP, via a new `[Email]` craft-connector.yml section; `$$`-substitution does apply, via a small closed token set — see "Handlers" above and `email_alert.py`'s own module docstring for the full design, including the later-added HTML/CSS status-digest mode.
4. ~~The dependency-poll backoff schedule~~ **Resolved (2026-09-19)**: the percentage-based schedule alone governs — see "Dependency resolution & polling" above.
5. Schema drift detection (a declared column list per target table, diffed against the live warehouse each run) is an endorsed principle from the original design discussion, but has no table or mechanism designed yet.
6. Every `[DEVIATION]`, `[ADDITION]`, and `[CHOICE]` flagged in `sql/schema.sql` — that file's closing summary block is the authoritative, current list; this file's "Database schema" section above is a pointer to it, not a duplicate.
7. ~~No migration tooling (Alembic or otherwise) has been discussed~~ **Resolved (2026-09-20)**: a small Alembic-*lite* runner (`src/etl_craft/migrate.py`, `etl-craft migrate`, the `SCHEMA_MIGRATIONS` table) — see "Where things stand"'s "Execution engine refinements" entry and `sql/migrations/README.md` for scope and the fresh-install/empty-migrations-dir consistency argument. `schema.sql` keeps its existing role as the full definition for a brand-new install; this only carries an *already-deployed* database forward incrementally from here on.
