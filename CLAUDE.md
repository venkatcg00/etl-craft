# CLAUDE.md — etl-craft

## What this is

`etl-craft` is a standalone, metadata-driven ETL orchestration engine. It is barebones by design, ships as a PyPI package (uv-compatible), and is meant to be adoptable by any team, independent of which orchestrator — if any — they use. This repo is **Repo 1** of a two-repo split: the engine itself, with zero domain knowledge and no team-specific pipelines baked in. A separate `metadata-etl-implementation` repo (not this one) will hold a reference implementation ("Support Insights") that proves the engine works at scale, and evolves independently of the engine.

The whole pipeline/task/step/dependency structure is modeled as **rows in Postgres**, not as Python DAG files or model files. The engine reads that metadata and executes it; it never writes logic it wasn't explicitly given.

## Where things stand / where to start

**SQLite is the default Engine DB; PostgreSQL is recommended for production — done (2026-09-24), per explicit instruction** ("lets make things easier for this project, implement sqlite as default engine and postgres as recommended for production"). **[DEVIATION]** from "Engine DB — always Postgres, no exceptions"; the Architecture section below is updated.
- **Zero-config start.** `etl-craft setup` with nothing set (no `.env`, no variables) writes `craft-connector.yml` with `Engine: {Profile: dev, Jdbc_url: jdbc:sqlite:etl-craft-engine.db}` and creates the schema. Every bootstrap input now defaults (`ETL_CRAFT_MODE=local`, `ETL_CRAFT_SOURCE_TYPE=environment`, profile `dev`). **[CHOICE]** a literal `Engine.Jdbc_url` is accepted for SQLite only: its URL is a credential-free file path, and every other Engine DB still goes through `Variables`. **[CHOICE]** `setup` never swaps an existing non-SQLite Engine DB for the default: a missing `ENGINE_JDBC_URL` against such a manifest is an error, and so is `ENGINE_USER`/`ENGINE_AUTH_MODE`/`ENGINE_SECRET` without a URL. `auth_mode: none` is valid for the Engine DB exactly when the URL is `jdbc:sqlite:` (`config._check_engine_profile`).
- **What was Postgres-only, and what replaced it.** `sql/schema_sqlite.sql` is a table-for-table translation of `schema.sql` (AUTOINCREMENT ids so the trackers' "newer than" comparisons never see reuse, partial unique indexes unchanged — including `ux_pipeline_run_one_active` — AFTER triggers for the audit/`DEPENDS_ON_PIPELINE_ID` logic, `IS NOT` for `IS DISTINCT FROM`, JSON text for JSONB). Timestamps are stored as one fixed UTC text format so text ordering is time ordering; `db.py` registers the adapter/converter so reads come back as aware datetimes, exactly as from Postgres. Query rewrites, portable to both: `bool_or` → `MAX(CASE …)`, `DISTINCT ON` → correlated `MAX`, SQL `now()` → a bound Python timestamp, `EXTRACT(EPOCH …)` → a dialect branch (`julianday`). **New `locks.py`**: the migration lock and the single-writer-warehouse queue (E2-61) are Postgres advisory locks or, on SQLite, OS file locks beside the database file. `migrate`/`init-db` split SQLite scripts with `sqlite3.complete_statement` (trigger bodies) and open an explicit transaction for DDL, which Python's sqlite3 otherwise autocommits. Packaged SQLite ENGINE migrations live in `sql/migrations/sqlite/` (empty: the SQLite schema postdates every Postgres migration).
- **[CHOICE] Relative SQLite paths resolve against the config file's directory**, not the cwd, so every command and every spawned task opens the same file. `jdbc:sqlite::memory:` is refused (each task is its own process). WAL mode, a 60 s busy timeout and `foreign_keys=ON` are set on every connection.
- **Known, stated limits — why Postgres stays the production answer**: one machine (remote orchestrator workers cannot open the file; `doctor` says so under `Mode: remote`), serialized Engine DB writes, no database users so `CREATED_BY` defaults to `etl-craft`, and `runlog`'s `begin_nested` SAVEPOINT commits early on pysqlite (harmless for those inserts, which are meant to be durable).
- **Bugs found by running, not reading**: an intermittent hang of a forked task child under the SQLite suite (it stalled `make check` for 20+ minutes, since `multiprocessing` joins leftover children at exit, beyond pytest-timeout's reach). `run_task` forked while the parent's pool still held idle SQLite connections, and SQLite forbids carrying an open connection across `fork()`; `_dispatch_with_crash_detection` now disposes the pool first on SQLite. Also: cloning's same-database guard raised on a `jdbc:sqlite:` Engine URL (so cloning never ran on SQLite) — fixed, covered by the end-to-end test. And `docs/first-pipeline.md` was broken on *any* Engine DB: it paired `CREATE_TABLE` with an `SCD1_MERGE` into the same target, which the merge's audit-column check refuses, and used Postgres-only SQL. It is now an `OVERWRITE_TABLE` staging copy merged into the mart, and `tests/test_sqlite_engine.py` executes the doc's own SQL blocks twice. **Flagged, not changed**: `SETUP_TABLE` drops and recreates its target on every run, so pairing it with a merge silently rebuilds the merged table each run.
- **Verification**: default suite 633 passed / 4 cloud skips (14 new tests in `tests/test_sqlite_engine.py`, no Docker needed). The whole suite also runs on a SQLite Engine DB via `ETL_CRAFT_TEST_ENGINE=sqlite` (`make test-sqlite-engine`, in CI and `make check`): 568 passed, 69 skipped — 57 by name in `conftest.POSTGRES_ENGINE_ONLY`, each reviewed and grouped by its Postgres-only premise (mostly tests that use the Engine fixture as their Postgres warehouse), plus the Trino-warehouse tests. **Open item, stated rather than hidden**: in that run, forking a task child from the long-lived, heavily threaded pytest process intermittently deadlocks the child inside the Trino HTTP client, and the orphaned child then blocks interpreter exit (which is what hung `make check`). It never reproduced alone, in the Postgres-engine run, or from the CLI (which forks from a fresh single-threaded process), so those tests are skipped in the SQLite run only, where they add no Engine-side coverage. `scripts/wheel-smoke.sh` now also runs zero-config `setup`/`doctor`/`list`/`validate` from an installed wheel. The first-pipeline walkthrough was run by hand through the real CLI as well.

**Iteration 3 round-2 remediation complete (2026-09-23, Codex).** E3-08–E3-13
and E3-15 are fixed; E3-14's claimed rename failure was not reproduced because the
renamed replacement uses the current CREATE format. E3-16 fixes validation of
Snowflake-managed Iceberg storage. Databricks JDBC credentials are stripped before
persistence, existing pipeline runs retain their original dependency watermark,
and token profiles reject unused fields. Credential-free SQL and migration-ledger
regression coverage is expanded. `make check`: **619 passed, 4 skipped, 95.36% coverage**; schema checks, mypy, Black, Ruff, and pydocstyle passed. `make wheel-smoke` also passed.
Cloud tests were not re-run; the prior live results below remain historical evidence.
E2-18 and E2-20 remain deferred. See ITERATION_3.md for per-finding resolutions.

**SCD1 null preservation (2026-09-23, user-requested).** Optional task parameter
`PRESERVE_TARGET` defaults to `false`. For `SCD1_MERGE`, `true` updates business
columns with `COALESCE(source_value, target_value)`: source nulls retain existing
values, while non-null source values replace them. Inserts and SCD2 are unchanged.
Both change detection and the stored `HASH_KEY` use the effective values of
`MERGE_COMPARE_COLUMNS`, so repeating a partially populated source does not cause
false updates. Validation and execution reject invalid boolean values or use on
another action. Regression coverage includes default/false/true behavior, inserts,
stable ROW_ID, stored hashes, and repeat-load idempotence; the current full-suite
result is recorded above.

**Preferred cloud connection shape adopted, Databricks UniForm decision, and Snowflake retested — done (2026-09-23).** Continues the entry immediately below on the same day, per explicit instruction to move Databricks and Snowflake onto a specific connection shape and test both against real cloud endpoints, including Iceberg.
- **Preferred connection: separate fields instead of one packed JDBC URL, tested and made the recommended shape for both.** Per explicit instruction: Databricks connects via `jdbc_url` (host/port/httpPath only) + `catalog` + `schema` + `token`; Snowflake connects via `user` + `account` + `database` + `schema` + `warehouse` + `role` + `token`. `warehouse.PREFERRED_CONNECTION_FIELDS`/`preferred_connection_url` and `config.py`'s `token`-in-`Variables` branch already existed from the prior session's work; this round is what actually proved them against both real endpoints and updated every doc that still taught the older packed-URL shape as primary (`docs/craft-connector.env-secrets.example.yml`, `docs/craft-connector.file-secrets.example.yml`, `docs/craft-connector.variables.env`, `docs/warehouse-test.env.example`, `docs/configuration.md`, `tests/conftest.py`'s own fixture docstring). The older single-JDBC-URL shape (key_file/password for Snowflake) stays documented as a supported alternative for a team standardising on RSA key-pair auth instead of PATs — nothing is removed, only reordered.
- **Databricks: both `native` and `iceberg` verified live, end-to-end through `CREATE_TABLE`/`OVERWRITE_TABLE`/`SCD1_MERGE` (insert + matched-row update), via the preferred connection shape and via a genuine `etl-craft setup` bootstrap round-trip (`WAREHOUSE_NAME=Databricks` + the four fields above → a real manifest → a real connection → `SELECT 1`).**
- **[DEVIATION] Databricks' `iceberg` format changed from a literal `USING ICEBERG` clause to a managed Delta table with UniForm enabled, per explicit instruction ("it should be uniform tables in databricks").** Probed both live before deciding: `CREATE TABLE ... USING ICEBERG` produces `DESCRIBE DETAIL` → `format: 'iceberg'`, a genuinely Iceberg-native Unity-Catalog-managed table with no Delta log at all; `CREATE TABLE ... USING DELTA TBLPROPERTIES ('delta.enableIcebergCompatV2' = 'true', 'delta.universalFormat.enabledFormats' = 'iceberg')` produces `format: 'delta'` plus the UniForm properties — a normal Delta table that also emits Iceberg metadata for external readers. `sql_actions.ICEBERG_TABLE_CLAUSE["databricks"]` now emits the UniForm clause; `CFG_TASK_PARAMETERS.TABLE_FORMAT` vocabulary is unchanged (`native`/`iceberg`), only what "iceberg" physically means on this one dialect changed. Low-risk by construction: Databricks always reads/writes the Delta representation regardless, so every existing `UPDATE`/`MERGE` statement this module issues for Databricks needed no change and was re-verified live against the new clause (both the native and iceberg/UniForm smoke tests re-run and passed after the switch).
- **Snowflake: connection, native tables, and Iceberg tables all verified live via the preferred (PAT) shape — the full SQL action vocabulary passes both table formats.** Sequence, in order:
  1. First attempt failed with `390432 (08001): Network policy is required` — a PAT cannot authenticate at all without a network policy assigned to the account or the user (Snowsight → Admin → Security → Network Policies), independent of credential correctness. This resolved partway through this session (the account gained a network policy); retried and the connection succeeded.
  2. `CREATE_TABLE`/`OVERWRITE_TABLE`/`SCD1_MERGE` (insert + matched-row update) passed against real Snowflake using `Table_format: native`, through both the test-suite fixture and a genuine `etl-craft setup` bootstrap round-trip (`WAREHOUSE_NAME=Snowflake` + the seven fields above → a real manifest → a real connection → a live session query). The live session's `CURRENT_ROLE()` initially reported `ACCOUNTADMIN` while the test `.env`'s `ROLE` field said `SYSADMIN` — a stale value in that file, not an engine or PAT limitation; corrected to `ACCOUNTADMIN`.
  3. **Iceberg tables were blocked, then unblocked by a genuinely different mechanism than the one first tried — `EXTERNAL_VOLUME = 'SNOWFLAKE_MANAGED'`, per a fix supplied by the user.** The account's own external volume (`ICEBERG`, GCS-backed) sits on a different cloud than the Snowflake account itself (AWS, `AWS_US_WEST_2` — found via `CURRENT_REGION()`/`SYSTEM$ALLOWLIST()`), and every attempt to write actual data through it failed with `100089 (42501): Failed to access remote file: access denied`, even after a correct GCS IAM grant (`SYSTEM$VERIFY_EXTERNAL_VOLUME` reported write/read/list/delete all `PASSED`; only a real data write — CTAS or INSERT — still failed, isolated precisely by testing DDL-only creation, which always succeeded, against CTAS/INSERT, which never did). **`EXTERNAL_VOLUME = 'SNOWFLAKE_MANAGED'` is a reserved value meaning "Snowflake's own internal storage, no customer bucket at all"** — it sidesteps the cross-cloud mismatch entirely, since the data never leaves Snowflake's own (AWS-hosted) storage. Verified live end to end: DDL, `INSERT`, and CTAS all succeed, `SHOW TABLES` reports `is_iceberg: 'Y'` / `is_external: 'N'`.
  4. **`sql_actions._create_iceberg_table_with_storage` now defaults to `SNOWFLAKE_MANAGED` when a task declares neither `EXTERNAL_VOLUME` nor `BASE_LOCATION`** — a real behavior change, not just a test fixture change: Snowflake Iceberg support is zero-config by default now, with no customer cloud storage to provision first. A task that names a real customer volume still must pair it with `BASE_LOCATION` (unchanged for that case). `ICEBERG_VERSION = 2` is now always specified explicitly too, per the original instruction to target Iceberg format v2 (verified as accepted syntax).
  5. **Two more Snowflake-Iceberg-specific fixes, both found by running the full vocabulary, not the DDL alone.** `ALTER TABLE ... RENAME TO` — used by both the schema-evolution rebuild and the computed-`ROW_ID` rebuild — is rejected by Snowflake against a table it created as `CREATE ICEBERG TABLE` ("Iceberg tables should use ALTER ICEBERG TABLE commands"); `_alter_table_keyword()` now selects `ALTER ICEBERG TABLE` there, keyed off the same per-connection `TABLE_FORMAT_INFO_KEY` `create_table_as` already reads. And Snowflake's own `TIMESTAMP WITH TIME ZONE` (`TIMESTAMP_TZ(9)` by default) is rejected on an Iceberg table's own column for its scale, and `TIMESTAMP_TZ` at *any* scale is then rejected outright ("Unsupported data type ... for iceberg tables") — Snowflake's managed Iceberg tables support no timezone-aware timestamp type at all. `_audit_column_type()` now returns `TIMESTAMP_NTZ(6)` for `CREATE_DATE`/`UPDATE_DATE` on Snowflake Iceberg specifically (matching the type Snowflake's own worked Iceberg-table example uses); the engine only ever writes UTC instants, so this loses no information.
- **Historical verification at `3915fd8`: 578 passed, 4 skipped (2 Databricks + 2 Snowflake, without credentials in the plain run), 94.17% coverage, `black`/`ruff`/`pydocstyle`/`mypy` all clean — the number independently re-measured and confirmed for this exact commit by `ITERATION_3.md`'s own round-2 review (see E3-11: an earlier version of this line, and two more numbers elsewhere in this same narrative, disagreed with each other and with the actual repo state).** Two of the eight `test_config_manifest.py` tests had to be rewritten rather than merely updated: they asserted validation behaviour (an unimplemented `auth_mode`, a `Warehouse.Name`/`jdbc_url` dialect mismatch) that lives on the older packed-URL shape, which the shipped Databricks example no longer demonstrates as its lead shape — moved to a small inline manifest fixture so that validation stays covered independent of which shape the docs choose to lead with. `test_sql_actions_run_against_real_snowflake` split into `..._native`/`..._iceberg`, matching the Databricks pattern, so both formats are exercised on every credentialed run rather than one or the other depending on which env vars happen to be set.
- **Test-matrix result: all four cells pass.** Databricks managed (`native`, Delta) and managed-UniForm (`iceberg`, Delta+UniForm); Snowflake native tables and Snowflake-managed Iceberg tables (`CATALOG = 'SNOWFLAKE'`, `EXTERNAL_VOLUME = 'SNOWFLAKE_MANAGED'`, format v2) — all fully verified live. Per this file's own practice, the shipped docs (`docs/configuration.md`, `docs/craft-connector.*.example.yml`) describe exactly this, and nothing that didn't pass.

**Live cloud verification continued by Codex (2026-09-23).** Databricks passed
both native Delta and Iceberg smoke tests against `practice_catalog`: CREATE_TABLE,
OVERWRITE_TABLE, SCD1 initial insert, and SCD1 matched-row update. The two tests
passed in 215.79 seconds, including verification of business values and stable
`ROW_ID` after update. This does not claim full SQL-action acceptance coverage.
Live failures exposed additional compatibility fixes: audit timestamp casts use
Databricks `TIMESTAMP`, hash inputs use `STRING`, qualified stage names are split
into catalog/schema/table for information-schema lookup, and SCD1 scalar update
subqueries use `FIRST` after source-key deduplication to satisfy Spark's scalar
cardinality rule. Cloud fixtures now reference each backend's own secret variable
instead of accidentally retaining the Postgres fixture's password. Smoke tests
use per-pipeline table names, verify resulting business values and `ROW_ID`, clean
up their targets, and allow 300 seconds for cloud execution.

Snowflake's fully qualified JDBC hostname now supplies the account argument that
the installed SQLAlchemy dialect otherwise omits. **Snowflake native smoke test
now passes (106.98 seconds)** after the user replaced browser SSO with password
authentication: SELECT 1, CREATE_TABLE, OVERWRITE_TABLE, SCD1 insert and matched-row
update, verified values/stable ROW_ID, and target cleanup. Iceberg remains untested
on Snowflake because no external volume/base location was configured. The earlier
browser-SSO attempt returned 390190 before opening a browser.

The cloud fixture accepts JDBC query credentials, removes them from the profile's
URL, and gives an explicit URL password precedence over the old key-file placeholder.
Literal `#` in JDBC query passwords is preserved. The standalone Snowflake schema
variable in `.env` still contains example text, so this live run set it in the
process environment from the URL's `schema` parameter. No private key was used;
`.env` was not changed. **Test counts below are this checkpoint's own snapshot at
the time, mid-session — an intermediate `test_unit.py`-only figure and one fuller
run, both since superseded by later work in the entries above.** [E3-11: flagged
as historical, not current, after CLAUDE.md's own count was found internally
inconsistent — see ITERATION_3.md's D-7/E3-11 for the current, single, re-verified
number.] After this fixture change: 265 unit tests passed, plus
the live Snowflake test; Black, Ruff, pydocstyle, mypy and diff checks passed.
Local regression verification: 573 passed, 3 cloud tests skipped, 94.39%
coverage; Black, Ruff, pydocstyle, mypy, and `git diff --check` passed. The two
Databricks tests were run separately with credentials as described above.

**Iteration 1 is feature-complete; the iteration-2 backlog is `ITERATION_2.md` — read it before starting new work (2026-09-20).** Every scope item this file describes is built (24 modules, 368 tests passing, CI green). A full code review of the finished result is written up in `ITERATION_2.md` as a numbered, prioritized backlog (`E2-01`…`E2-40`), with four P0 correctness bugs each **reproduced against real Docker Postgres** during the review — a `FAILURE`-gated task making a successful pipeline report `FAILED`, a duplicate invocation clobbering a still-running task's log row, engine-created tables that can never pass `validate`'s own primary-key check, and SCD merges permanently breaking on duplicate `MERGE_KEY` source rows. It also records the packaging finding that the built wheel ships no `sql/` at all (so there's no way to create the schema, and `etl-craft migrate` silently no-ops), plus a "deliberately not on this list" section so settled decisions don't get re-litigated. Anything in that file marked "from code" needs a confirming test written first; several items name a design question to raise rather than guess at.

**Iteration-2 scope set (2026-09-20).** A planning session walked the backlog against the live code, confirmed every claim it spot-checked, and set scope to **all 40 items** — plus three genuinely new ones that came out of the design decisions made there, recorded in `ITERATION_2.md`'s own "Added during iteration-2 planning" section along with the phase order and the decisions that are now settled: **E2-41** conditional dependency cardinality (`CFG_TASKS.RUN_CONDITION` = `ALL`/`ANY`/`N`, resolved into Airflow trigger rules at DAG-generation time), **E2-42** every SQL action except `DROP_TABLE`/`DELETE_ROWS` creating its target if absent, and **E2-43** a redesigned pipeline-level, three-flavour (`SUCCESS`/`COMPLETED_WITH_ERRORS`/`FAILED`) `EMAIL_ALERT` that supersedes the task-level design described under "Handlers" below. That section is the current map of what iteration 2 is doing and why; this file's own sections are updated as each phase lands.

**Iteration 2, phase 1 — dependency semantics and run status — done (2026-09-20).** Closes `ITERATION_2.md`'s E2-01, E2-02, E2-37 and E2-41, plus the `trigger_rule` half of E2-14. Four interlocking pieces, landed as one change because each is load-bearing for the others:
- **E2-41, conditional dependency cardinality.** **[ADDITION, post-signoff schema change]** `CFG_TASKS` gains `RUN_CONDITION` (`ALL`/`ANY`/`N`, NULL means `ALL`) and `RUN_CONDITION_COUNT`, with two `CHECK` constraints — per explicit instruction that a task depending on 10 others should be able to run once at least one meets its condition. `resolver.DependencyGraph.required_edge_count()` is the new pure primitive; `ready()`'s old `all(...)` over edges became a count comparison against it, so every pre-E2-41 row (NULL → ALL) behaves exactly as before. **[CHOICE]** the columns live on `CFG_TASKS`, per explicit instruction ("these should be resolved while dag chain generation itself") — a per-task mode maps 1:1 onto an Airflow trigger rule, which an OR-group on `CFG_TASK_DEPENDENCY` could never do. **[CHOICE]** named `RUN_CONDITION` rather than the also-offered `RUN_TYPE`, since `CFG_PIPELINES.REFRESH_TYPE` already owns the `*_TYPE` shape here. `build_graph` validates the mode standalone (so the resolver stays usable without a live Engine DB) *and* rejects a count larger than the task's own edge count — genuinely beyond what any `CHECK` can express, since it spans two tables, so `validate` surfaces it through `validate_graphs`.
- **E2-01, a `FAILURE`-gated task making a successful pipeline report `FAILED`.** New `resolver.DependencyGraph.unsatisfiable()` — the counterpart to `ready()`, returning never-run tasks whose edges can never be satisfied — plus `orchestrator.settle_unsatisfiable_tasks()`, which records them `SKIPPED`. Called from *both* the wave loop (every pass, since one `SKIPPED` can unblock a downstream `ALWAYS` edge) and `_finalize_from_task_states`, so `--finalize-only` under `Mode=orchestrator` — the generated DAG's `__finalize__` step, and that mode's only finalize path — is correct too. **The load-bearing detail**: an edge is permanently unsatisfiable only when its upstream is in the *new* `resolver.SETTLED_STATUSES` (`SUCCESS`/`SKIPPED`), deliberately **not** `TERMINAL_STATUSES` — `FAILED` stays retry-eligible per "retry resumes", and converting a merely-failed upstream into a downstream skip would report a broken run as `SUCCESS`, which is worse than the bug. A dedicated test pins that boundary. `orchestrator.py` now imports `SETTLED_STATUSES` from `resolver.py` rather than keeping its own identical copy (E2-35's spirit, applied while the file was open).
- **E2-02, a duplicate invocation clobbering a still-running task's log row.** `runner.run_task()` gains an explicit `IN-PROGRESS` branch beside the existing `SUCCESS` short-circuit, placed *before* the dependency check: it writes **nothing** and exits 0. `resolver.ready()` excludes an `IN-PROGRESS` task deliberately ("never re-dispatch"), and the dependency check could only read that exclusion as "dependencies not met" — so a second invocation used to overwrite the live row with `SKIPPED`, which lied in the audit log, disarmed the original process's crash detection (it only writes `FAILED` while the row still reads `IN-PROGRESS`), and left the outcome depending on which process wrote last. Reachable by ordinary means: an Airflow retry firing mid-run, or a human running a task the local orchestrator already spawned. The blanket skip reason also became honest — `_describe_unready()` now distinguishes "not yet" from "never will be", and both name how many of how many edges are needed.
- **E2-14's `trigger_rule` half.** **[DEVIATION]** `generate-yml`'s `depends_on` entries now carry `trigger_rule` instead of the raw `dependency_type` — one vocabulary, not two side by side, per explicit instruction ("dont do two parameter types"). `generate_yml.trigger_rule_for()` maps `(RUN_CONDITION, DEPENDENCY_TYPE)` onto `all_success`/`one_success`/`all_failed`/`one_failed`/`all_done`/`one_done`. Two combinations genuinely have no Airflow equivalent and are stated in the module docstring rather than papered over: `RUN_CONDITION='N'` (Airflow has `all_*`/`one_*` and nothing in between) and `HAS_DATA` (Airflow cannot see `TARGET_COUNT`). Both emit the permissive rule and leave the real gate to the engine, which fails safe — Airflow starts the task, the engine then records it `SKIPPED` if the condition isn't met.
- **E2-37** fixed in passing while `resolver.py` was open: `_check_acyclic` uses an explicit stack, so a chain deeper than Python's recursion limit raises `ResolverError`, not `RecursionError`.
- **First real migration file.** `sql/migrations/0001_add_run_condition.sql` — the mechanism built for open question #7 had never actually applied anything outside a test's `tmp_path`. Writing it immediately surfaced E2-05's documented `;`-splitting limitation *for real*: an earlier draft mentioned the semicolon character inside its own header comment and broke on itself. Two consequences, both kept: the file is written with no semicolon anywhere except as a statement terminator, and it drops each constraint before adding it (Postgres has no `ADD CONSTRAINT IF NOT EXISTS`, and this runner has no `$$` block available), so it is genuinely re-runnable as `sql/migrations/README.md` requires. The pre-existing `test_cli_migrate_reports_up_to_date_with_no_pending_files` was rewritten into `..._applies_the_real_migrations_directory_and_is_idempotent`, which now runs `migrate` twice against real Postgres — the only test that exercises a genuinely shipped migration rather than a synthetic one.
- **27 new tests (368 → 395), 99.8% coverage, `make check` and `make db-schema-test` both clean.** The E2-01 and E2-02 tests were each verified to *fail* against the pre-fix code before being kept, not merely to pass after it — the review's own rule for turning a reproduction into a regression test. `sql/schema_test.sql` gains 3 `EXPECT SUCCEED` and 4 `EXPECT FAIL` cases for the new constraints (12 `EXPECT FAIL` total, up from 8), each verified to land exactly on its marked statement. Manually verified end-to-end through the real CLI against Docker Postgres: `generate-yml` emitting `one_success` for an `ANY` task with three upstreams; that task passing its gate with only one upstream succeeded; and a `FAILURE`-gated `EMAIL_ALERT` on a successful pipeline finalizing `SUCCESS` (exit 0) with an honest `SKIPPED` row naming why — the exact scenario that reported `FAILED` before.
  - **Behaviour worth knowing, not changed here**: a task recorded `SKIPPED` is settled, so a manual `run --task_code` that gates it off early makes it un-runnable for the rest of that pipeline run even if the dependency is satisfied later. That follows from "retry resumes... skips what's already `SUCCESS` or `SKIPPED`" and predates this work; noted because the smoke test above walked straight into it.

**Iteration 2, phase 1b — the round-2 review's own findings — done (2026-09-20).** Phase 1 was independently re-reviewed and nine follow-on findings (`E2-44`…`E2-52` in `ITERATION_2.md`) were raised against it, six reproduced. All nine are fixed. The pattern worth remembering: **most of them were in the new surface phase 1 created, not in what it fixed** — E2-41 was specified over "a task's dependencies" but implemented over `same_pipeline_edges` only, because `fetch_pipeline_graph` deliberately filters cross-pipeline edges out before `build_graph` ever sees them.
- **E2-44/E2-45, resolved by explicit decision: `RUN_CONDITION` ranges over ALL of a task's edges, cross-pipeline included.** A task author writing "depends on 10 tasks, any one will do" has no reason to care which pipeline an upstream lives in. `TaskNode` gained `cross_pipeline_edge_count` (the edges themselves still stay out of the graph — the resolver has no way to settle them — but the *count* has to reach the arithmetic), `cfg.fetch_pipeline_graph` populates it, and **[DEVIATION]** `crosspipe.check_task_cross_pipeline_dependencies` now returns a `CrossPipelineCheck` (satisfied count, total, per-edge reasons) instead of the first-failure `str | None`, which could only ever express ALL. `runner.run_task` counts both halves against one requirement rather than gating them one after the other. Before this, `ANY` silently meant "any same-pipeline edge **and** every cross-pipeline edge", and `N` rejected a valid cross-pipeline config outright with "it could never run". **Real upside the new shape buys:** `check_task_cross_pipeline_dependencies` takes a `needed` count and stops once it is reached, so an `ANY` task whose condition is already met from the same-pipeline side never polls a cross-pipeline edge at all — previously it could block a process (and an orchestrator worker slot) for up to an hour on an edge it did not need.
  - **[CHOICE], and the non-obvious part:** `ready()` treats *unevaluated* cross-pipeline edges optimistically — as though satisfied — and uses real counts only when a caller supplies them. That is not laziness: `ready()` is also `orchestrator._run_until_settled`'s wave pre-filter, and the real cross-pipeline gate runs *inside* the spawned `run --task_code` subprocess. Counting them pessimistically there would deadlock — the task would never be spawned, so the check that settles it would never run. `unsatisfiable()` counts them as still-possible for the same reason: this module cannot see `AUD_*_DEPENDENCY_TRACKER`, so it must never doom a task on the strength of the half it can see.
- **E2-46, resolved by explicit decision: `trigger_rule` moved from the edge to the task.** Airflow's `trigger_rule` is a property of the *task* — one value, applied to all its upstreams — but phase 1 emitted it per `depends_on` edge, which is only coherent while every edge of a task shares one `DEPENDENCY_TYPE`, and nothing requires that. A task with a `SUCCESS` edge and an `ALWAYS` edge emitted two conflicting rules, handing a loader a choice it cannot make correctly — precisely the failure that emitting `trigger_rule` instead of the raw type was meant to prevent. `depends_on` is now a plain list of task names, and `generate_yml.task_trigger_rule()` resolves one rule per task; mixed types give the permissive `all_done` and let the engine gate, the same documented fail-safe already used for `N` and `HAS_DATA`. Preserves every currently-valid config rather than forbidding mixed types.
- **E2-47: "not yet" and "never" are no longer recorded the same way.** `SKIPPED` is terminal (`NOT_RETRYABLE`, `SETTLED_STATUSES`), so writing it for a task whose dependency simply hasn't run *yet* permanently disqualified that task from the run — a later invocation, even with the upstream genuinely `SUCCESS`, still refused, and the pipeline finalized `SUCCESS` with the task never having run. CLAUDE.md explicitly supports the paths that hit this ("a manual single-task run, a backfill, a re-triggered task"), so the verification must not be destructive. **[DEVIATION]** only the permanently-unsatisfiable case writes a terminal row now; "not yet" writes **nothing** and exits 0, the shape E2-02 established for `IN-PROGRESS`. A cross-pipeline edge that came back unsatisfied still writes `SKIPPED` — `crosspipe` just polled it to its budget, so it has had its chance, which is what "correctly gated off by design" means.
- **E2-48, resolved by explicit decision: both halves.** The generated `__init__` edge emits `all_success`, not `all_done` — with `all_done`, a failed run-minting step still started every root task, and each took `resolve_run_for_task`'s dev/ad-hoc fallback into the *previous, already-finalized* run and rewrote its rows. And `resolve_run_for_task` now refuses to bind to an already-**finished** run without `--force`. **[CHOICE]** `FINISHED_RUN_STATUSES` is `SUCCESS`/`FAILED` only, deliberately **not** `SKIPPED`: a `SKIPPED` run is what `orchestrator.py` writes when a pipeline's own cross-pipeline gate was never met, precisely so every task binding to it records `SKIPPED` in turn. Binding there is additive and intended; binding to a `SUCCESS` or `FAILED` run overwrites rows already reported on. That distinction was caught by an existing test failing, not by review — the first version of the guard broke the cross-pipeline gating flow outright.
- **E2-49 + E2-39 (pulled forward from phase 7, same line of defence): `main()` has no path left that tracebacks on a non-bug.** `ResolverError` joins `RUN_ERRORS` (E2-41 gave `build_graph` four new ways to raise, and `--finalize-only` now builds a graph where it previously did not, so a config problem could take out a generated DAG's synthetic *last* step), `_graph_command` builds its graph inside its own `try`, and a top-level `SQLAlchemyError` catch covers every command — `list` and `generate-docs` previously had none at all, so an unreachable Postgres printed a raw traceback.
- **E2-50: `settle_unsatisfiable_tasks` could re-create E2-02's clobber from the other side.** It read `run_state` in one transaction and wrote in another, with nothing re-checking that the rows were still absent — so a concurrent `run --task_code` that created the row and started executing in between got overwritten with `SKIPPED`. The window is real, not theoretical: it runs on every wave pass while subprocesses are live. `TaskRunBinding` gained `created: bool`, and settle now writes one transaction per task, only when *it* created the row.
- **E2-51/E2-52, both made deliberate rather than silent.** `waves()` stays the guaranteed-safe static order and deliberately ignores `RUN_CONDITION` (**[CHOICE]** — "earliest possible" and "guaranteed safe" are different questions, and its two consumers, `graph` and `run_pipeline(force=True)`, both want the second); `graph` now names any task whose `RUN_CONDITION` lets it start earlier. And every generated YAML file carries a header explaining why `tasks:` speaks `trigger_rule` while the informational `pipeline_dependencies`/`cross_pipeline_task_dependencies` blocks keep the raw `dependency_type` — they describe `CFG_` rows, not anything Airflow executes, so there is no task for a rule to attach to.
- **407 tests (up from 395), 99.8% coverage, `make check` and `make db-schema-test` clean.** **Method note worth keeping:** the E2-50 regression test initially passed for the *wrong* reason, and coverage is what exposed it — seeding the `IN-PROGRESS` row up front means `unsatisfiable()` excludes the task before the `created` guard is ever reached, so the guard line stayed uncovered while the test went green. It now drives the interleaving explicitly (monkeypatching `fetch_run_state` to create the row after returning state) and was verified to fail without the guard. A regression test for a race has to actually reach the race.
- Manually verified end-to-end through the real CLI against Docker Postgres: the new one-rule-per-task YAML shape (roots on `all_success`, a mixed-edge task on `all_done`, `__finalize__` on `all_done`, plus the explanatory header), and `run --task_code` refusing cleanly (exit 1, one line) against a pipeline whose latest run is already `SUCCESS`.

**Iteration 2, phase 2 — SQL action correctness — done (2026-09-20).** Closes `E2-03`, `E2-04`, `E2-42`, `E2-31`, `E2-33` and E2-25's crash half. One pass over `sql_actions.py`'s action bodies; no schema change (every addition is a `CFG_TASK_PARAMETERS` convention, per this project's established practice).
- **E2-03, `PRIMARY_KEY`.** CLAUDE.md states "every target table is required to have a single-column primary key — an enforced framework convention", checked by `validate` through introspection — but every table this module creates is built with `CREATE TABLE ... AS SELECT`, which never creates one, so the engine's own tables could never satisfy the engine's own convention. New optional `CFG_TASK_PARAMETERS.PRIMARY_KEY`, applied as `ALTER TABLE ... ADD PRIMARY KEY` after the CTAS by every creating action, and **re-applied by `_evolve_schema` after its drop-and-rename**, which would otherwise destroy it. **[CHOICE]** deliberately independent of `MERGE_KEY`, per explicit instruction ("a merge can have both primary key and merge key") — a target's identity and the columns a merge matches on are different questions even when they coincide. **Known, now-stated limit of the same mechanism**: the evolution rebuild does *not* recreate indexes or grants, only the primary key this module knows about.
- **E2-42, create-if-absent.** Per explicit instruction ("apart from drop and delete, everything should create a table if the target does not exist, using select query and adding audit columns"), `_check_or_evolve_schema` now bootstraps a missing target (`_create_target_shape`: the staged SELECT's business columns, plus `PIPELINE_RUN_ID` and that action's own `AUDIT_COLUMNS`, plus the declared primary key, created empty with `WHERE 1 = 0`) instead of raising "run a SETUP_TABLE or CREATE_TABLE task against it first". `DROP_TABLE`/`DELETE_ROWS` unchanged. A first run of `OVERWRITE_TABLE` or either SCD merge no longer needs a separate setup task.
- **E2-04, `MERGE_DEDUPE_ORDER`.** Nothing checked that the staged SELECT yields one row per `MERGE_KEY`, and the target had no primary key to catch it either — so two source rows sharing a key silently corrupted an SCD1 target on run 1 (both took the `NOT EXISTS` insert leg, leaving two "current" rows, reported `SUCCESS`), then killed it permanently on run 2 with a cardinality violation from the correlated `SET col = (SELECT ...)`. By then the duplicates were in the *target*, so no retry could recover it without manual SQL. `_dedupe_stage` now runs **before any statement touches the target**. **[CHOICE], per explicit decision:** duplicates are resolved by a declared ordering (`MERGE_DEDUPE_ORDER`, an `ORDER BY` fragment) and **rejected outright when none is declared** — the engine will not invent an ordering nobody gave it, since which row survived would be undefined and could differ between runs. The dedupe itself is `ROW_NUMBER() OVER (PARTITION BY ... ORDER BY ...)` into a new temp table rather than a `DELETE`, because deleting duplicates in place needs a dialect-specific row identity (`ctid`, `ROWID`) — the one thing this module works hardest to avoid.
- **E2-25's crash half, `split_object_ref()`.** A `TARGET_OBJECT` with no dot raised a bare `ValueError: not enough values to unpack` from inside the crash-detection fork, so the parent reported only its generic "died unexpectedly" fallback — a traceback-shaped message about a config typo. Worse, `qualify()` didn't validate either (it just prefixed the database), so the creating actions silently emitted a *malformed two-part name*. One validating splitter now backs `qualify()` and all three former `split(".", 1)` sites.
- **E2-31/E2-33, portability — and a real ClickHouse finding the tests surfaced.** `_hash_expression` uses ANSI `CAST(... AS VARCHAR)` instead of Postgres's `::text` shorthand; `_add_hash_key`'s `UPDATE` lost its non-ANSI target alias; `AUDIT_COLUMN_TYPES` gained lengths on `VARCHAR` (bare `VARCHAR` is rejected in DDL by several dialects) and `TIMESTAMP WITH TIME ZONE` for `CREATE_DATE`/`UPDATE_DATE` (schema.sql uses `TIMESTAMPTZ` throughout and the engine writes `datetime.now(UTC)`, so every warehouse-side audit timestamp was silently losing its offset). The module docstring now states plainly that the atomicity guarantee is **DDL-dependent** and does not hold on MySQL/Oracle, rather than leaving it implied.
  - **Two ClickHouse differences, both verified against the live container rather than assumed** — the "prove it for real" bar `warehouse.py`'s own ClickHouse test set. (1) ClickHouse's `MD5()` returns `FixedString(16)` — raw bytes — where Postgres's returns 32 hex characters, so `HASH_KEY VARCHAR(32)` stored the wrong thing entirely there; the dialect branch wraps it in `lower(hex(...))`. (2) **Found only because the test ran against real ClickHouse, not from reading code**: `CAST(col AS VARCHAR)` raises `CANNOT_INSERT_NULL_IN_ORDINARY_COLUMN` on a nullable column the moment any value is NULL, and the surrounding `COALESCE` *cannot* rescue it because the cast is evaluated first. The branch casts to `Nullable(String)` there instead. Both keyed off `conn.dialect.name`, never an import — the pattern `cloning.py` already established. `_apply_primary_key` is likewise a no-op on ClickHouse, where ordering is a table-engine property fixed at `CREATE` time and there is no `ALTER TABLE ... ADD PRIMARY KEY` at all.
- **416 tests (up from 407), 100% coverage on `sql_actions.py`, `make check` clean.** The E2-04 test was verified to fail against the pre-fix code — without the guard, run 1 reports `SUCCESS` while writing both duplicate rows, which is exactly the silent corruption. One pre-existing test (`..._missing_target_fails_clearly`) asserted the behaviour E2-42 deliberately reverses and became `..._creates_a_missing_target`.

**Iteration 2, phase 3 — `EMAIL_ALERT` redesigned as a pipeline-level completion alert — done (2026-09-20).** Closes `E2-43`. Designed by interview during iteration-2 planning rather than guessed at; supersedes the task-level design the original module was built around. No schema change — every addition is a `CFG_TASK_PARAMETERS` convention.
- **Scope moved from the task to the run.** Per explicit decision ("task level emails are noise"), one email per pipeline run. `email_alert.run_flavour()` computes it from every active task's status under this `pipeline_run_id` via `cfg.fetch_task_statuses_for_run`, **not** from `AUD_PIPELINES_RUN_LOG` — under `Mode=orchestrator` the run isn't finalized until the `__finalize__` step, which is after the alert.
- **Three flavours**, worst-first: `FAILED` (any task FAILED) → `COMPLETED_WITH_ERRORS` (nothing failed outright but something short of clean — a `SKIPPED` task, a task carrying an `ERROR_MESSAGE` while succeeding, or a task not settled yet) → `SUCCESS` (every task SUCCESS, none carrying an error). The middle one is the literal case the instruction named: "if the pipeline is marked success with failure then a neutral status like pipeline is COMPLETED with errors". **[CHOICE]** unsettled tasks (`PENDING`/`IN-PROGRESS`) land in the neutral middle rather than `SUCCESS` — the alert is *designed* to run at the end but nothing forces it to, and reporting a plain `SUCCESS` for a run that hasn't finished is the one genuinely misleading answer of the three.
- **The alerting task excludes itself**, which needed `cfg.TaskStatusEntry` to carry `task_id` (it previously exposed only `task_code`). An `EMAIL_ALERT` task is necessarily `IN-PROGRESS` while it runs, so counting it would make every single run resolve to `COMPLETED_WITH_ERRORS`. Easy to miss, and it would have made the feature useless rather than merely wrong.
- **Three templates, one chosen** ("have three templates as said in flavour answer and choose 1 as needed"): optional `EMAIL_SUBJECT_<STATUS>`/`EMAIL_BODY_<STATUS>`, each falling back to the plain `EMAIL_SUBJECT`/`EMAIL_BODY`. That fallback is why every pre-existing `EMAIL_ALERT` test kept passing untouched — a task declaring only the plain pair behaves exactly as before. New `$$status` substitution token, and a coloured flavour banner above the existing intro/digest.
- **`EMAIL_ON_STATUS`**, pipe-separated ("if there are parameters saying which status to send, send only on that condition, else send on all statuses"). When the computed flavour isn't in the list the task records **`SUCCESS`** with `EMAIL_SENT = false` and the reason in `TASK_LOG` — deliberately not `SKIPPED`: it ran and correctly decided not to act, and `SUCCESS` keeps it out of the unsettled set so it can never re-create E2-01. An unrecognized value is a clean `HandlerError`, not a silent no-send.
- **426 tests (up from 416), 100% coverage on `email_alert.py`, `make check` clean.**
- **Still open, deliberately**: `COMPLETED_WITH_ERRORS` currently keys off a `SKIPPED` task or a non-null `ERROR_MESSAGE` on a succeeded row, because today's schema cannot distinguish "succeeded first time" from "succeeded on retry" — `update_task_run` overwrites in place. E2-21's `ATTEMPT_COUNT` (phase 7) is what makes that computable; worth revisiting the definition once it lands, as `ITERATION_2.md` records.

**Iteration 2, phase 4 — the install path — done (2026-09-20).** Closes `E2-13`, `E2-05`, `E2-06`, `E2-15`, `E2-16`. `CLAUDE.md`'s first paragraph says the engine "ships as a PyPI package (uv-compatible), and is meant to be adoptable by any team" — a team running `uv add etl-craft` could not previously reach a working Engine DB by any route.
- **E2-13, the wheel shipped 24 `.py` files and nothing else.** No `sql/`, no `py.typed`, no license, and `[project]` had no `license`/`classifiers`/`keywords`/`urls`. `sql/` therefore **moved to `src/etl_craft/sql/`** — uv_build packages `src/<module>/` only — with `[tool.uv.build-backend] source-include` shipping it and `py.typed`. New `packaged_sql.py` reads it through `importlib.resources` rather than `__file__` arithmetic, so it behaves the same from a checkout, a wheel, or a zipimport. `docker-compose.yml`, the `Makefile` and CI all point at the new path (verified by a full `docker compose down -v` + re-up, so the schema genuinely re-applies from there).
- **New `etl-craft init-db`** (`init_db.py`), the verb that was missing entirely: applies the packaged schema to an empty database. **Refuses** a database that already has engine tables — `schema.sql` is plain `CREATE TABLE` and deliberately not idempotent, so re-running it would fail half-applied; the refusal names what it found and points at `migrate`. `--force` overrides, and is never used on the engine's own initiative.
- **E2-05, `migrate` silently no-opped.** Its `Path(__file__).parents[2]` default resolved above `site-packages` once installed, and `Path.glob` on a nonexistent directory yields nothing *without error*, so it printed "already up to date" and skipped a team's migrations. Resolution is now `--migrations-dir` → `$ETL_CRAFT_MIGRATIONS_DIR` → `./sql/migrations` → the packaged copy, and a directory that doesn't exist is a clean error rather than silence. `SCHEMA_MIGRATIONS` is created if absent (a database predating it previously produced an uncaught `ProgrammingError`), and a Postgres advisory lock spans the whole run so two concurrent `migrate` calls can't double-apply.
  - **The `;`-splitting limitation is fixed, not just documented.** `split_statements` is now a small scanner tracking single-quoted strings (with their `''` escape), dollar-quoted bodies including tagged `$tag$` ones, and `--`/`/* */` comments. It had to be: `schema.sql`'s own trigger functions are `CREATE FUNCTION ... $$ ... ; ... $$` bodies, so `init-db` could not have worked at all under the old `split(";")`, and the review's prediction that this would be "very likely the *first* migration anyone writes" was if anything understated.
- **E2-06, config could only be found in the cwd.** New `config.resolve_config_path`: `--config PATH` (on every command) → `$ETL_CRAFT_CONFIG` → an upward search from the cwd (the `pyproject.toml`/`.git` pattern) → the plain relative name, so the "not found" error still names something a reader recognizes. Threaded into `configure`/`set-execution-mode` too, so `--config` means the same thing for the verbs that *write* the file as for those that read it.
- **E2-16, `configure` never named the secret it needed.** It writes profiles whose secrets are read from `ETL_CRAFT_{SECTION}_{PROFILE}_SECRET` and never mentioned that name, so the flow was: answer every prompt, then watch the next command fail. It now prints the exact variable per profile, says where to put them based on `[Source]`, and points at the new verb. **New `etl-craft doctor`** (`doctor.py`) resolves config, every secret, the Engine DB, the Data DB and the SMTP relay, reporting *every* check rather than stopping at the first — the same "collect, don't raise" shape `validate` uses. Built entirely from functions that already existed.
  - **[Bug caught by its own test, and a real design flaw]**: `doctor` was first dispatched after the shared `load_config`/`build_engine` setup — so an unresolvable secret exited 2 from `build_engine` before doctor said a word, defeating the entire point of the command. It now runs *before* that setup and reports a bad config as its own `[FAIL] Configuration:` check.
- **E2-15, documentation.** `README.md` was the 11-byte string `# etl-craft`, and that is the PyPI landing page. Replaced with a real one (what it is, install, five-minute quickstart, the full command table, an exit-code table), plus `docs/`: `configuration.md`, `parameters.md` (the `CFG_TASK_PARAMETERS` reference per handler, extracted from the module docstrings), `first-pipeline.md` (a runnable walkthrough with the actual `INSERT`s, since pipeline creation is deliberately not a CLI verb), and a fully commented `craft-connector.example.yml` (verified to parse).
- **452 tests (up from 426), 99.5% coverage, `make check` clean. Proved for real, which is the only way this class of bug shows up:** built the wheel, listed it (`sql/`, `py.typed` and the license all present), installed it into a clean venv *outside* the checkout, and ran `--help` → `init-db` → `init-db` again (correctly refused) → `migrate` → `migrate` again ("already up to date") → `list` → `validate` → `doctor` against a throwaway database. That smoke test also caught a genuine bug in `init_db`'s sentinel query — a tuple bound to `IN :names` without `expanding=True`, which no unit test would have found. CI gets the same job in phase 5.

**Iteration 2, phase 5 — making CI able to catch the next round — done (2026-09-20).** Closes `E2-26`, `E2-27`, `E2-28`, `E2-29`. The review's headline observation was that 368 passing tests sat alongside four P0 bugs, all in untested scenario space; this phase is about the gates, not more features.
- **E2-26, a gate that didn't gate.** `sql/schema_test.sql`'s own header said "There is no pass/fail summary line; verify by eye that failures land exactly on the statements marked EXPECT FAIL" — and CI ran it **without** `-v ON_ERROR_STOP=1` (unlike the `schema.sql` step immediately above it), so the step always exited 0 and nobody was looking. A regression making an `EXPECT FAIL` case start *succeeding* — the exact class of bug the file exists to catch — passed silently. All 12 `EXPECT FAIL` cases are now wrapped in a `DO $do$ ... RAISE EXCEPTION 'EXPECT FAIL did not fail: ...' EXCEPTION WHEN <sqlstate> THEN ... END $do$` block, each swallowing only the SQLSTATE class its own `\echo` names. `EXPECT SUCCEED` statements stay plain — under `ON_ERROR_STOP=1` any of them failing aborts the run, which *is* the assertion. The "verify by eye" note is gone.
  - **Proved both directions against real Postgres, which is the only way to know a gate gates**: a correct schema exits 0; dropping `ck_tasks_handler` and re-running produces `ERROR: EXPECT FAIL did not fail: HANDLER not in allowed list` and a non-zero exit.
- **E2-27, CI and the Makefile disagreed about a known hazard.** The Makefile was fixed months ago to run the schema test in a disposable `etl_craft_schema_test` database, precisely because its leftover `PL_A`/`PL_B` rows broke a globally-scoped query; CI still ran it against the shared `etl_craft` database, before `pytest`. CI now creates/applies/drops the same disposable database. (`schema_test.sql` still hardcodes `PIPELINE_ID = 1`/`2`, which is now *correct* rather than fragile, since both runners guarantee an empty database — its own header already required that.)
- **E2-28, CI gaps.** Python matrix 3.11/3.12/3.13 (`requires-python = ">=3.11"` claimed support only 3.11 ever exercised); `on: push: branches: [main]` added, so `main` can no longer be broken by a merge whose base moved; `.github/dependabot.yml` was the unmodified template with `package-ecosystem: ""` and updated nothing — now `uv` + `github-actions`. **And a real wheel job**: `scripts/wheel-smoke.sh` builds the wheel, asserts it actually contains `sql/`, `py.typed` and a license, installs it into a clean venv **outside the checkout**, and drives `--help` → `init-db` → `init-db` again (must refuse) → `migrate` → `migrate` again → `list`/`validate`/`doctor` against a disposable database. This is the gap that hid E2-13 entirely: a suite that always runs from the git checkout cannot see a packaging bug. It runs locally too (`make wheel-smoke`), falling back to `docker compose exec` for `psql` on a dev machine that doesn't have it installed.
- **`make check` now genuinely mirrors CI** — it previously omitted both `psql` schema steps and had no typecheck, which is how the two came to disagree in the first place.
- **E2-29, mypy.** Every module uses `from __future__ import annotations` with complete signatures and nothing checked them. Added to the dev group, `make check` and CI. **[CHOICE]** not `--strict`: `disallow_untyped_defs`/`check_untyped_defs`/`warn_unused_ignores` and friends, over `src/` only — the goal is catching real defects, not spending the iteration annotating test helpers. **Its first pass found 13 real issues**, including two the review predicted and one genuine latent bug:
  - `db.py` passed `sslpassword` as **bytes** where psycopg's signature is `str | int | None`; it happened to work, but was wrong by the driver's contract. One existing test asserted the wrong thing and was corrected with a note.
  - `email_alert.py` passed `profile.user` (`str | None`) straight to `SMTP.login`. An `[Email]` profile with `auth_mode: password` and no `user` would have crashed the forked child with a `TypeError`, losing its real message to the generic "died unexpectedly" fallback. Now a clean `HandlerError` naming the profile.
  - `sql_actions.py` and `cloning.py` both reached `config.warehouse.active` behind a comment asserting it was non-None. Narrowed properly.
  - `resolver.py`'s `HAS_DATA` comparison, and four `cli.py` variables reused across branches for genuinely different types.
- **452 tests, 99.5% coverage, `make check` (now including `db-schema-test` and `typecheck`) clean.**

**Iteration 2, phase 6 — the remaining correctness items — done (2026-09-20).** Closes `E2-07`, `E2-08`, `E2-09`, `E2-10`, `E2-11`, `E2-12`.
- **E2-10, security-relevant: the Engine DB silently dropped its JDBC query string.** `_JDBC_POSTGRES_RE` had no `query` group and stopped the database capture at `?`, so `jdbc:postgresql://host/db?sslmode=require` connected **without** TLS and said nothing. `warehouse.py`'s own translator parsed and forwarded query parameters all along, which made the Engine DB — the one that is always Postgres and always required — the weaker of the two. Now parsed and forwarded to the driver by both creators.
- **E2-09, predictable failures surfaced as "died unexpectedly".** `handlers.dispatch` wraps only `ConfigError` and `SQLAlchemyError`, so anything else — an `OSError` from a missing `SCRIPT_NAME`, a `NotImplementedError` from an unbuilt warehouse auth mode, a plain bug — killed the forked child with its message written nowhere, and the parent reported only the generic fallback. `_dispatch_and_record` now catches `BaseException`, writes `FAILED` with the real type and message plus a truncated traceback in `TASK_LOG`, and re-raises. The parent's fallback is left covering only what it was designed for (OOM-kill, segfault).
  - **[Bug found, in a test rather than the code]**: `test_run_task_detects_a_child_that_dies_unannounced` monkeypatched `dispatch` with a stale one-argument `_crash(handler)` stub. The child was therefore dying of a `TypeError` inside `runner`, never reaching `os._exit` at all — and the test passed anyway, because *any* non-zero exit produced the same "died unexpectedly" row. E2-09's new handler exposed it by giving the `TypeError` a real message. The stub now matches `dispatch(engine, ctx)`, so the test exercises the path it claims to.
- **E2-08, `HAS_DATA` was unsatisfiable downstream of an ingestion task.** `HAS_DATA` means "upstream SUCCESS and `TARGET_COUNT > 0`", but `scripts.py` set only `source_count` — so an edge on the most natural place to want one could never fire (and, before E2-01, also poisoned the pipeline's status). **[CHOICE]** `INGESTION_COUNT` now populates `target_count` and `insert_count` as well: what an ingestion script wrote is exactly what a downstream `HAS_DATA` means.
- **E2-11, the "hard 1-hour timeout overall" was per edge.** Both the deadline and `MAX_POLLS` were computed inside `_wait_for_*_to_settle`, called once per edge in a loop — so a task with three cross-pipeline edges could wait three hours and spend ninety polls. New `PollBudget`, created once per `check_*` call and shared across every edge it waits on.
- **E2-12, the tracker consumed a run the gate never used.** `consume_*` re-evaluated satisfaction at finalize time and recorded whatever qualified *then*. If the upstream completed a second qualifying run while this one executed, the watermark jumped past it and marked it consumed by a pipeline that never read its data — making the tracker's own claim ("the run last consumed for each dependency edge") false in exactly the different-cadence case it was designed for. The gate now returns the run id it actually resolved per edge (`PipelineGateResult.consumed` / `CrossPipelineCheck.consumed`), threaded back into `consume_*`. `--force` skips the gate, so it keeps the old re-derive behaviour, as do callers finalizing a run they didn't gate themselves.
- **E2-07, "the engine owns every write" wasn't enforced.** CLAUDE.md calls for "a bare, **validated**, read-only SELECT" and nothing validated it — `SOURCE_SQL` goes straight into `CREATE TEMPORARY TABLE stage AS {select}`, and Postgres supports data-modifying CTEs, so `WITH x AS (DELETE FROM other RETURNING *) SELECT * FROM x` is a perfectly valid "SELECT" that writes. New `validate.looks_read_only` + `validate_read_only_sql` lint every active `SOURCE_SQL` and `BUSINESS_RULE_SQL`: must start with `SELECT`/`WITH`, must not contain a write keyword as a bare word, with comments and string literals stripped first so a column named `update_date` or a literal `'DROP TABLE t'` is not a false positive. **[CHOICE], stated in the code**: this is a **lint, not a security boundary** — the real control is that `CFG_` rows are git-reviewed. It lives in `validate` rather than at runtime so a config problem is findable before 3 a.m.
- **469 tests (up from 452), 99.3% coverage, `make check` clean.**

**Iteration 2, addendum — documentation, fuzzy matching, and column-level lineage — done (2026-09-20).** A mid-iteration scope addition, folded in immediately rather than deferred: "better documentation, fuzzy matching, a task parameter for documentation ... documentation versioning. sql parser to implement column level lineage. dont re-invent the wheel use metadata and any existing package that can do this."
- **[DEVIATION, reverses a Non-goal] `sqlglot` is now a hard dependency**, used only by the new `column_lineage.py`. CLAUDE.md's Non-goals ruled out a SQL parser, and E2-07/E2-25 deliberately settled for lint-grade string checks because of it. The instruction was explicit about the fallback — "if it is easy implement our own, else go with hard dependency, our product should ship with it" — and writing our own is not easy: resolving which upstream column feeds a target means handling aliasing, CTEs, subqueries, joins, set operations, star expansion and per-dialect quoting. sqlglot is tens of thousands of lines across twenty-odd dialects and has **zero dependencies of its own**, so this adds one package and nothing transitive. `validate`'s read-only lint stays a string check — that one genuinely is a lint, not a parse.
- **Column-level lineage, computed *and* cached** (per explicit decision, "Both"). `lineage --column schema.table.column` reports which upstream column feeds a target column and what expression was applied, in both directions. **[CHOICE]** the cache key is an MD5 of the `SOURCE_SQL` it was derived from, not a timestamp: a cached row is reused only when it came from byte-for-byte the SQL in `CFG_TASK_PARAMETERS` right now, so editing a query invalidates it with nothing to remember — the same reasoning as `HASH_KEY` for SCD change detection. **[ADDITION, post-signoff schema change]** `AUD_COLUMN_LINEAGE`, plus `sql/migrations/0002_column_lineage_and_docs.sql`.
  - **Resolves through CTEs, to a fixpoint**, because lineage that stops at a CTE name is much less useful and CTEs are everywhere in ETL SQL. **[CHOICE]** a CTE reading *two* tables is left unresolved rather than attributed to one of them — same principle as a multi-column expression getting no single source. Verified end to end: `cust_name` traced through a CTE *and* a join back to `raw.customers.name`, with `UPPER(...)` recorded as its transformation.
  - **[CHOICE]** a parse failure is reported, never raised: one unparseable task should cost that task's column lineage, not the whole `lineage` command or a whole documentation build.
- **`DOCUMENTATION` task parameter, versioned by content hash.** **[ADDITION, post-signoff schema change]** `AUD_TASK_DOCUMENTATION`. **[CHOICE]** the version is derived from the text rather than being a parameter an author sets, per the reasoning that a hand-set version drifts out of sync the moment someone edits one and not the other — and telling current documentation from stale is the entire point of having one. Whitespace-insensitive, so re-indenting is not a new version. Recorded on demand (`docs-version`, and `generate-docs`), not on every task run: documentation changes when someone edits it, which has nothing to do with whether a pipeline executed.
- **Fuzzy matching, in both halves the instruction named.** The generated docs site now ranks with **Fuse.js**, *vendored* into `src/etl_craft/vendor/` and copied into the output rather than loaded from a CDN — the original dependency-free reasoning still holds (this site gets published to networks with no outbound access, where a CDN script tag leaves the search box silently dead), and 15 KB buys a site that works everywhere. The CLI's own "did you mean" uses `difflib` from the standard library instead: ranking three candidates for an error message is a genuinely different, much smaller problem than ranking hundreds of entries as someone types. Unknown `PIPELINE_CODE`/`TASK_CODE` now suggests near misses and prefixes, case-insensitively.
- **[DEVIATION] Interactive `configure` is gone, replaced by `setup` — the dbt route**, per explicit instruction ("remove the ineractive setup, lets go in dbt route. one single command with required files and it should itself up. so, everytime the command is ran, it either set itself up, or updates the setup with newest data"). One idempotent command: read settings from a `.env`-style file **or from the process environment** (`--from-environment`, since "the craft connector can take values from .env or the environment itself based on the options"), write/update `craft-connector.yml`, then bring the Engine DB to current — packaged schema if empty, pending migrations if not — and name the secrets the result expects. No separate first-run path, no prompts, identical on a laptop and in CI. **[CHOICE]** `init-db` and `migrate` survive as separate verbs: a DBA applying a schema, or a CI job running only a migration, should not have to rewrite `craft-connector.yml` to do it. **[CHOICE]** an unreachable Engine DB is *reported*, not raised — writing the config is useful on its own and is often the step that fixes the connection.
- **Better documentation pages**: each task now renders its `DOCUMENTATION` prose with a version badge, a collapsible column-lineage table, and collapsible parameters, with a dark-mode-aware stylesheet. Documentation text is in the search index, so searching prose finds the task.
- **489 tests (up from 469), 98% coverage, `make check` and the wheel smoke test both clean.** Two real bugs found while building, both by tests rather than review: `tests/conftest.py`'s `committed_pipeline` teardown didn't know about the two new tables, so its FK-ordered delete failed and the *next* run collided on `PIPELINE_CODE` — the same class of gap as the earlier cloning-fixture fix; and `setup`/`doctor` never disposed the engines they opened, so a short-lived command held pooled connections and `DROP DATABASE` failed with "other sessions using the database". Both fixed at the source.

**Iteration 2, phase 7 — operability and cleanups — done (2026-09-20). Iteration 2 is now complete.** Closes `E2-17` through `E2-24` and `E2-34` through `E2-40`, plus `E2-25`'s remainder.
- **E2-17/E2-19, nothing had a timeout and nothing had a cap.** New `[Execution]` `Task_timeout_seconds` (default 6 hours) and `Max_parallel_tasks` (default 8), with a per-task `TASK_TIMEOUT_SECONDS` parameter overriding the first (`0` disables). **[CHOICE]** real defaults rather than `None`: a limit nobody sets is a limit nobody benefits from, and six hours is generous enough that anything hitting it is genuinely wedged. Applied to all four places that had none — `runner.py`'s handler fork (terminate, then `FAILED`), `orchestrator.py`'s subprocess waits, `scripts.py`'s `subprocess.run`, and `smtplib.SMTP`. **Why this one mattered most**: a hung task left its row stuck `IN-PROGRESS`, and `IN-PROGRESS` is in `resolver.NOT_RETRYABLE` — so the task became permanently un-retryable and the pipeline could never recover without someone editing `AUD_TASK_RUN_LOG` by hand. `_run_wave` now also batches rather than spawning one process per ready task at once (a 40-task wave meant 40 processes, each building its own engines and forking a child that built another), and `business_rules.py`'s `ThreadPoolExecutor(max_workers=len(wave))` is capped the same way.
  - `task_timeout_seconds` lives in its own `limits.py` because both `handlers.py` and `runner.py` need it and `runner` imports `handlers` — the same reason `execution.py` exists.
- **E2-21, retry history was destroyed.** **[ADDITION, post-signoff schema change]** `AUD_TASK_RUN_LOG.ATTEMPT_COUNT` (+ `sql/migrations/0003_task_attempt_count.sql`). The one-row-per-task-per-run rule is load-bearing and unchanged — this counts attempts *within* the row. New `runlog.begin_attempt` bumps it and resets the row's per-attempt state, and `update_task_run` no longer `COALESCE`s counts forward (a retry reporting fewer fields silently inherited the previous attempt's numbers, so one row could hold two attempts' figures with nothing saying so). **`START_DATE` is reset too**, which is the subtle half: it previously kept the *first* attempt's timestamp, so a task retried an hour later reported a duration spanning the gap — and that duration feeds `crosspipe._average_task_duration_seconds`, which drives how often a downstream pipeline polls. The poll cadence was being computed from time nothing spent running.
  - **This also closed E2-43's deferred question.** `COMPLETED_WITH_ERRORS` now keys off `ATTEMPT_COUNT > 1` as well, so "succeeded on the third try" is finally distinguishable from "succeeded" — the case that flavour was designed for but could not previously see.
- **E2-22, cloning materialized whole tables in memory.** `rows = [dict(r) for r in conn.execute(select(t)).mappings().all()]` after every pipeline run — for `AUD_TASK_RUN_LOG` after a year of daily runs, millions of rows held at once on a box that also has a pipeline to run. Streamed in batches now (`stream_results`/`yield_per`, `CLONE_BATCH_ROWS = 5000`), so memory is bounded regardless of table size.
- **E2-24, the blank-URL-plus-`creator` pattern.** Both builders now pass a real `URL.create(...)` with everything *except* the password. The original goal (never render a secret into a logged engine URL) is preserved — SQLAlchemy cannot log a password it was not given — while `engine.url` becomes truthful. It had already broken two real things, both documented in `cloning.py`.
- **E2-40, business rules didn't resume on retry.** `_find_or_create_run_log` reused the existing row but never short-circuited on an existing `SUCCESS`, so a retry re-ran every rule in every earlier wave — a task with thirty rules that failed on the last one re-ran all thirty. **[Interaction caught by an existing test, not by review]**: `--force` must bypass the new short-circuit, because a forced business-rule run means "scan all data", which is a different question from the one the previous run answered under its `PIPELINE_RUN_ID` scope.
- **E2-34/E2-35/E2-36, the cleanups.** `[Execution]` integers are type-checked rather than taken straight from `raw.get(...)` (`Max_parallel_tasks: "three"` is now an error, not something that flows into the generated YAML). `TOKEN_POOL_RECYCLE_SECONDS` is finally *wired up* for `token`/`sso` profiles, as CLAUDE.md always claimed; `cfg.fetch_task_handler`, `runlog.TERMINAL_STATUSES` and `_check_or_evolve_schema`'s unused return are gone. `--version` added; `graph` accepts `--pipeline_code` as well as `--name`, so the one inconsistency in CLAUDE.md's own CLI table stops being one without breaking anything scripted against `--name`.
- **E2-25's remainder, `validate` is no longer thin.** New `validate_task_parameters`: `SQL_ACTION` present and recognized, the parameters each action actually requires, `TARGET_OBJECT` single-valued and well-formed for `HANDLER='SQL'` (E2-32), `TASK_CODE` a safe identifier (codes are interpolated unquoted into both SQL and `bash_command`), `HANDLER='PYTHON'`'s `SCRIPT_NAME` and both mandatory `RETURN_VALUES`, `HANDLER='EMAIL_ALERT'`'s `EMAIL_TO` and body — and **any `PARAMETER_NAME` outside the known vocabulary**, since a typo like `MEREG_KEY` is silently ignored at runtime and behaves exactly like forgetting the parameter. These are conventions the execution code already depended on; checking them here means a config mistake surfaces from `validate` rather than from a task failing at 3 a.m. Two existing "validate says OK" tests turned out to be describing genuinely incomplete config and were completed.
- **500 tests (up from 489), 97% coverage, `make check` (black/ruff/pydocstyle/mypy/schema-test/pytest) and the wheel smoke test both clean.**

**Iteration 2, phase 3b — round 3's findings — done (2026-09-20).** Eight findings (`E2-53`…`E2-60`) raised against the completed iteration; all fixed, each re-verified against live Postgres and live ClickHouse before being acted on.
- **E2-57 first, and it was the fair one.** `ITERATION_2.md` claimed "every item is closed" and wrote phase 7's range as `E2-17…E2-24`, which swept in **E2-18 (logging)** and **E2-20 (task output)** — neither of which was built. `grep logging src/etl_craft` returns nothing and `cli.py` alone has 71 `print(` calls. Both are now listed as **deferred, not done**, with the reason. Deferring them was defensible; recording them as closed was not.
- **E2-54, resolved by explicit correction: "all primary keys are basically identity columns. merge keys are natural keys".** The `PRIMARY_KEY` parameter added in phase 2 named an existing *business* column — unusable on an SCD2 target, which holds several rows per merge key by design. Reproduced: declaring the natural key worked for exactly one run, then failed permanently with a unique violation, leaving the target holding only the **old** version of every changed row — the history the merge exists to record was never written. The parameter is gone; every engine-created table now gets `ROW_ID`, a generated identity primary key. That makes CLAUDE.md's single-column-PK convention satisfiable on *every* target, SCD2 included, with no exemption for `validate` to know about. `_evolve_schema` carries the values across and re-promotes the column rather than regenerating, since `AUD_BUSINESS_RULES_RESULTS` rows reference them.
- **E2-53, ClickHouse: supported, per explicit decision — so fixed *and* tested.** Five failures, three from the review and two the new tests found:
  1. no `ENGINE` clause on `CREATE TABLE … AS SELECT`; 2. `CAST(NULL AS <non-nullable>)`; 3. **`TIMESTAMP WITH TIME ZONE` is a syntax error there — a regression phase 2 introduced via E2-33**, correct for Postgres, breaking a dialect the project claims to support;
  4. **the staging table**: ClickHouse's temporary tables are session-scoped and its HTTP driver uses a new session per statement, so the stage vanished before the next statement read it — every action builds a stage, so this blocked the whole vocabulary;
  5. **`UPDATE` does not exist**: only asynchronous `ALTER TABLE … UPDATE` mutations, explicitly not row-level updates. An SCD merge built on them would report `SUCCESS` before the target had changed, which is strictly worse than failing, so `SCD1_MERGE`/`SCD2_MERGE` are now **refused up front** with a clear reason. **A real scope limit, stated rather than papered over**: the insert-only actions work and are tested on ClickHouse; the merges cannot, short of a different implementation built on `ReplacingMergeTree` and inserts.
  - **The root cause was a test shape, not carelessness**: `make_config(warehouse=True)` pointed `[Warehouse]` at the same Postgres, so the entire action vocabulary was exercised against one dialect while the code carried ClickHouse branches nothing ran. New `make_config(clickhouse_warehouse=True)` plus real SQL-action tests against the container. **[DEVIATION]** `qualify()` now takes the dialect: ClickHouse names objects `database.table`, so a three-part name is a syntax error — the profile's database wins and the CFG_ schema part is dropped there, which means two CFG_ rows differing only by schema **collide** on a two-level engine. Documented, not solvable without dialect-aware `validate`.
- **E2-55**: the "already FAILED" advice is mode-aware. It recommended `--force` unconditionally — which `Mode=orchestrator` refuses outright — so in the mode a real deployment runs in, the most common operational action had no route *and* the error pointed at a rejected flag. Under orchestrator it now names `--init-only` and says plainly that gives a new `pipeline_run_id`.
- **E2-56, and wider than reported**: `generate-docs` wrote through a connection the CLI never commits. The review named the lineage cache; probing showed the **documentation-version writes were discarded too** — both tables held 0 rows afterwards. Now genuinely read-only (`cache=False`/`record=False`), which also exposed a design question the review did not reach: a page was rendering the last *recorded* documentation text, so an edit stayed invisible until someone ran `docs-version`. Pages now show the current parameter text with the recorded version beside it, or "unversioned".
- **E2-58**: `docs_generator.py`'s docstring and CLAUDE.md's CLI-surface paragraph both still said the search was "dependency-free ... not a vendored copy of Fuse.js", thirty lines above the code that vendors Fuse. Both rewritten; the no-CDN reasoning is kept, since it is exactly *why* Fuse is vendored rather than linked.
- **E2-59/E2-60, two new `validate` checks.** A `HAS_DATA` edge whose upstream is `BUSINESS_RULES` or `EMAIL_ALERT` can never be satisfied — and E2-01's `unsatisfiable()` made that *worse*, silently recording the downstream task `SKIPPED` and finalizing `SUCCESS` where it previously showed up as stuck. And each `EMAIL_ALERT` must depend on every non-alert leaf: E2-43 made it a pipeline-level completion alert, but nothing made it run last, so gating it on one task meant it sent the amber "something went wrong" email for a run that went on to finish cleanly.
- **470 tests, 97% coverage, `make check` / `make db-schema-test` / the wheel smoke test all clean.** (500 → 470 is net: 9 obsolete `PRIMARY_KEY`/`configure` tests removed, 13 added.)

**ClickHouse dropped; DuckDB is the second supported warehouse — done (2026-09-21), per explicit decision.** "okay, then clickhouse goes away. DuckDB is our warehouse now. duckdb and postgresql are the ones we want to majorly support. because clickhouse is not really great on ansi". This **supersedes E2-53's entire entry above** — every ClickHouse-specific branch that entry describes is deleted rather than kept for a dialect nobody runs.
- **The reason is the right one, and it is the same one E2-53 already documented without acting on.** That entry had to record a real scope limit — `SCD1_MERGE`/`SCD2_MERGE` *refused up front* on ClickHouse, because it has no row-level `UPDATE` at all — plus session-scoped temp tables that broke every action's staging step, a mandatory `ENGINE` clause on `CREATE TABLE`, a two-part `database.table` namespace that made two `CFG_` rows differing only by schema **collide**, and an `MD5()` returning raw bytes where the schema wants 32 hex characters. Half the action vocabulary did not work there and could not be made to. Carrying five dialect branches for that is worse than not supporting it.
- **[DEVIATION, and a deliberate, scoped reversal of a Non-goal]** `duckdb-engine` is now a **hard dependency**, which "Non-goals" rules out for third-party dialects in general. The reason it does not reopen that rule: DuckDB is *embedded*. There is no server to stand up, so shipping the dialect is what makes `etl-craft setup` reach a working warehouse with no extra step — the same argument that makes `psycopg` a hard dependency for the Engine DB. Every *other* dialect stays exactly as before: an optional extra a team installs itself, discovered through SQLAlchemy's entry points, never imported here. The `clickhouse` extra, its self-referential `etl-craft[clickhouse]` dev entry and `[tool.uv.sources]` are all gone.
- **[ADDITION] `auth_mode: none` for the Data DB.** A file has no user to be and no password to present, so requiring a secret would mean inventing one that authenticates nothing. `[Email]` already used `none` for the same reason, so this widens an existing vocabulary rather than adding one. `config.AUTH_MODES_WITHOUT_USER` is what makes `user` optional for it; `doctor` skips the secret check for it; `build_data_engine` resolves no secret at all.
- **[ADDITION] `translate_jdbc_url` grew a DuckDB branch — the case its own comment predicted.** That comment already flagged "a vendor whose JDBC URL shape isn't `scheme://host[:port]/database` at all ... would need its own parsing added when that vendor is actually chosen — not guessed at now." It has been: `jdbc:duckdb:<path>`, or bare `jdbc:duckdb:` for in-memory. The catalog name `qualify()`'s three-part form needs is DuckDB's own — the file stem (`/data/warehouse.duckdb` → `warehouse`), or `memory`.
- **[DEVIATION] `_dbapi_connect` now calls `dialect.connect(...)`, not `dbapi.connect(...)` — a real bug DuckDB exposed immediately.** The original went to the DBAPI directly on the reasoning that this is what `DefaultDialect.connect()` does internally. True, but it bypasses any dialect that *overrides* `connect()` — which is how a dialect does its own setup. `duckdb_engine`'s `DBAPI` class has no `connect` attribute at all, so the first pool checkout was an `AttributeError`. Going through the dialect is both more correct and strictly more general, and still imports no driver.
- **Reversions, not just deletions.** `qualify()` is back to two arguments and always emits three-part `database.schema.table` (the dialect parameter existed only for ClickHouse's two-level namespace, so the documented schema-collision hazard is gone with it). `_hash_expression` is plain `MD5(CAST(... AS VARCHAR))` again with no dialect parameter. `_build_stage` is unconditionally `CREATE TEMPORARY TABLE`. `cloning.py` lost `_create_clickhouse_table`/`_clickhouse_schema`/`_CLICKHOUSE_TYPE_NAMES` and the `warehouse_database` parameter they needed.
- **One genuine DuckDB-specific branch remains, in `_add_surrogate_key`**: DuckDB rejects adding an identity column to an existing table, so `ROW_ID` is a `BIGINT DEFAULT nextval(<sequence>)` there against `GENERATED ALWAYS AS IDENTITY` elsewhere. Keyed off `conn.dialect.name`, never an import — the same pattern the deleted branches used, kept because this one is a two-line difference in a single function rather than a vocabulary that does not work.
- **[ADDITION] A real hazard found by probing, now documented in `runner.py`: DuckDB writes from a forked child are *silently lost* if the parent had the warehouse file open before the fork.** The child commits, reports `SUCCESS`, and the rows are not there. Verified directly, not reasoned about. The engine is safe **by construction** — `run_task` never opens the Data DB in the parent; `handlers.dispatch` builds it inside the child, after the fork — which is exactly the property the crash-detection comment's reason (1) already claimed for general hygiene and which now has teeth. Anything added to `runner.py` that opens the warehouse before the fork would break every DuckDB deployment without failing a test loudly, so the constraint is written down where that change would be made. The DuckDB integration test had to hand the file back (dispose its seed engine) before each `run_task` for the same reason.
- **[Bug found by covering a branch nothing ran, and wider than the branch]** Writing a test for the one new DuckDB-specific line (`_restore_surrogate_key`'s sequence repositioning, uncovered after the migration) surfaced that **`duckdb_engine` does not reflect primary keys at all** — `Inspector.get_pk_constraint()` returns an empty `constrained_columns` list for a table DuckDB is genuinely enforcing a PK on (verified directly: the duplicate insert fails at commit). `validate.validate_business_rule_keys` is built on exactly that call, so it reported **every** target on the new primary warehouse as having no primary key — failing precisely where CLAUDE.md's single-column-PK convention is being honoured. Fixed with `validate._primary_key_columns`, which falls back to DuckDB's own `duckdb_constraints()` catalog view, keyed off the dialect *name* and only when reflection came back empty, so a future `duckdb_engine` that does reflect keys silently takes over. Reproduced as a failing test against pre-fix code first, with the negative case (a DuckDB table genuinely without a PK) landing alongside it so the fix could not be a blanket skip. **This is E2-53's own lesson repeating**: the bug was not in the dialect switch, it was in a code path that had only ever run against one dialect.
- **Infrastructure**: the `clickhouse` service and its volume are gone from `docker-compose.yml`, and the matching `services:`/env entry from CI. DuckDB needs neither — `tests/conftest.py`'s `duckdb_engine` fixture is a `tmp_path` file, function-scoped so nothing leaks between tests, and unlike the container fixtures it can never skip.
- **The test that mattered most is the one that made E2-53 necessary**, and it is kept in DuckDB form: `[Warehouse]` pointed at a genuinely different dialect, running the real action vocabulary. Unlike ClickHouse, DuckDB runs **all** of it — `test_sql_actions_run_end_to_end_against_real_duckdb` covers `CREATE_TABLE` and a two-run `SCD1_MERGE` including the correlated `UPDATE` leg ClickHouse could not do at all — plus `test_hash_expression_yields_the_same_32_hex_chars_on_both_warehouses`, which now asserts the *opposite* of its ClickHouse ancestor: one expression, identical output, no branch.

**Round 4 — warehouse architecture settled, and DuckDB's process model fixed — done (2026-09-21).** A fourth review round (`E2-61`…`E2-64` in `ITERATION_2.md`) raised four findings, all about DuckDB's *process* model rather than its SQL. Worth recording the headline positive first, because it shaped everything else: the reviewer ran all ten SQL shapes `sql_actions.py` emits against a real DuckDB file — staging temp table, CTAS with audit casts, `ADD PRIMARY KEY`, `TRUNCATE`, `MD5`, the correlated `UPDATE`, `DELETE … WHERE EXISTS`, the drop-and-rename rebuild, `information_schema.columns`, `ROW_NUMBER() OVER (…)` — and **all ten work unmodified**. The ANSI discipline this module has been held to since iteration 1 is what made a warehouse swap cost one dialect branch instead of a rewrite.
- **Architecture, settled by explicit decision: `engine: postgres` / `warehouse: postgres, (duckdb + iceberg)`.** Postgres is a first-class warehouse and the one with no caveats; the DuckDB path is intended to become DuckDB-as-compute over Iceberg-as-storage, with the plain DuckDB *file* being what ships today. **Iceberg is added to scope, not built** — `ITERATION_2.md`'s closing section records the analysis so it is not re-derived, including the two things that decide the design: DuckDB's Iceberg *write* support must be verified against a real catalog first (reads are near-universal, writes are not, and this engine only writes), and **Iceberg has no primary keys or sequences**, which collides directly with E2-54's `ROW_ID` identity key and `validate`'s PK introspection.
- **E2-61, the one that mattered: DuckDB admits exactly one writing OS process, and the engine's execution model is one subprocess per task.** `runner.py` already documented the smaller *fork* half of this. The larger half was unaddressed — a second process is refused outright (`IO Error: Could not set lock on file`), and so is a **read-only** connection while a writer holds it (verified; stricter than ordinary single-writer/multi-reader). With `Max_parallel_tasks` defaulting to 8, any wave holding two `SQL`/`BUSINESS_RULES` tasks failed all but one — the default behaviour on an embedded warehouse, not an edge case — and the same applied under Airflow, whose parallel tasks are separate processes too.
  - **[CHOICE, per explicit decision] The subprocess model stays; access queues instead.** In-process threading would have given genuine DuckDB concurrency but contradicts "local runs mirror what an orchestrator does" and does nothing for Airflow, where the processes are Airflow's. New `warehouse.data_db()` is now the single way the engine reaches the Data DB: for Postgres it is exactly the old `build_data_engine(...)`/`dispose()` pairing and costs nothing (waves stay fully parallel), and for a single-writer warehouse it additionally holds a Postgres advisory lock **in the Engine DB** for the duration. The Engine DB is the right place for it because CLAUDE.md makes a valid Engine DB connection the one hard dependency of every action, so it is reachable from every process that could contend — including Airflow workers on other machines. `migrate.py` already coordinates concurrent runs the same way.
  - **[CHOICE] Queueing, not retrying.** An advisory lock queues fairly; a retry loop on DuckDB's own `IOException` would spin and can starve a waiter. Bounded — by the task's own timeout for a task, by 30s for a read-only verb — and the timeout message names the cause instead of surfacing the raw lock error. **Verified, not assumed**: Postgres's `lock_timeout` does apply to `pg_advisory_xact_lock`.
  - **When Iceberg lands this largely dissolves** — no shared file means `is_single_writer` returns False and no lock is taken. Written to make that a one-line change.
  - `doctor` reports the constraint. The regression test is the one the review asked for: `test_run_pipeline_runs_a_parallel_wave_against_a_duckdb_warehouse` — a real two-task wave, real subprocesses, real DuckDB file — **verified to fail without the lock** (`wave_a` dies on the file lock, the pipeline reports `FAILED`). It needed a new `duckdb_craft_connector_on_disk` fixture, because every DuckDB test ran in one process and every subprocess-spawning orchestrator test pointed `[Warehouse]` at Postgres: the combination that breaks was untested, the same structural blind spot E2-53 identified, one level up.
- **E2-64, and the review had it wrong in the engine's real configuration — in the dangerous direction.** The DuckDB surrogate-key sequence was named from the *bare* table name and created unqualified, so `staging.orders` and `marts.orders` shared one. Probed on a single connection that looks self-limiting: DuckDB refuses the `DROP SEQUENCE` with a dependency error, which is what the review saw ("fails loudly rather than corrupting anything"). But the engine runs every task in its own process with its own connection, and **there the DROP succeeds silently** — both tables end up on one sequence that has just been reset to 1. A later insert into a table the run never touched then dies with `Duplicate key "ROW_ID: 2" violates primary key constraint`, leaving it un-writable until someone repairs the sequence by hand. Sequences are now schema-qualified and created in the target's own schema. **Method note**: the first version of the regression test asserted only that both tables built, and *passed against the buggy code*; it asserts the aftermath now. A test that passes against the bug is worse than no test.
- **E2-62**: the derived DuckDB catalog name (the file stem, interpolated unquoted by `qualify()` into `database.schema.table`) is validated where it is derived. `my-warehouse.duckdb` — not an exotic filename — produced `my-warehouse.public.t`, a parser error that never mentioned the file. **[CHOICE]** reject rather than quote: quoting would make the catalog case-sensitive and diverge from how the Postgres path builds the same name. `validate`'s own identifier check cannot catch this — it checks `CFG_` values, and this one comes from `craft-connector.yml`.
- **E2-63**: `jdbc:duckdb:` was documented as in-memory but produced `duckdb:///memory`, which DuckDB reads as **a file named `memory` in the current working directory** — two task subprocesses left a 274 KB `memory` file in the repo root and the second saw the first's data. Now genuinely `:memory:`, and `doctor` refuses it: every task is its own process, so an in-memory warehouse is empty at the start of each one and nothing errors — targets simply are not there.
- **Found while doing the above, and the same class as E2-13: `setup` never wrote a `[Warehouse]` section at all.** The one command meant to take a team from nothing to a working deployment produced a config in which every `SQL`/`BUSINESS_RULES` task failed with "no [Warehouse] section configured". `configure_from_env` now writes it from `ETL_CRAFT_WAREHOUSE_JDBC_URL`/`_PROFILE`/`_USER`/`_AUTH_MODE`, merged the way `[Postgres]` is, and optional so an Engine-DB-only setup is unchanged.
- **484 tests (up from 473), 96% coverage, `make check` / `make db-schema-test` / the wheel smoke test all clean.**




**Warehouse architecture rebuilt around Iceberg — done (2026-09-22), per explicit decision.** "engine: always postgres / warehouse: postgres (pg analytics), databricks with iceberg unity catalog, snowflake with iceberg hybrid tables, maybe be trino with iceberg or any sql tool over plain iceberg", plus the binding constraint that followed: **"if the warehouse is not postgres, every table we create or operate should be iceberg compatible."** This supersedes the 2026-09-21 "Postgres and DuckDB" framing — DuckDB is still supported and useful for local development, but is no longer a headline warehouse.
- **The existing design absorbed this almost entirely, which is the point worth recording.** All three new dialects (`databricks`, `snowflake`, `trino`) register under their plain vendor name and resolve through `warehouse.py`'s existing `_dbapi_connect` with **no vendor-specific connection code** — verified directly via `entry_points(group="sqlalchemy.dialects")` and each dialect's own `create_connect_args`, not assumed. They are `[project.optional-dependencies]` extras (`databricks`/`snowflake`/`trino`), never imported by engine code, exactly as Non-goals requires. The last item in the instruction — "any sql tool over plain iceberg" — therefore needs no code at all, only a dialect on the path.
- **[ADDITION] `JDBC_PARSERS`, a per-vendor JDBC-URL registry.** `translate_jdbc_url`'s own comment had always said a vendor whose URL shape isn't `scheme://host[:port]/database[?query]` "would need its own parsing added when that vendor is actually chosen". Three now are: Databricks' parameters are **semicolon-separated** after the path, and Snowflake's path is empty with the database in the **query string** — neither is readable by the generic parser. Trino's `catalog/schema` path already fits it, which is what the generic fallback is for. Databricks' transport/auth JDBC parameters (`AuthMech`, `UID`, `PWD`) are deliberately **dropped rather than forwarded** — `PWD` would put the token in the URL, which this module goes out of its way to avoid.
- **[ADDITION] `parts["catalog"]`.** Snowflake and Trino carry database *and* schema in one URL segment because that is what their dialects split themselves, but `qualify()`'s three-part `catalog.schema.table` needs the catalog half alone (the schema comes from `TARGET_OBJECT`). `active_database` now reads `catalog` where the two differ, and raises a clear error when the profile names no catalog at all.
- **[DEVIATION] `auth_mode='token'` is implemented for the Data DB**, where it previously raised `NotImplementedError` on the grounds that "the credential-minting provider is team-specific". That reasoning still holds for a token a provider *mints* per connection (OAuth2 client-credentials, STS AssumeRole) — still unimplemented — but it conflated those with the far commoner case: a long-lived token the team already has, presented like a password. Databricks personal access tokens are exactly that, and the engine cannot reach Databricks without it.
- **[ADDITION] Every table created on a non-Postgres warehouse is an Iceberg table.** `create_table_as` — the named helper the ClickHouse work deliberately left behind for exactly this — now adds the clause each dialect needs (`USING ICEBERG` on Databricks; nothing on Trino, where a table in an Iceberg catalog is Iceberg by construction and a clause would be a syntax error). Getting this wrong is **silent**: the table is created, the pipeline reports `SUCCESS`, and it is simply not readable by anything else in the lakehouse — which is the entire reason for choosing Iceberg.
  - **[CHOICE] Snowflake table creation is refused, not approximated.** Its Iceberg tables need a different statement (`CREATE ICEBERG TABLE`) plus an `EXTERNAL_VOLUME` and `BASE_LOCATION` that have no home in `CFG_` metadata yet. Refusing with a clear reason beats silently creating an ordinary Snowflake table that looks fine and is not Iceberg — the same reasoning that refused SCD merges on ClickHouse: producing the wrong thing successfully is worse than failing.
  - **Factual correction, made once and built accordingly**: the instruction said "snowflake with iceberg hybrid tables", but **Snowflake hybrid tables and Iceberg tables are different features** — the first is a row-store with enforced primary keys, the second external Iceberg format — and a table cannot be both. Iceberg compatibility is the binding requirement, so Iceberg tables are the target.
- **[ADDITION] `ROW_ID` has a third strategy, because Iceberg has no constraint concept at all** — no primary keys, no identity columns, no sequences, so neither the Postgres (`GENERATED ALWAYS AS IDENTITY`) nor the DuckDB (sequence default) route exists. It is **computed**: the largest value already present plus a row number over the rows being added, read with its own query *before* the mutating statement (this module's own rule for counts, and it avoids referencing a table inside the statement writing to it). Every `INSERT` leg supplies it explicitly, where the other two warehouses let the column fill itself. **[CHOICE] What is given up, stated rather than implied**: uniqueness is not database-enforced, because Iceberg cannot enforce it. `validate` reports that once for an Iceberg warehouse rather than failing every business rule — the same false-failure class as DuckDB's missing PK reflection (E2-61's round).
- **Verification, and its real limit — stated plainly because this is exactly E2-53's lesson.** The URL parsing, dialect resolution, auth-token URL construction and Iceberg clause selection are all genuinely tested. The **execution** path against Databricks, Snowflake and Trino is **not** verified: there is no such endpoint reachable from the test suite. What *is* verified is that the Iceberg code path produces well-formed SQL with correct `ROW_ID` arithmetic across runs, by forcing it on against real Postgres (`test_sql_actions_assign_row_ids_on_an_iceberg_backed_warehouse` removes Postgres from `NATIVE_STORAGE_DIALECTS`). That covers most of the risk — syntax and logic — but **not** whether those three engines accept the statements. Treat the Databricks/Snowflake/Trino execution paths as unproven until someone runs them against a real endpoint.
- **500 tests (up from 486), 97% coverage, `make check` clean.**


**A real local Iceberg warehouse, and the Iceberg path verified against it — done (2026-09-22).** Closes the gap the previous entry stated plainly: the Iceberg execution path had no reachable engine, so it was proven only to emit well-formed SQL. `docker-compose.yml` now runs **MinIO + an Iceberg REST catalog + Trino** alongside Postgres, and `test_sql_actions_run_end_to_end_against_real_trino_iceberg` drives a two-run `SCD1_MERGE` through `run_task` against it. CI brings the same stack up via Compose (not `services:`, which cannot express the bucket-init container or Trino's catalog-file mount).
- **Asked and answered: MinIO cannot serve Snowflake.** Snowflake's external volumes are read by Snowflake's own cloud service, so the bucket must be reachable from the public internet with real S3/Azure/GCS credentials — a local container is not. Trino is the engine that makes a *local* Iceberg warehouse possible, and it exercises the same code path, which is why the stack is built around it.
- **Four genuine bugs, every one found by running rather than reading** — the same lesson as E2-53, and the reason this stack was worth building:
  - **Trino has no temporary tables at all** (`CREATE TEMPORARY TABLE` is a syntax error). Every action builds a stage, so this blocked the entire vocabulary. The stage is now an ordinary table on such dialects — safe because it was already uniquely named per task run and explicitly dropped by `_drop_stage` on every path; `TEMPORARY` only ever added automatic cleanup on top of cleanup this module already does.
  - **Trino's `md5()` takes and returns varbinary**, so `MD5(<varchar>)` is a type error and the result needs hex-encoding to be the 32 characters `HASH_KEY VARCHAR(32)` expects. `_hash_expression`'s own docstring had warned for months that "the exact function/return shape varies ... flagged, not solved further" — this is that bill coming due, now solved for a dialect that actually exists.
  - **Trino's `UPDATE` rejects a table alias** (`UPDATE t x SET` is a syntax error); the target's columns are qualified by the table's own name instead. `_update_target()` returns the right pair per dialect, applied to all three correlated updates (SCD1, SCD2's deactivate, soft `DELETE_ROWS`). Only the UPDATE statements need it — a plain SELECT aliases freely everywhere, so the count queries are untouched.
  - **[DEVIATION] `_none_creator` dropped host and port entirely.** Written for DuckDB, where the path *is* the database and there is no server — and silently wrong for anything else. A Trino cluster with authentication disabled (an ordinary local/dev setup, and a perfectly valid `auth_mode: none`) resolved the literal hostname `"none"`. A server profile now keeps host/port/user/query; only a pathless one takes the file form.
- **What the real run proves that the forced-path Postgres test could not**: the computed `ROW_ID` genuinely survives a merge — an updated row keeps its own value while a newly inserted one takes the next — and `SHOW CREATE TABLE` confirms the target really is an Iceberg table, not the engine's default format.
- **Still unverified, and still stated rather than implied**: Databricks and Snowflake. Both need a cloud account, and Snowflake additionally needs cloud object storage for its external volume, which a trial does not include. Trino's success raises confidence for them (it is the same code path) but is not evidence about their particular SQL.
- **510 tests, 97% coverage, `make check` clean.**


**Non-Iceberg table formats supported on Snowflake and Databricks — done (2026-09-22), per explicit decision** ("lets support non iceberg as well, on the snowflake and databricks setups"). A deliberate widening of the 2026-09-22 rule that *every* table on a non-Postgres warehouse must be Iceberg.
- **[CHOICE] A declaration, not an inference, and `iceberg` stays the default.** `CFG_TASK_PARAMETERS.TABLE_FORMAT` (`iceberg`/`native`) overrides `[Warehouse].Table_format`, which defaults to `iceberg` — the same two-tier shape `generate-yml`'s settings already use. Making `native` the default would have silently changed the format of every table an existing pipeline creates, and inferring the format from context would reintroduce exactly the guessing the Snowflake refusal exists to prevent. An unrecognized value is rejected rather than assumed.
- **Databricks names Delta explicitly** (`USING DELTA`) rather than omitting the clause and taking the workspace default. The point of declaring a format is that it is declared.
- **Snowflake's `native` path is an ordinary `CREATE TABLE`** and needs no `EXTERNAL_VOLUME`. The Iceberg refusal still fires for `TABLE_FORMAT=iceberg` without storage — that guard was never about Iceberg being mandatory, it was about never silently producing something the lakehouse cannot read when Iceberg is what was asked for.
- **Three places that assumed Iceberg now follow the declaration**: `cloning.py`'s mirrors, so a native deployment is not refused for missing storage it does not need; and `warehouse.verify_iceberg_catalog` (E2-69), which no longer fails `doctor` for a team legitimately running native tables on a non-Iceberg catalog — that check inventing a requirement would be its own bug.
- **`_is_iceberg_backed` deliberately unchanged**, and worth knowing why: it governs the `ROW_ID` strategy, which is about whether the *dialect* has identity columns, not about the table's storage format. A computed `ROW_ID` is correct on a Delta or standard Snowflake table too. Snowflake native tables could support a real `AUTOINCREMENT` primary key, which would be a genuine improvement and is a separate change, not a side effect of this one.
- **526 tests (up from 516), 96% coverage, `make check` clean.** Includes a real end-to-end `TABLE_FORMAT=native` run against the live Trino/Iceberg stack — where the catalog decides the format either way, so what it proves is that asking for `native` does not break the action path. The Databricks/Snowflake clauses are unit-tested, neither being reachable from here.


**Business-rule keys: `ROW_ID` was the wrong thing to key on, and `validate` was forcing it — fixed (2026-09-22).** Found by a direct question about the E2-71 documentation ("then how do you deactivate the business rule result correctly for an updated rule?"), which turned out to expose a defect the docs change had made worse rather than a gap in the docs.
- **The mechanism**: `AUD_BUSINESS_RULES_RESULTS` records a flagged row by its `BUSINESS_RULE_KEY_COLUMN` value, and `business_rules.py` deactivates a flag only for keys its "no longer violates" query *returns* — which are keys still present in the target. A key that did not survive the run is never deactivated.
- **`ROW_ID` is regenerated by every full-replace action**, and the two warehouses break differently. Reproduced both: on Postgres the identity keeps climbing, so the flagged key no longer exists and the flag stays active forever pointing at nothing; on Iceberg `ROW_ID` is `max-present + row_number`, so after the truncate it **restarts at 1** — a flag recorded against `ROW_ID` 1 for customer 100 came back pointing at customer 999. A data-quality flag silently re-attributed to a different entity is worse than a stale one.
- **The engine was forcing the broken choice.** `validate_business_rule_keys` required `BUSINESS_RULE_KEY_COLUMN` to *equal* the target's primary key, and E2-54 had made that `ROW_ID` — so naming the stable business key, the correct answer, failed validation. That equality was never CLAUDE.md's rule: the convention is that a target *has* a single-column primary key, "which is why `BUSINESS_RULE_KEY_COLUMN` can safely stay a single column rather than a list". Two different requirements, conflated in one check.
- **Fixed three ways**: the equality requirement is gone (replaced by "the key column must exist on the target", so a typo is still caught); new `validate_business_rule_key_stability` flags a rule keyed on `ROW_ID` whose target is written by `CREATE_TABLE`/`OVERWRITE_TABLE`, and stays quiet for `SCD1_MERGE`, where rows persist and `ROW_ID` is legitimate; and `docs/parameters.md` now says plainly not to key on `ROW_ID` and why.
- **[Correction] The E2-71 documentation was wrong**, not merely incomplete. It said `ROW_ID` "is the column `BUSINESS_RULE_KEY_COLUMN` should name" — written while fixing a *different* documentation-contradicts-code bug, and it walked readers into this one. Worth noting as a pattern: fixing stale docs by reading the code tells you what the code does, not whether what it does is right.
- **533 tests, `make check` clean.**

**Round 7 — E2-74…E2-89, all sixteen fixed (2026-09-22).** A seventh review round; `ITERATION_2.md`'s own "Round 7" section has the per-finding detail. 533 → **546 tests, 96% coverage, `make check` and the wheel smoke test both clean**. Every reproduced finding's regression test was verified to **fail against the pre-fix code** before being kept.
- **E2-74, the one that lost data: `SCD2_MERGE` did not converge on retry.** `changed_keys` required an `ACTIVE_FLAG = 'Y'` row while the new-row `NOT EXISTS` looked at *every* row regardless of flag, so a key holding rows but **no active row** fell through both legs forever — the merge reported `SUCCESS` and never wrote the current version. That is exactly the half-written state a committed deactivate plus a failed INSERT leaves behind, which on Trino/Iceberg is an ordinary failure mode, not a hypothetical: `sql_actions.py`'s own docstring promises idempotency rather than atomicity there, and for this action the promise was false. **[CHOICE]** fixed by scoping the new-row leg to active rows, which is what "new" means for SCD2 — *no current version present*. Safe on the ordinary path because that leg runs *after* the deactivate and the changed-key insert.
- **E2-75, the E2-04 guard was unreachable on Iceberg.** `_dedupe_stage` was the third and last site still emitting `CREATE TEMPORARY TABLE` directly, and Trino has none — so the only thing standing between duplicate source rows and a permanently corrupted SCD target failed with a syntax error naming `TEMPORARY`. Routed through `_create_scratch_table`, and the `{stage}_dedup` name given to `_sweep_stage` through a new shared `_dedupe_table_name` so the two cannot drift again. **Why two rounds of Trino work missed it**: the end-to-end vocabulary test used duplicate-free sources, so `_dedupe_stage` always returned early at its own `if not duplicates` guard — a test that exercises a function but not its branch.
- **E2-76/E2-79/E2-83, the install path — what the code assumed about its environment.** `encoding="utf-8"` on every read and write in `src/` (schema.sql has 74 non-ASCII lines, so `init-db` and `setup` died on any non-UTF-8 locale), held by **ruff `PLW1514`**; `exec_driver_sql` instead of `execute(text(...))` in `migrate`/`init_db`, because `text()` re-scanned carefully-split SQL for `:name` bind parameters with none of the splitter's quote awareness; and new `migrate.mark_packaged_migrations_applied`, so a fresh `setup` records the engine's migrations rather than executing all of them on top of the schema that already contains them.
  - **[CHOICE], and the part the review did not draw:** the marking is scoped to the migrations shipped *inside the package*, never to whatever `resolve_migrations_dir` happens to pick. `schema.sql` is the authoritative definition of the *engine's* schema, so the engine's own migrations are by definition already in it — but a team's `./sql/migrations/` holds changes `schema.sql` knows nothing about, and recording those unexecuted would silently skip a team's migration. That is a worse bug than the one being fixed, so it has its own test.
- **E2-77, two `EMAIL_ALERT` tasks always disagreed and the first was wrong.** `run_flavour` excluded only itself, which is right for one alert and breaks for two — and two is supported (`validate` treats alerts as a set; `EMAIL_ON_STATUS` exists so one can go to ops on `FAILED` and another to stakeholders on `SUCCESS`). Both depend on the same leaves, so both land in the same wave, and whichever ran first saw the other as `PENDING` — the neutral middle — and sent a false amber email about a perfectly clean run. `cfg.TaskStatusEntry` carries `handler` now, the same one-field extension `task_id` needed when this exclusion was first built.
- **E2-78, `--config` was dropped for every spawned task.** `ConnectorConfig` carries `config_path`; `_run_wave` passes it on — **before** the verb, since `--config` is top-level, which the test caught rather than review. `$ETL_CRAFT_CONFIG` worked all along because `Popen` inherits the environment, so only the explicit flag was lost, which is what made it look like it worked right up until someone used the flag.
- **E2-80/E2-81, one pass over `handlers.dispatch`.** The Engine DB *write* transaction is gone from around both long-running handlers — an open Postgres transaction pins the `xmin` horizon for the whole database, so eight parallel tasks holding one for hours stopped autovacuum reclaiming anything anywhere. `scripts.execute` takes the `Engine` and opens its one connection *after* `subprocess.run` returns; `sql_actions.execute` takes the `Engine` and its two sibling lookups open short connections. **[ADDITION]** `warehouse.single_writer_lock`, split out of `open_warehouse` so `HANDLER=PYTHON` can take E2-61's queueing **without** opening a warehouse engine — an ingestion task legitimately runs with no `[Warehouse]` section, and reusing `open_warehouse` would have made one mandatory. It is the ingestion handler, so writing to the warehouse is its whole purpose, and it was the one handler bypassing the lock.
- **E2-82, resolved by taking *both* shapes the review offered**, because neither alone is enough. `ready()` no longer lets an optimistic cross-pipeline count *alone* start an `ANY`/`N` task whose same-pipeline upstreams have not run; and `run_task` writes nothing (exit 0, E2-47's shape) rather than a terminal `SKIPPED` when a cross-pipeline edge is all that is short while a same-pipeline upstream is still pending. Option 2 alone fails because `_run_until_settled` tracks an `attempted` set, so a task exiting 0 without writing is never re-dispatched in that `run_pipeline` call — it converts "permanently SKIPPED" into "permanently unsettled". Option 1 alone fails because a manual `run --task_code` never goes through the wave pre-filter. **The boundary that keeps it from becoming its own bug**: the task is held only while an upstream is *not yet terminal*, deliberately **not** until it is `SETTLED` — a permanently `FAILED` upstream has had its say and the cross edge may still satisfy `ANY`, which is precisely what `ANY` is for.
- **E2-84…E2-89, hardening.** `PIPELINE_CODE` checked by `_SAFE_IDENTIFIER` where `TASK_CODE` already was (the `generate-docs` `f"{pipeline_code}.html"` case is a plain bug for an innocent code); identifier checks on `MERGE_KEY`/`MERGE_COMPARE_COLUMNS` and a conservative shape check on `MERGE_DEDUPE_ORDER`, which is deliberately a SQL fragment; `.env` values losing one *matching pair* of wrapping quotes rather than every leading and trailing quote character (a secret ending in `"` was silently truncated while `doctor` reported it as found, because it was), with the supported subset now stated in `docs/configuration.md`; the lineage cache key hashing `TARGET_OBJECT` and the parse dialect alongside `SOURCE_SQL`; and one wall-clock deadline per wave batch rather than a fresh clock per `wait()`, which had made the real bound `N x (timeout + 120)` — over two days at the six-hour default.
- **E2-88, the wheel smoke test now drives `setup`.** It wrote `craft-connector.yml` with a heredoc, which is exactly the step `setup` exists to replace, and `setup` was the only verb in the CLI table the install-path test skipped — the same command that shipped without writing a `[Warehouse]` section at all. It now runs `setup --env`, asserts the result carries both `Postgres:` and `Warehouse:` and that a second `setup` is idempotent, then still exercises `init-db`/`migrate` standalone. Run for real; its output shows E2-83 working ("recorded 3 packaged migration(s) as already applied").
- **Still deferred, for the third round running: E2-18 (logging) and E2-20 (task output).** `grep -rn logging src/etl_craft` still returns nothing. Recording them as closed would be the dishonest option; they are now the largest remaining operability gap.

**Vocabulary: "engine" and "warehouse" — the term "Data DB" is retired (2026-09-22), per explicit instruction** ("call it warehouse, not data_db. engine and warehouse"). The two databases have always had two names each in this repo — "Engine DB / Postgres" and "Data DB / warehouse" — and the second pair had drifted into code, user-facing `doctor` output and docs alike. There is now one name for each.
- **Renamed throughout `src/`, `tests/`, `docs/` and `schema.sql`**: `build_data_engine` → `build_warehouse_engine`, `data_engine` → `warehouse_engine`, `data_conn` → `warehouse_conn`, the `data_db_tables` test fixture → `warehouse_tables`, and every prose "Data DB" → "warehouse". `doctor`'s check labels are `Warehouse`, `Warehouse secret`, `Warehouse connection`, `Warehouse concurrency`, matching the capitalized `Engine DB` labels beside them.
- **[CHOICE]** `warehouse.data_db()` became **`warehouse.open_warehouse()`**, not `warehouse.warehouse()` — it lives in a module already called `warehouse`, so the bare noun would have read as `from etl_craft.warehouse import warehouse` and shadowed the module at every call site. A verb keeps "warehouse" as the noun and the call site reads as what it does: `with open_warehouse(...) as warehouse_engine:`.
- **Dated history entries above and in `ITERATION_2.md` are deliberately left as written**, per this file's own practice of recording what happened rather than editing it away — so they still name `build_data_engine`/`data_db`. This note is the map from those names to the current ones. The living-spec sections below (Non-goals, Architecture, Business rules, craft-connector.yml, Open questions) are updated.
- No behaviour changed; 538 tests, `make check` clean.

**Round 7, the business-rule-key correction, and the config-manifest pivot (2026-09-22/23) — done, but not previously recorded here; this entry closes that gap.** Three pieces of real work landed after the "Vocabulary" entry above and before this file was next touched, and none of them got a "Where things stand" entry at the time — the exact lapse `ITERATION_3.md`'s review (D-1/D-2, below) caught. Recorded now rather than edited into the entries above, per this file's own practice of leaving dated history as written:
- **Round 7 — `E2-74`…`E2-89`, all sixteen fixed.** `ITERATION_2.md`'s own "Round 7" section has the per-finding detail; the headline is `SCD2_MERGE` not converging on retry (E2-74, real data loss on Iceberg — its new-row leg wasn't scoped to active rows) and the E2-04 dedupe guard being unreachable on Iceberg (E2-75, the last `CREATE TEMPORARY TABLE` site). 546 tests.
- **`BUSINESS_RULE_KEY_COLUMN` forced onto `ROW_ID` was itself wrong (2026-09-22).** E2-54 (below) had made every engine-created table's primary key a regenerated `ROW_ID`, and a later validate check required `BUSINESS_RULE_KEY_COLUMN` to equal it — but `ROW_ID` is regenerated by every full-replace action, so a flag recorded against it silently re-attributes to a different row after the next run. `validate` no longer requires the two to match (only that the named column exists on the target); a new check flags a rule keyed on `ROW_ID` specifically for a `CREATE_TABLE`/`OVERWRITE_TABLE` target, where it's wrong, and stays quiet for `SCD1_MERGE`, where rows persist and it's legitimate.
- **The config-manifest pivot (commits `578ab74`..`8e0f96f`).** `craft-connector.yml`'s format was rewritten — see the "craft-connector.yml" section below for the current shape in full, since this is the change that section now documents. In outline: `[Execution]`/`[Source]`/`[Postgres]`/`[Warehouse]`/`[Orchestrator]` became `Orchestration:`/`Secrets:`/`Engine:`/`Warehouse:`/`Cloning:`/`Dag_defaults:`/`Email:`, with `Engine`/`Warehouse`/`Email` each carrying a `Profile` plus a `Variables` mapping (field name → the *name* of the env/`.env` variable holding the value, never the value itself) rather than literal connection values. `config.py` reads both the legacy and canonical shapes losslessly. Migration tooling was rewritten alongside it: `migrate.py` now separates packaged `ENGINE` migration history from an optional deployment `PROJECT` stream and keeps a SHA-256 checksum ledger (`SCHEMA_MIGRATIONS.SOURCE`/`CHECKSUM`, `sql/migrations/0004_migration_streams_and_checksums.sql`). New docs not previously cross-referenced from this file: `SECURITY.md`, `docs/operations.md`, `docs/release-checklist.md`, `CHANGELOG.md` — see "craft-connector.yml"'s own closing paragraph for the current documentation-surface list.

**Iteration 3 — `E3-01`…`E3-07`, all fixed (2026-09-23).** A fresh review (not requested as part of building anything — a standalone "check shippability and bugs" pass, written up as `ITERATION_3.md`) found the config-manifest pivot above had shipped without this file being updated to match (the gap the previous entry now closes), plus one real shippability blocker and four further confirmed correctness bugs, all in untested scenario space — the same pattern every prior review round found. All seven are fixed; `ITERATION_3.md` has the full per-item detail and is left in place as the historical record, same as `ITERATION_2.md`.
- **E3-01, major: `setup` wrote a different secret-variable naming convention than the one the shipped docs teach, and silently discarded a hand-authored canonical manifest's own `Variables` mapping on every re-run.** `configure.py`'s canonical-manifest writer hardcoded `ETL_CRAFT_POSTGRES_*`/`ETL_CRAFT_WAREHOUSE_*` pointers — a second, undocumented convention, distinct from the `ENGINE_*`/`WAREHOUSE_*`/`EMAIL_*` names `docs/craft-connector.variables.env` and both shipped worked examples teach. A team that hand-authored a manifest per the docs and later re-ran `etl-craft setup` (the tool's own advertised "run it again to update" contract) had its `Variables` mapping silently overwritten with names nothing in its environment had ever set — the next `etl-craft run` then failed to resolve its connection. **Fixed by making the bootstrap-input name and the written pointer name the same string**: `setup` now reads `ENGINE_JDBC_URL`/`WAREHOUSE_JDBC_URL`/etc. directly (previously `ETL_CRAFT_POSTGRES_JDBC_URL`/`ETL_CRAFT_WAREHOUSE_JDBC_URL`) and writes those same names into `Variables`, so the one variable a team sets serves both `setup`'s own bootstrap and every later `etl-craft run` — no second convention to drift out of sync with the first. The secret pointer is also now written flat (`ENGINE_SECRET`, not the profile-embedded `ETL_CRAFT_POSTGRES_{PROFILE}_SECRET` it was before), which lets `config.py`'s existing tier-scoped fallback (`ENGINE_DEV_SECRET` tried before `ENGINE_SECRET`) do the multi-tier job instead of baking one tier into the base name permanently. `ETL_CRAFT_POSTGRES_PROFILE` also became `ETL_CRAFT_ENGINE_PROFILE`, matching the runtime tier-override name `config.py` already read and fixing the last place the pre-rename "POSTGRES" token survived. `docs/warehouse-test.env.example` (the cloud-warehouse test harness .env), `docs/configuration.md`, and `scripts/wheel-smoke.sh` updated to match; verified end-to-end via `scripts/wheel-smoke.sh` against real Docker Postgres, which prints `Engine.dev ENGINE_SECRET` / `Warehouse.dev WAREHOUSE_SECRET` from a genuinely fresh install.
- **E3-02, major: the pipeline-level cross-pipeline dependency tracker never received the gate-resolved run id — half of E2-12 was dead code for pipelines.** `orchestrator.py`'s `init_pipeline_run`/`run_pipeline` both computed `PipelineGateResult.consumed` (what the gate actually resolved) but every `consume_pipeline_dependency_edges` call site passed only `pipeline_id`, so `consumed` always defaulted to `None` and re-derived "whatever qualifies now" — precisely the watermark-jumping bug E2-12 already fixed once for the same-invocation case, now showing up because under `Mode=orchestrator` the gate (`--init-only`) and the eventual finalize (`--finalize-only`) are two separate CLI invocations with no way for an in-memory value to survive between them. **Fixed by recording consumption immediately, at the one point the gate is actually evaluated**, rather than deferring it to whatever eventually finalizes the run: `init_pipeline_run` now calls `consume_pipeline_dependency_edges(engine, pipeline_id, gate.consumed)` right after minting, and `finalize_active_run` (Mode=orchestrator's only finalize path) no longer consumes at all — `_finalize_from_task_states` gained a `record_consumption` flag for exactly this. `run_pipeline` (local mode) threads its own freshly-computed `gate.consumed` through to its own finalize call the same way `runner.py:323` already does for the task-level tracker; `--force` and a call that reuses an already-active run it didn't itself gate keep the old re-derive behaviour, per CLAUDE.md's existing "as do callers finalizing a run they didn't gate themselves." Regression test spans a real `init_pipeline_run` call followed by a separate `finalize_active_run` call with a second qualifying upstream run appearing in between — verified to fail against the pre-fix code (no tracker row at all after `init_pipeline_run`) before being kept.
- **E3-03, major: `fetch_recorded_versions()` collided across pipelines sharing a `TASK_CODE`.** Keyed its result by bare `TASK_CODE`, but `ux_tasks_code_active` scopes that uniqueness *per pipeline*, not globally — two independently-authored pipelines with an ordinary shared task name (`LOAD`, `VALIDATE`, ...) silently overwrote each other's documentation-version badge on the generated docs site. Now keyed by `(pipeline_code, task_code)`; also hoisted to run once for the whole site in `collect_docs` rather than once per pipeline, which its own docstring had already (incorrectly) claimed it did.
- **E3-04, major: `validate`'s `EMAIL_ALERT`-ordering check (E2-60) was blind to a task with zero dependency edges.** `all_tasks`/`leaves` were built purely from `CFG_TASK_DEPENDENCY` edge endpoints, so a completely ordinary standalone task — nothing depends on it, it depends on nothing, e.g. a single ingestion step feeding nothing downstream — never appeared in any edge and was invisible to the check; `validate` reported a pipeline clean even when its `EMAIL_ALERT` task had no dependency on that task at all. The same edges-only source also meant an `EMAIL_ALERT` task with *no* edges of its own was invisible to the `alerts` set and never checked in the first place. Both now built from every active task in the pipeline (`cfg.fetch_tasks_with_parameters`), not edge endpoints.
- **E3-05, minor/moderate: a failure while committing business-rule results left that rule's run-log row stuck `IN-PROGRESS` forever.** `business_rules.py`'s per-rule commit block (the `INSERT`/`UPDATE` into `AUD_BUSINESS_RULES_RESULTS` plus the final `_mark_run_log(..., "SUCCESS")`) had no exception handling of its own — only the earlier warehouse-fetch step did. The task itself still correctly failed (`handlers.dispatch`'s own catch), but the audit trail misrepresented what happened until a retry. Now wrapped the same way the fetch step already was.
- **E3-06, minor/moderate: `setup` silently dropped `Cloning.External_volume`/`Base_location` on every write, with no bootstrap variable to set them.** Both write paths replaced the whole `Cloning` block unconditionally. New `ETL_CRAFT_CLONING_EXTERNAL_VOLUME`/`ETL_CRAFT_CLONING_BASE_LOCATION` bootstrap inputs, and an explicit bootstrap value now wins over whatever was already on disk rather than always replacing it — so a hand-added value a team set because there was no other way to set it survives a later `setup` run.
- **E3-07, minor: `cloning.py` interpolated `Cloning.External_volume`/`Base_location` unescaped into `CREATE ICEBERG TABLE` DDL.** The same class of site `sql_actions.py`'s own Snowflake-Iceberg path was already hardened for (E2-84/E2-85); `cloning.py`'s own copy of the same DDL shape had not been. Now rejects a value containing a quote before building the statement, matching `sql_actions.py`'s own guard exactly.
- **569 tests (up from 546 after round 7), 94%+ coverage, `make check`, `make db-schema-test`, and `scripts/wheel-smoke.sh` all clean.** Every regression test above was verified to fail against the pre-fix code before being kept, plus one of `ITERATION_3.md`'s own noted test-coverage gaps closed alongside them (`config.py`'s mixed-legacy-and-canonical-manifest rejection, previously untested entirely). `ITERATION_2.md` was also found deleted, uncommitted, with no explanation — restored (it is still referenced by name from this file and `CHANGELOG.md`) rather than guessed at.
- **What this round did not attempt, stated plainly**: the Databricks and Snowflake execution paths remain unverified against any real endpoint in this environment — no credentials are available here. `docs/operations.md`'s framing (see "Architecture at a glance" above) — PostgreSQL is the controlled, supported launch path; DuckDB is local-dev only; Trino-over-Iceberg is integration-tested against the repo's own local stack; Databricks/Snowflake need their own acceptance test before a customer workload — is the accurate current statement of what "works without runtime failures" actually means per warehouse, and nothing in this round changed that picture for the two cloud dialects.

**Schema is signed off (2026-09-19).** `sql/schema.sql` is reconciled against two pasted schema snapshots plus every verbal correction made after them. It makes a real number of judgment calls translating ambiguous or conflicting source material into concrete SQL — every one of them is flagged inline with `[DEVIATION]`, `[ADDITION]`, or `[CHOICE]` tags (explained in the file's header) and summarized in a block at the end of the file. Every flag was walked through individually and confirmed as written — none required a change. **Read that summary anyway before touching the schema further** — it's the map of every non-obvious decision baked into it.

Build order, per the natural sequencing agreed once schema was confirmed — resolver → CLI (`run` first) → `generate-yml` → connector/auth layer:

- **Resolver — done.** `src/etl_craft/resolver.py`. Pure, DB-free dependency-graph logic scoped to same-pipeline `CFG_TASK_DEPENDENCY` edges only (cross-pipeline edges are explicitly out of scope here — those resolve via the tracker-table polling described above, not via this module). Covers: cycle/self-dependency validation (`build_graph`), static topological waves for `graph`/`generate-yml` (`DependencyGraph.waves`), and the retry-aware `ready()` computation implementing the SUCCESS/FAILURE/ALWAYS/HAS_DATA edge semantics plus "retry resumes, not restarts" (skip SUCCESS/SKIPPED, re-attempt FAILED/never-run, never re-dispatch an already-IN-PROGRESS task). 21 tests in `tests/test_unit.py`, all passing.
- **Engine DB access layer — done (2026-09-19).** The plumbing `run` needs, built ahead of the CLI itself since it has the most unresolved ambiguity to flag:
  - `src/etl_craft/config.py` — loads and validates `craft-connector.yml`. **[CHOICE]**: the file's own example renders section names as bracketed INI-style headers (`[Execution]`, `[Postgres]`, ...) inside a yaml fence, which isn't literal YAML — the loader expects plain nested top-level keys instead (`Execution:`, `Postgres:`, ...). **[ADDITION]**: each profile stores a `jdbc_url` (per "JDBC URL is the preferred storage format") plus `user`/`auth_mode`; secret material (password/token/passphrase) is never in the file — it's looked up via an env var name resolved through `[Source]`'s file-or-environment mechanism, defaulting to `ETL_CRAFT_{SECTION}_{PROFILE}_SECRET` unless a profile sets `secret_var:` explicitly. The Data DB section was deliberately not parsed at this point in the build (open question #1 was still unresolved) — now covered below under "Data DB / warehouse connector layer."
  - `src/etl_craft/db.py` — `AUTH_REGISTRY` keyed by `auth_mode`, each entry building a `creator` callable handed to `create_engine(creator=...)` uniformly (per CLAUDE.md, not just for token/sso). `password` and `key_file` are fully implemented; `token` and `sso` raise `NotImplementedError` — CLAUDE.md establishes *that* they mint short-lived credentials but not which provider/SDK, which is necessarily team-specific.
  - `src/etl_craft/runlog.py` — implements "Run-id resolution" above as two distinct entry points, not one: `find_or_create_active_run` (for whatever mints the run — the local orchestrator before spawning task subprocesses, or the Airflow DAG's synthetic first step) versus `resolve_run_for_task` (what a plain `run --task_code` binds to: the active run if one exists, else CLAUDE.md point 5's dev/ad-hoc fallback to the latest logged run). **[CHOICE]**: "updates its dates" in that fallback is implemented as touching only `END_DATE` — `STATUS` and `START_DATE` are left alone, since reopening a terminal run's status could collide with `ux_pipeline_run_one_active` the next time that pipeline genuinely kicks off. Also includes `find_or_create_task_run` (the per-task binding, short-circuits on an existing `SUCCESS`) and `update_task_run` (in-place update, never a second row per retry).
  - Tested against an in-memory SQLite stand-in (50 tests, part of `tests/test_unit.py`) for the control-flow logic, plus a real-Postgres integration suite (part of `tests/test_integration.py`, 5 tests) that includes an actual multi-thread concurrency test proving `ux_pipeline_run_one_active` — not application code — is what makes concurrent run creation race-safe. See "Local Postgres (Docker)" below for how to run it.
- **Single-task execution core (`run --task_code`) — done (2026-09-19).** `src/etl_craft/runner.py`'s `run_task()` is what that invocation actually does — dependency check, short-circuit, bind, dispatch, finalize — built and verified against real Postgres.
  - `src/etl_craft/cfg.py` — read-only `CFG_PIPELINES`/`CFG_TASKS`/`CFG_TASK_DEPENDENCY` queries: `resolve_pipeline_id`/`resolve_task_id` (code → id, active rows only), `fetch_pipeline_graph` (active tasks + same-pipeline edges shaped for `resolver.build_graph`, plus `cross_pipeline_task_ids` — surfaced but not yet acted on, since cross-pipeline polling isn't built), `fetch_task_handler`.
  - `src/etl_craft/handlers.py` — a stub `HANDLER_REGISTRY`/`dispatch()` seam; every handler body (`PYTHON`/`SQL`/`BUSINESS_RULES`/`EMAIL_ALERT`) raises `NotImplementedError` — each is its own substantial unbuilt piece (closed SQL action vocabulary, `$$pipeline_id` substitution, Data DB connection, script invocation, business-rule sequencing).
  - `src/etl_craft/runner.py`'s `run_task()` sequence (as first built; **superseded 2026-09-19, see the "Cross-pipeline dependency polling" entry below** — an unmet dependency is no longer a raised exception): refuse `--force` under `Mode=orchestrator` first (before any DB access); resolve pipeline/task ids; resolve `pipeline_run_id` via `resolve_run_for_task` (never mints a fresh run itself — see runlog.py above, so a single-task invocation against a pipeline that's *never* run at all correctly raises `RunLogError`, not silently minting one); unless `--force`, short-circuit on an existing `SUCCESS` binding, then live-check same-pipeline dependencies by building the full pipeline graph via `resolver.build_graph`/`ready()` fed with current `AUD_TASK_RUN_LOG` state (`runlog.fetch_run_state`) — if unmet, raised `DependenciesNotMetError` without writing anything to `AUD_TASK_RUN_LOG` at all; only then bind via `find_or_create_task_run` and dispatch to the (stub) handler, marking `FAILED`/`SUCCESS` on the way out.
  - Deliberately **not** included yet (at this point in the build — see the later entry): cross-pipeline dependency polling.
  - 19 integration tests against real Postgres (part of `tests/test_integration.py`), all passing — 74 total across the whole suite. Caught the same identifier-case-folding pitfall noted above, this time in a test assertion, not production code.
- **CLI entry point — `run`, `list`, `graph`, `set-execution-mode`, `configure --env` done.** `src/etl_craft/cli.py`'s `main(argv)` wires up `etl-craft run --pipeline_code X --task_code Y [--force]` (matching the CLI surface table below exactly — `--pipeline_code` required, `--task_code` optional). `--task_code` given dispatches to `runner.run_task`; omitted dispatches to `orchestrator.run_pipeline` (below). Every exception either `run_task` or `run_pipeline` can raise for a non-bug reason (`CfgError`, `RunLogError`, `ConfigError`/`ConnectionError_`, `ForceNotAllowedError`, `DependenciesNotMetError`) is caught and printed as a clean one-line `error: ...` rather than a raw traceback. `src/etl_craft/__init__.py`'s `main()` (the `[project.scripts]` target) and `__main__.py` (for `python -m etl_craft`) both delegate to it.
  - `list` prints every active pipeline (`cfg.fetch_all_pipelines`) — code, name, refresh type, tab-separated.
  - `graph --name X` prints `X`'s same-pipeline task waves (via `resolver.build_graph`/`waves()`), its own `CFG_PIPELINE_DEPENDENCY` edges, and any task-level cross-pipeline edges. **[CHOICE]**: CLAUDE.md's CLI surface table literally writes this as `graph --name <pipeline>`, unlike every other command's `--pipeline_code` — an inconsistency in the source doc, not a deliberate difference here. `--name` is resolved as a `PIPELINE_CODE`, same as everywhere else, since that's the one designated CLI lookup key (open question #2). Flagging in case `--name` was actually meant to match `PIPELINE_NAME`.
  - **`set-execution-mode {local,orchestrator}`** (2026-09-20, `src/etl_craft/configure.py`) and **`configure --env <path>`** (same file) are the first commands that write `craft-connector.yml` rather than read it — both handled in `cli.py`'s `main()` *before* the usual `load_config`/`build_engine` setup, since neither needs (or can assume) a working Engine DB connection. Per sign-off: both do a full parse + re-serialize via PyYAML (`sort_keys=False`), never a surgical text edit — simple and robust, but every write touches the whole file, so comments/exact formatting in a hand-edited file don't survive a call. **[ADDITION]**: `configure --env`'s env-var names (`ETL_CRAFT_MODE`, `ETL_CRAFT_POSTGRES_JDBC_URL`, `ETL_CRAFT_POSTGRES_PROFILE`, `ETL_CRAFT_POSTGRES_USER`, `ETL_CRAFT_POSTGRES_AUTH_MODE`, `ETL_CRAFT_SOURCE_TYPE`/`_PATH`, `ETL_CRAFT_CLONING_ENABLED`/`_SCOPE`, `ETL_CRAFT_ORCHESTRATOR_NAME`) aren't specified anywhere in CLAUDE.md, only that the command exists — confirm this naming before anything else (docs, the reference-implementation repo) comes to depend on it. **[CHOICE]**: re-running `configure --env` against an existing file *merges* — adds/updates just the one Postgres profile named in the env file (preserving any others already there) and makes it `Active_profile`, but wholesale-replaces `Execution`/`Source`/`Cloning` since those are singular/global. Interactive `configure` (no `--env`) is not built — refused with a clear "not implemented yet" message and exit code 2.
  - Manually verified end-to-end against Docker Postgres: `configure --env` generating a fresh, `load_config`-round-trippable file; `set-execution-mode` updating just `Mode`; a second `configure --env` call correctly merging a `uat` profile alongside `dev` and switching `Active_profile`.
- **`run --init-only`, and `run_pipeline` refused under `Mode=orchestrator` — done (2026-09-20).** A real design gap surfaced while starting `generate-yml`: a generated Airflow DAG needs a synthetic first task to mint the run (CLAUDE.md: "an additional pipeline id creation step that starts in step 1 before all named steps"), but the existing `orchestrator.run_pipeline()` — a full local wave-spawning scheduler — would be redundant with (and race against) Airflow's own native per-task scheduling if invoked that way. **[CHOICE], confirmed:** `run_pipeline()` is now refused outright under `Mode=orchestrator`, *regardless of `--force`* (a real behavior change from before — it used to only refuse `--force` specifically; a bare no-`--task_code` call under orchestrator mode used to silently run the full local scheduler, which the design gap shows was always wrong). `orchestrator.py`'s own `[CHOICE]` comment has the full reasoning. The generated DAG's synthetic first step instead uses the new `--init-only` flag on `run` (`orchestrator.init_pipeline_run`) — mints/reuses the active run via `find_or_create_active_run`, does *not* spawn any task subprocesses, and is legal under both modes (`--task_code` and `--init-only` are mutually exclusive at the argparse level). Cross-pipeline dependency polling wasn't wired into it yet at this point in the build — done later, see the "Cross-pipeline dependency polling" entry below.
  - Manually verified end-to-end against Docker Postgres: `--init-only` mints on first call, reuses the same `pipeline_run_id` on a second call, and `--init-only --task_code` is rejected by argparse itself.
  - 144 tests total (up from 137), 100% coverage maintained.
- **Pipeline-level orchestration (`run --pipeline_code X`, `--task_code` omitted) — done (2026-09-19).** `src/etl_craft/orchestrator.py`'s `run_pipeline()` — CLAUDE.md's "the engine becomes its own tiny scheduler": mints/reuses the run via `find_or_create_active_run` (unlike `run_task`, which never mints), then loops — compute the ready wave via `resolver.build_graph`/`ready()` fed live `AUD_TASK_RUN_LOG` state, spawn one genuine `python -m etl_craft run --task_code` **subprocess** per ready task (not an in-process call — this is what makes local execution mirror Airflow-side execution), wait for the whole wave, repeat — until every task is settled (`SUCCESS`/`SKIPPED`) or none are ready (stuck: some task's dependency will never be satisfied this pass), then finalizes `AUD_PIPELINES_RUN_LOG` `SUCCESS`/`FAILED` accordingly.
  - **[Bug caught and fixed]**: the first version re-selected and re-spawned a permanently-FAILED task forever within one `run_pipeline()` call — real infinite loop, reproduced as an actual test hang (~150s before it was killed), not a theoretical concern. `resolver.ready()` correctly treats `FAILED` as retry-eligible *in general* (point of the whole "retry resumes" design), but that's for a **separate, later** `run` invocation to pick up — not for the orchestrator to retry within a single pass. Fixed by tracking an `attempted` set scoped to one `run_pipeline()` call. Also added `pytest-timeout` (60s, `[tool.pytest.ini_options]`) project-wide as a safety net against this whole bug class recurring, since a hang fails a lot less legibly than an assertion.
  - `--force`: since it bypasses dependency checks entirely, `ready()` can't be used to gate anything — falls back to `graph.waves()` (static topological order) instead, spawning every task in each wave regardless of current status.
  - Deliberately **not** included yet, at this point in the build: cross-pipeline dependency polling (the self-check/poll step CLAUDE.md describes before run-id minting — done later, see below), and orchestrator-level crash detection for a subprocess that dies without writing its own terminal status (CLAUDE.md's crash detection is specifically about `run_task`'s own internal fork/monitor, still deferred — a task stuck that way just can't become ready again, so the existing stuck-detection already keeps the pipeline from hanging on it).
  - 6 real end-to-end tests spawning actual subprocesses against Docker Postgres (independent tasks, blocked-downstream/stuck, retry-skip-already-SUCCESS, `--force`, mode refusal, empty pipeline) plus manual CLI smoke tests, all passing — 80 total across the whole suite (up from 74).
- **`generate-yml` — done (2026-09-20).** `src/etl_craft/generate_yml.py`'s `generate_pipeline_dag()` builds a plain dict (the CLI `yaml.safe_dump`s it, to stdout or `--output <path>`) for one pipeline's DAG. **[ADDITION]**: CLAUDE.md only says "hand-rolled, Airflow-YAML-inspired shape" — no field names or structure are specified anywhere, so every choice below is this module's own, flagged for confirmation before anything downstream depends on it:
  - `dag_id`/`description`/`schedule`/`sla_hours`/`refresh_type` from `CFG_PIPELINES` (new `cfg.fetch_pipeline_detail`), then a `tasks:` block (dag-factory-visually-inspired: `bash_command` + `depends_on`, not dag-factory's actual schema).
  - Every generated DAG gets a synthetic `__init__` task first (`etl-craft run --pipeline_code X --init-only`) per CLAUDE.md's old design-notes phrasing ("an additional pipeline id creation step that starts in step 1 before all named steps"). Each real task's `bash_command` is `etl-craft run --pipeline_code X --task_code Y` — the literal form CLAUDE.md's Execution section describes. `depends_on` is the task's own same-pipeline `CFG_TASK_DEPENDENCY` edges (task code + `dependency_type`, e.g. an `EMAIL_ALERT` task correctly shows up depending on its watched task via `FAILURE`); a task with none instead depends on `__init__` (with `ALWAYS`) rather than every task listing `__init__` redundantly — downstream tasks inherit the ordering transitively through their own chain, the same way a real Airflow `>>` DAG would be hand-written.
  - `build_graph` runs first and its `ResolverError` (cycle, self-dependency, unknown task) propagates as a clean CLI error — there's no meaningful YAML to emit for a graph that can't become a DAG.
  - Pipeline-level (`CFG_PIPELINE_DEPENDENCY`) and task-level cross-pipeline dependencies are surfaced under their own top-level keys (`pipeline_dependencies`, `cross_pipeline_task_dependencies`) purely informationally — CLAUDE.md: "one DAG can't natively depend on a task in a separate DAG" — never wired into `tasks:`, since nothing resolves them yet (see orchestrator.py's self-check/poll gap).
  - Manually verified end-to-end against Docker Postgres with a real 4-task pipeline (linear chain + an `EMAIL_ALERT` task gated on `FAILURE`) — output matches the design exactly; also verified `--output`, unknown-pipeline, and cycle-rejection error paths.
  - 12 new tests (7 for `generate_pipeline_dag` covering the structural cases above, 4 CLI-level, 1 for the new `cfg.fetch_pipeline_detail` query) — 156 total, 100% coverage maintained.
  - **Richer Airflow-facing fields, added later (2026-09-19) per explicit request to make the output easier to wire straight into a real Airflow DAG:** `catchup: false`, `tags` (`[REFRESH_TYPE.lower()]`), and a `default_args` block (`owner`, `retries`, `retry_delay_minutes`, `depends_on_past`, `email_on_failure`). **[ADDITION]**, every value grounded in real reasoning, not arbitrary: `owner` reads `CFG_PIPELINES.CREATED_BY` (new column on `cfg.fetch_pipeline_detail`/`PipelineDetail`, auto-stamped by `trg_set_audit_columns` — the closest real analog to "owner," since there's no dedicated column); `email_on_failure: false` because CLAUDE.md's own `EMAIL_ALERT` handler is the engine's alerting mechanism, and Airflow's native email-on-failure alongside it would double-alert on the same failure; `depends_on_past: false` because the run-id model has no notion of "depends on the previous DAG run" — all ordering comes from `CFG_TASK_DEPENDENCY`/`CFG_PIPELINE_DEPENDENCY`, and leaving Airflow's own past-run gating on would silently add ordering this design doesn't have; `catchup: false` because a run minted via `ux_pipeline_run_one_active` has no meaningful backfill semantics — leaving catchup on would let many missed-interval DAG runs all race to mint/reuse the same active run at once; `retries`/`retry_delay_minutes` are the one genuinely arbitrary pair (1 retry, 5 minutes — no CFG_ column backs them) but deliberately nonzero rather than Airflow's bare default, since `run --task_code`'s idempotent retry-resumes design (CLAUDE.md) makes an Airflow-level retry of the exact same `bash_command` safe by construction.
  - No new test functions needed — 3 existing ones strengthened instead (`fetch_pipeline_detail`'s two tests now assert `created_by`; `generate_pipeline_dag`'s linear-chain test now asserts the new dag-level keys), since the new fields are unconditional dict literals any existing call already exercised structurally. Still 188 total, 100% coverage maintained. Manually re-verified the full generated YAML by eye against real Docker Postgres.
- **Crash detection — done (2026-09-19).** `src/etl_craft/runner.py`'s `run_task()` now forks the actual handler dispatch (`handlers.dispatch(handler)` alone, not the dependency-check/binding work around it) into a child process via `multiprocessing.get_context("fork")` and waits on it, per CLAUDE.md's "Crash detection" section. A child that exits normally — `SUCCESS`, or a caught `HandlerError` → `FAILED` — writes its own terminal `AUD_TASK_RUN_LOG` row, same as always. A child that dies unannounced (non-zero/`None` exit code with no exception the parent ever saw — OOM-kill, segfault) gets `FAILED` written on its behalf by the still-alive parent, but only if the row is still `IN-PROGRESS`, so a child that wrote its real outcome and then hit unrelated exit-time trouble is never overwritten.
  - **[CHOICE]**: `fork`, not `spawn` or a genuine `subprocess.Popen` of a fresh `python -m etl_craft` invocation (unlike `orchestrator.py`'s task-level subprocesses, which deliberately are separate processes to mirror Airflow). Two reasons: the child always builds its own fresh `Engine` via `build_engine(config)` rather than reusing anything the parent has open, so fork's usual "never share an inherited DB connection across a fork" hazard doesn't apply; and fork duplicates the parent's already-patched in-memory state, which is what let the real crash-detection test (below) monkeypatch `handlers.dispatch` to call `os._exit(1)` and observe the parent react correctly — `spawn` would re-import everything fresh in the child and silently ignore any monkeypatch applied in the test process.
  - **[Bug caught and fixed]**: the forked child's `_dispatch_and_record()` calls `build_engine(config)` independently of the parent, which means it needs a real, resolvable secret even in tests that inject a bare `postgres_engine` fixture and otherwise never touch config/secret resolution at all. Two existing tests failed on `ConfigError: secret 'ETL_CRAFT_POSTGRES_DEV_SECRET' not found` until `tests/conftest.py`'s `postgres_engine` fixture set it via `os.environ.setdefault(...)` (never clobbering a real override) — a genuine gap this feature exposed, not a design flaw; the child is correct to insist on a real secret.
  - **[Bug caught and fixed, unrelated but found during this verification pass]**: `tests/conftest.py`'s `craft_connector_on_disk` fixture never checked Postgres reachability itself — with Docker down, any test depending on it alone (several CLI tests don't also depend on `postgres_engine`) errored with a raw `connection refused` traceback instead of skipping, contradicting this file's own documented guarantee that plain `pytest -q` never needs Docker. Fixed by making `craft_connector_on_disk` depend on `postgres_engine` (value unused, but its skip-if-unreachable check now runs first).
  - `[tool.coverage.run]` needed `"multiprocessing"` added to `concurrency` (alongside the existing `"thread"`) plus `parallel = true`, so the forked child's own execution is actually measured — each process writes its own data file, which `pytest-cov` combines automatically at session end. Without this the fork's code silently read as uncovered despite genuinely running, the same class of gap `"thread"` fixed earlier for the concurrency tests.
  - New test (`test_run_task_detects_a_child_that_dies_unannounced`, `tests/test_integration.py`) genuinely exercises the crash path against real Postgres: monkeypatches `dispatch` to `os._exit(1)`, then asserts the row still ends up `FAILED` with a "died unexpectedly" message — not simulated any other way, since this is exactly the scenario no in-process mock can prove.
  - 157 tests total (up from 156), 100% coverage maintained (all of `runner.py`, including the forked child's own lines, now shows 100%).
- **Data DB / warehouse connector layer — done (2026-09-19).** `src/etl_craft/warehouse.py`'s `build_data_engine()` — the last piece of the resolver → CLI → `generate-yml` → connector/auth layer build order.
  - **Open question #1, resolved: the Data DB section is called `[Warehouse]`.** It's the term CLAUDE.md itself already uses throughout ("Data DB / warehouse"), reads as a plain noun rather than the rejected Informatica-shaped names, and is a one-line rename in `config._parse_config` if a different name is preferred later. **[CHOICE]**: unlike `[Postgres]`, `[Warehouse]` is optional at parse time — `config.warehouse` is `None` when the section is absent, since most existing commands (`list`/`graph`/`set-execution-mode`/`configure`/`run --init-only`, and `run`/`run --task_code` until a real `HANDLER=SQL` implementation lands) never touch the Data DB at all. `build_data_engine` itself raises a clear `ConnectionError_` if called with no `[Warehouse]` configured and no profile passed explicitly.
  - **[ADDITION], the actual design problem this layer exists to solve:** per CLAUDE.md's Non-goals, the engine must never import a third-party SQLAlchemy dialect directly, yet the Data DB can be "any SQLAlchemy-supported relational engine" — so `db.py`'s existing approach (hand-written `psycopg.connect(...)` per `auth_mode`) can't generalize here; there's no single driver to hardcode. `warehouse.py`'s `_password_creator` solves this by building a real `sqlalchemy.engine.URL` from the profile, then — inside the `creator` callable `create_engine` invokes at each pool checkout — asking that URL's own resolved dialect to turn itself into raw DBAPI connect args via `dialect.import_dbapi()` / `dialect.create_connect_args(url)`, the exact mechanism SQLAlchemy's own `DefaultDialect.connect()` uses internally. This works against whatever dialect is actually installed (Postgres today, Snowflake/Databricks/etc. later as optional extras a team installs itself) without this module ever importing a specific driver — verified for real against Docker Postgres, standing in as "some installed dialect," not because the Data DB is expected to be Postgres in practice.
  - `translate_jdbc_url()` splits a `jdbc:<scheme>://host[:port]/database[?query]` URL into a SQLAlchemy dialect name + parts, via a deliberately small, non-exhaustive scheme→dialect map (`postgresql`→`postgresql+psycopg`, `mysql`→`mysql+pymysql`); an unmapped scheme passes through unchanged as the dialect name (correct whenever the two already match, e.g. `oracle`/`mssql`). **Flagging a real limit, not silently papering over it**: a vendor whose JDBC URL shape isn't `scheme://host[:port]/database[?query]` at all — Snowflake's account-identifier-as-host form, Oracle's `thin:@` form — isn't handled by this translator and would need its own parsing added once that vendor is actually chosen, not guessed at now.
  - `auth_mode=password` is the only one implemented for the Data DB, for the reason above. `key_file`/`token`/`sso` all raise `NotImplementedError` — same as `db.py`'s existing `token`/`sso` stubs, but `key_file` is unimplemented here too (unlike `db.py`, where it's fully built for Postgres): how a private-key/cert credential maps to DBAPI connect args is genuinely dialect-specific (Postgres SSL client certs and, say, Snowflake private-key auth share nothing), so there's nothing generic to build yet.
  - 12 new tests: 4 for `translate_jdbc_url`, 3 for the auth-registry creators (mocking the generic DBAPI-connect step, not a specific driver), 2 for `build_data_engine`'s own error paths, 3 for `config.py`'s new optional `[Warehouse]` parsing — plus one real-Postgres integration test (`test_build_data_engine_connects_for_real`) proving the generic mechanism, not just the mocked unit-level version, genuinely opens a connection.
  - **[Bug caught and fixed, same class as crash detection's]**: the new integration test initially had no `postgres_engine` fixture dependency (it never uses the fixture's own engine — `build_data_engine` opens its own), so it skipped the reachability guard entirely and hard-failed with a raw connection-refused traceback when Docker was down, same gap as `craft_connector_on_disk` above. Fixed the same way: depend on `postgres_engine` for its skip check alone.
  - 169 tests total (up from 157), 100% coverage maintained, stable across repeated runs.
  - **Not included**: `Cloning`'s actual copy mechanism (merge Engine DB tables into the Data DB) — `build_data_engine` is what it would connect with, but the copy logic itself isn't built.
  - **Real ClickHouse connectivity, added later (2026-09-19) per explicit request** to actually prove the generic mechanism against a dialect other than Postgres, not just assert it in theory. `clickhouse-sqlalchemy` is registered as `[project.optional-dependencies].clickhouse` (never a hard dependency of the base package, per CLAUDE.md's own Non-goals — it's also in the `dev` group, via a self-referential `etl-craft[clickhouse]` entry, so this repo's own test suite has it) — no code changes to `translate_jdbc_url`/`warehouse.py` were needed at all: `jdbc:clickhouse://host:port/db` already passes the `clickhouse` scheme through unchanged (nothing in the scheme map catches it), and that happens to be exactly `clickhouse-sqlalchemy`'s own registered plain-`clickhouse` dialect entry point (verified directly — `importlib.metadata.entry_points(group="sqlalchemy.dialects")` — before writing anything, not assumed), which resolves to its HTTP driver. First real proof this module's "never hardcode a driver" design actually holds up against a second, genuinely different dialect.
    - `docker-compose.yml` gets a `clickhouse` service (image `clickhouse/clickhouse-server:24`, host port `58123` not `8123` for the same reason Postgres uses `55432`) alongside Postgres; `.github/workflows/ci.yml` gets a matching native `services:` entry so CI exercises this for real too, not just locally. `tests/conftest.py`'s new `clickhouse_engine` fixture mirrors `postgres_engine` exactly — skips cleanly (not a hard failure) if ClickHouse is unreachable *or* the `clickhouse` extra isn't installed, since `_reachable`'s broad `except Exception` catches both the same way.
    - New test `test_build_data_engine_connects_to_real_clickhouse` — 1 more test, 188 total (up from 187 after the `validate` commit, since this landed before the `generate-yml` enrichment above), 100% coverage maintained.
- **`validate` — done (2026-09-19).** `src/etl_craft/validate.py`, wired into `cli.py` as the last unbuilt row in the CLI surface table. Per CLAUDE.md's own description — "Config integrity check — including the cross-database checks (e.g. single-column PK on a `TARGET_TABLE`) that no Engine DB constraint can enforce" — two checks make up the scope, both concrete and explicitly called for elsewhere rather than invented: **[ADDITION]** flagged since CLAUDE.md's "including" implies more could exist later; these two are simply the only ones anything currently calls for.
  - `validate_graphs()`: runs `resolver.build_graph` across *every* active pipeline in one pass (not just one named pipeline the way `graph`/`generate-yml` do), catching cycles/self-dependencies/unknown-task-ids — exactly the kind of thing no Engine DB `CHECK` constraint can catch, since `CFG_TASK_DEPENDENCY` rows are only meaningful in aggregate.
  - `validate_business_rule_keys()`: the literal check `sql/schema.sql`'s own `CFG_BUSINESS_RULES` table comment names — "every `TARGET_TABLE` is required to have a single-column primary key ... enforce it at `validate` time via introspection, not here." Uses SQLAlchemy's `Inspector.get_pk_constraint()` against the Data DB (same dialect-agnostic spirit as `warehouse.py`, never a hardcoded driver call), comparing case-insensitively against `BUSINESS_RULE_KEY_COLUMN` (Postgres folds unquoted identifiers to lowercase — same pitfall class noted earlier in this file). Reports, per active business rule: the table doesn't exist, the table has zero or >1 PK columns, or the PK column doesn't match `BUSINESS_RULE_KEY_COLUMN`. If active business rules exist but no `[Warehouse]` is configured, that absence is itself reported as an issue rather than silently skipped.
  - Both functions collect every issue found into a flat `ValidationIssue` list rather than raising on the first one — one bad pipeline or rule doesn't hide the rest of the report, and it's why `cfg.py` gained one new query (`fetch_business_rule_targets`, the `TARGET_TABLE`/`BUSINESS_RULE_KEY_COLUMN` read for every active business rule across all pipelines).
  - `cli.py`'s `_validate_command`: prints `validate: OK` and exits 0 if the issue list is empty; otherwise prints each `[category] message` and exits 1. A `ConfigError` while building the Data DB engine (e.g. an unresolvable secret) or a `SQLAlchemyError` while actually checking against it (e.g. connection refused) are each caught and printed as a clean one-line `error: ...` with exit code 2, distinct from a genuine validation failure (exit 1) — matching the rest of the CLI's error-handling convention.
  - 18 new tests (2 for `fetch_business_rule_targets`, 3 for `validate_graphs`, 7 for `validate_business_rule_keys` covering every branch — missing table, no PK, composite PK, mismatched column, case-insensitive match, no warehouse configured, empty — 6 CLI-level tests covering the OK path with and without a configured warehouse, both error-status codes, and the missing-warehouse-with-active-rule path) — 187 total (up from 169), 100% coverage maintained, stable across repeated runs.
  - Manually smoke-tested end-to-end against Docker Postgres with a real pipeline/task/business-rule/table: clean state → OK; matching single-column PK → OK; `TARGET_TABLE` pointed at a nonexistent table → correctly reported and non-zero exit.
- **Cross-pipeline dependency polling — done (2026-09-19).** `src/etl_craft/crosspipe.py`, wired into both `runner.py` and `orchestrator.py`. The last piece of "Dependency resolution & polling" — resolving `CFG_PIPELINE_DEPENDENCY` edges and genuinely cross-pipeline `CFG_TASK_DEPENDENCY` rows (`DEPENDS_ON_PIPELINE_ID != PIPELINE_ID`) at runtime, since neither has a DAG-native equivalent in any orchestrator. Two open questions were resolved directly with the user before writing any code (not guessed at): **open question #4 is resolved** — the percentage-based poll schedule (70%/80%/+10% per poll of the dependency's average run duration, capped at 30 polls or a 1-hour wall clock) is the only real mechanism; the "exponential backoff of 60 minutes" mention elsewhere in this file is stray/superseded. And: pipeline-level `HAS_DATA` (schema-allowed on `CFG_PIPELINE_DEPENDENCY` but never defined at that grain, since `AUD_PIPELINES_RUN_LOG` has no `TARGET_COUNT` of its own) means "the run succeeded AND at least one of its own tasks reported `TARGET_COUNT > 0`" via `AUD_TASK_RUN_LOG` — the pipeline-grain analogue of `resolver.py`'s existing task-level rule.
  - `check_pipeline_dependencies()`/`check_task_cross_pipeline_dependencies()`: for each active edge, poll only while the dependency's own latest run is genuinely `IN-PROGRESS` (CLAUDE.md: "if it isn't, go straight to the tracker comparison rather than polling blindly"); then compare against `AUD_PIPELINE_DEPENDENCY_TRACKER`/`AUD_TASK_DEPENDENCY_TRACKER` — a candidate run satisfies the edge only if it's newer than whatever that specific edge already consumed (no tracker row yet == never consumed == any qualifying run counts) *and* matches the edge's own `DEPENDENCY_TYPE`, mirroring `resolver.py`'s SUCCESS/FAILURE/ALWAYS/HAS_DATA semantics exactly. `consume_pipeline_dependency_edges()`/`consume_task_dependency_edges()` advance the tracker — called only from the gated party's own finalize point, per CLAUDE.md ("the tracker only updates *after* the gated task/pipeline completes"), so a run that dies before finishing never burns a qualifying upstream run it didn't actually get to use.
  - **[CHOICE], found during review before anything shipped, not after:** every public function in `crosspipe.py` takes an `Engine`, not a `Connection`, and opens a fresh short-lived connection per query rather than holding one open across a poll's real `sleep()` calls (which can span most of an hour). An earlier version took a bare `Connection` throughout — functionally correct in tests, but wrong for a real deployment: many concurrent Airflow-triggered tasks each polling for up to an hour while holding one pooled connection idle is a real path to connection-pool exhaustion, not a theoretical one. Caught and fixed before wiring it into `runner.py`/`orchestrator.py`, which meant rewriting ~20 already-passing tests to use genuinely committed data (`tests/conftest.py`'s new `two_committed_pipelines` fixture plus `insert_committed_pipeline_run`/`insert_committed_task_run`/`insert_committed_pipeline_dependency`/`insert_committed_cross_pipeline_task_dependency`) instead of the rolled-back `pg_conn`, since each function now opens its own connections and can't see uncommitted work.
  - **[Bug caught and fixed, real not theoretical]**: `CAST(:after_run_id AS BIGINT) IS NULL OR ... > CAST(:after_run_id AS BIGINT)` was needed in the "newer than last consumed" query — a bind parameter compared only via `IS NULL` (the other side of the `OR` is what compares it against a real column) leaves Postgres unable to infer its type on its own (`AmbiguousParameter`). The first fix attempt, `:after_run_id::bigint`, silently failed a different way: SQLAlchemy's `text()` treats a literal `::` right after a bind name as an escaped colon, not a cast, so the substitution never happened at all and the literal text reached psycopg unparsed. Found by actually running it against real Postgres, not assumed.
  - **Testability**: both poll loops (`_wait_for_pipeline_dependency_to_settle`/`_wait_for_task_dependency_to_settle`) take injectable `sleep`/`now` callables (defaulting to `time.sleep`/`datetime.now(UTC)`), so tests prove real behavior — poll-then-settle (a fake `sleep` flips the DB row's status on its second call), poll-cap exhaustion, and wall-clock-deadline-hit-mid-loop (a fake clock that jumps hours per "sleep") — without ever actually waiting. `_next_poll_delay()` (the interval math itself) is a pure function, tested directly with plain assertions in `test_unit.py`.
  - **[ADDITION]**: `DEFAULT_ASSUMED_DURATION_SECONDS` (300s) and `MIN_POLL_INTERVAL_SECONDS` (1s) are this module's own engineering choices, nowhere specified. The former seeds the percentage schedule for a pipeline/task pairing with no run-duration history yet to compute 70%/80%/etc. of. The latter stops an "already overdue" check (elapsed already past the target fraction) from hot-looping through all 30 polls near-instantly with an effectively-zero delay between them.
  - **Wired into `runner.py`'s `run_task()`** — per direct, detailed instruction on exactly what should happen on every invocation, regardless of who triggers it (Airflow or a human): resolve the pipeline and its dependency graph; resolve the task within it; check same-pipeline *and* cross-pipeline dependencies (poll if the checked dependency is itself running); if a dependency will never be satisfied, don't error — record the task `SKIPPED` and still exit 0, since "correctly gated off by design" isn't a failure Airflow's own retry/alerting should react to. **[DEVIATION], a real, deliberate behavior change to already-shipped code**: this replaces the previous same-pipeline-only `DependenciesNotMetError` (raised, exit 1, nothing written to `AUD_TASK_RUN_LOG`) — same-pipeline and cross-pipeline unmet dependencies are now handled identically, both ending in a `SKIPPED` row and exit 0. `run_task` also short-circuits to `SKIPPED` immediately if the `pipeline_run_id` it resolves is itself already `SKIPPED` (see below), without attempting any dependency check of its own. After dispatch (success or failure), `consume_task_dependency_edges()` runs unconditionally — completion, not success specifically, is what the tracker cares about.
  - **Wired into `orchestrator.py`'s `init_pipeline_run()` and `run_pipeline()`** — both gate on `check_pipeline_dependencies()`, but *only* on the path that's about to mint a genuinely new run (an already-`IN-PROGRESS` run's own gate already passed when it started — checked via a new `runlog.fetch_active_pipeline_run_id`). `--force` bypasses this gate entirely in `run_pipeline`, per CLAUDE.md ("`--force` bypasses all dependency/state checks"); `init_pipeline_run` has no `--force` concept to begin with (pre-existing, unchanged). **[CHOICE]**: when the gate finds an unmet dependency, the run is still minted (so any task invocation already waiting on it has a real `pipeline_run_id` to bind to) but immediately finalized `SKIPPED` rather than left `IN-PROGRESS` — this is exactly what `run_task`'s new short-circuit above reacts to. `run_pipeline` also now calls `consume_pipeline_dependency_edges()` after every finalize (the empty-pipeline early-return, and the normal wave-loop completion), matching CLAUDE.md's "after the gated pipeline completes."
  - **[Known gap, surfaced but not fixed here at the time]**: once a run's own gate passes and it genuinely starts under `Mode=orchestrator`, nothing ever marked `AUD_PIPELINES_RUN_LOG` `SUCCESS`/`FAILED` — `finalize_pipeline_run` was only called from `run_pipeline()`, refused under that mode. **Closed later the same day — see "`--finalize-only`, closing the orchestrator-mode finalize gap" below.**
  - 25 new tests (5 pure poll-math in `test_unit.py`; ~20 in `test_integration.py` covering both `crosspipe.py` directly and its integration into `run_task`/`init_pipeline_run`/`run_pipeline`) — 219 total (up from 188 before this and the two entries above), 100% coverage maintained, stable across repeated runs.
  - Manually verified end-to-end against Docker Postgres with two real pipelines and a `SUCCESS`-typed pipeline dependency: `--init-only` before the upstream ever ran → minted + immediately `SKIPPED`; a task run under that `SKIPPED` run → `SKIPPED`, exit 0; upstream marked `SUCCESS`; a fresh `--init-only` call → a genuinely new, `IN-PROGRESS` run (not reusing the `SKIPPED` one); the same task run again → proceeded past the gate and failed on the stub handler, same as any other task — proving it was the gate itself that mattered.
- **Configurable Airflow-facing settings + optional global DAG — done (2026-09-19).** Per explicit request, the `default_args`/`catchup`/`tags`/`email_on_failure` fields `generate-yml` previously hardcoded (see the "Richer Airflow-facing fields" entry above) became real, three-tier-resolved settings, plus a wholly new optional cross-pipeline trigger DAG.
  - **[ADDITION, post-signoff schema change]** `CFG_PIPELINES` gains 7 nullable columns: `CATCHUP`/`DEPENDS_ON_PAST`/`EMAIL_ON_FAILURE` (boolean), `RETRIES`/`RETRY_DELAY_MINUTES` (integer, `CHECK >= 0` when set), `TAGS`/`EMAIL_RECIPIENTS` (`VARCHAR[]`). `EMAIL_RECIPIENTS` exists specifically because Airflow's own `email_on_failure` is inert without a recipient list to send to — confirmed with the user this is needed alongside the boolean toggle, not guessed at. Flagged in a new "POST-SIGNOFF CHANGES" block at the end of `sql/schema.sql`, separate from the original sign-off summary, since the schema was already signed off (2026-09-19) before this — a real, tracked departure from that state, not folded in silently. `sql/schema_test.sql` gained a round-trip test (all 7 columns, including the array types) and two `EXPECT FAIL` cases for the new `CHECK` constraints; both verified against real Docker Postgres.
  - **`craft-connector.yml` gains a new `[Orchestrator]` section** (`config.OrchestratorConfig`): `Global_dag` (bool, **defaults to `false`** per explicit instruction), plus a global default for each of the 7 fields above (all optional, `None` when unset).
  - **Three-tier resolution**, implemented as `generate_yml._resolve()` and used for every one of the 7 fields: (1) the pipeline's own `CFG_PIPELINES` column, if not `NULL`; (2) `craft-connector.yml`'s `[Orchestrator]` section, if set; (3) the same final hardcoded default `generate-yml` already used (1 retry, 5 min delay, `depends_on_past`/`catchup`/`email_on_failure` all `false`, `tags` <- `[REFRESH_TYPE.lower()]`). `owner` stays derived from `CREATED_BY`, unchanged — not part of this resolution, no per-pipeline/global override exists for who registered a pipeline. `default_args["email"]` is only emitted when `email_on_failure` resolves `true` — never a spurious empty list when notifications are off.
  - **The optional global DAG** (`generate_yml.generate_global_dag()`, `etl-craft generate-yml --global`, mutually exclusive with `--pipeline_code` at the argparse level, matching `run`'s `--task_code`/`--init-only` pattern): refused with a clear error unless `[Orchestrator].Global_dag: true`. **[CHOICE], confirmed with the user**: each node represents triggering that pipeline's own already-generated DAG — Airflow's `TriggerDagRunOperator`, described via a `trigger_dag_id` key in this hand-rolled shape, never imported (same spirit as `bash_command` in the per-pipeline DAG) — gated on the `CFG_PIPELINE_DEPENDENCY` edge's own `DEPENDENCY_TYPE`. This was the one part of the request needing real confirmation: `run --pipeline_code X` (bare, no `--task_code`) is already refused under `Mode=orchestrator`, so a global DAG's node literally cannot shell out to it the way per-pipeline DAG nodes shell out to `run --task_code` — `TriggerDagRunOperator`-shaped nodes are the only mechanism that actually works under real Airflow. Every pipeline touched by at least one active edge (either side) gets a node; a pipeline with no pipeline-level dependencies at all isn't included, since it has no cross-DAG ordering to represent. `GLOBAL_DAG_ID = "etl_craft_global_orchestration"` — this module's own naming choice, changeable before anything depends on it.
  - **[Bug found and fixed, environmental not code]**: the new `cfg.fetch_all_pipeline_dependency_edges()` is genuinely global (unlike every other `cfg.py` query, which is scoped to one `pipeline_id`) — the first query in the whole test suite to be affected by leftover rows from `sql/schema_test.sql` (`PL_A`/`PL_B`, ...), which `make db-schema-test` had been leaving permanently in the shared dev Postgres database (`etl_craft`) rather than a disposable one, contradicting `schema_test.sql`'s own header instructions. Fixed the Makefile target itself: it now creates/applies-schema-to/drops a genuinely disposable `etl_craft_schema_test` database inside the same container, never touching the persistent dev database `make test` and everything else uses.
  - 15 new tests (4 in `test_unit.py` for `[Orchestrator]` parsing; 11 in `test_integration.py` — the new global query, three-tier resolution including the pipeline-override-wins and global-fallback cases, `email` key presence/absence, `generate_global_dag`'s node inclusion/exclusion rules, and CLI-level `--global` coverage including the disabled-by-default and mutual-exclusivity error paths) — 234 total (up from 219), 100% coverage maintained, stable across repeated runs.
  - Manually verified end-to-end against Docker Postgres with two real pipelines: a pipeline-level `RETRIES` override correctly beat a conflicting global default; a pipeline with no override correctly picked up the global default instead; `email_on_failure`/`email` correctly propagated from `[Orchestrator]` to both pipelines' `default_args`; `generate-yml --global` correctly emitted both pipelines with the right `trigger_dag_id`/`depends_on` shape.
- **`--finalize-only`, closing the orchestrator-mode finalize gap — done (2026-09-19).** The gap flagged in the cross-pipeline-polling entry above (nothing ever marked `AUD_PIPELINES_RUN_LOG` `SUCCESS`/`FAILED` under `Mode=orchestrator`, since `finalize_pipeline_run` only lived in `run_pipeline()`, refused under that mode) is closed with a synthetic *last* step, mirroring `--init-only`'s synthetic first one.
  - `orchestrator.finalize_active_run(engine, config, pipeline_code)`: finds `pipeline_code`'s currently active (`IN-PROGRESS`) run — raising a clean `RunLogError` if there isn't one (nothing to finalize) — computes `SUCCESS`/`FAILED` from every active task's own current status (the exact same rule `run_pipeline()`'s own finalize step already used), writes it, and calls `consume_pipeline_dependency_edges()`. **Refactored, not just added**: extracted the shared logic (`_finalize_from_task_states`) so `run_pipeline()` and `finalize_active_run()` share one implementation rather than two copies that could drift — `run_pipeline()`'s own finalize step is now a thin caller of it, unchanged in behavior (verified: same tests, same outcomes). Legal under both modes, like `--init-only` — its only real use case is `Mode=orchestrator`, but nothing about it depends on mode.
  - **CLI**: `run --pipeline_code X --finalize-only`, added to the existing `--task_code`/`--init-only` mutually-exclusive group (now three-way). Exits 0 on `SUCCESS`, 1 on `FAILED`, matching every other `run` outcome's exit-code convention.
  - **`generate-yml`**: every generated DAG now gets a synthetic `__finalize__` task (`etl-craft run --pipeline_code X --finalize-only`), depending via `ALWAYS` on every *leaf* task — one nothing else depends on (computed as: not the `depends_on_task_id` of any same-pipeline edge). A pipeline with no tasks at all falls back to depending directly on `__init__`, same pattern as any ordinary task with no dependencies of its own. Verified against a diamond-shaped graph (two parallel branches converging on nothing further) that `__finalize__` waits on *both* leaves, not just one.
  - 8 new tests (4 in `test_integration.py`'s orchestrator.py section — success, failure-from-an-unsettled-task, the no-active-run error, and legal-under-orchestrator-mode; 2 in generate_yml.py's section for the diamond-leaf case and the no-tasks fallback; 3 CLI-level covering both exit codes and the new mutual-exclusivity case) plus the 3 pre-existing `generate-yml` structural tests updated to account for the new `__finalize__` task in every DAG's task set — 242 total (up from 234), 100% coverage maintained, stable across repeated runs.
  - **Unrelated, found and resolved during this same session**: an in-progress `git merge origin/main` (started outside this session, likely via an IDE's own Git integration) was sitting with an unresolved conflict on `.github/workflows/ci.yml` — origin/main had independently added the same `PGHOST`-style psql fix already on this branch (see the CI bug fix noted earlier in this file). Resolved by keeping this branch's version, a confirmed strict superset (byte-for-byte diffed against the pre-merge branch state) that also includes the ClickHouse service/health-check/env-var additions absent from origin/main; completed with an ordinary merge commit, nothing lost from either side.
  - Manually verified end-to-end against Docker Postgres under `Mode=orchestrator`: `--init-only` minted an `IN-PROGRESS` run; a task's `SUCCESS` was stamped; `--finalize-only` correctly resolved and wrote `SUCCESS`; a second `--finalize-only` call (nothing left `IN-PROGRESS`) correctly produced a clean error instead of silently doing nothing or crashing; `generate-yml`'s own output confirmed `__finalize__` correctly depends only on the pipeline's real leaf task.
- **The execution engine itself — SQL actions, business rules, Python scripts — done (2026-09-19).** Per detailed, explicit instruction, the last major unbuilt piece: every `HANDLER` body except `EMAIL_ALERT` now genuinely executes, closing out `handlers.py`'s stub registry. Four new modules — `execution.py` (shared `TaskExecutionContext`/`HandlerError`/`HandlerResult`, split out purely to avoid a circular import between `handlers.py` and the three it dispatches to), `sql_actions.py`, `business_rules.py`, `scripts.py` — plus `handlers.py`/`runner.py` rewritten to build and thread a `TaskExecutionContext` through the crash-detection fork instead of a bare `(config, task_run_id, handler)` tuple.
  - **[DEVIATION, post-signoff] `CFG_TASK_PARAMS` renamed to `CFG_TASK_PARAMETERS`, `VARIABLE_NAME`/`VARIABLE_VALUE` → `PARAMETER_NAME`/`PARAMETER_VALUE`** (and `TASK_PARAM_ID` → `TASK_PARAMETER_ID` to match) — corrected per explicit instruction mid-build, before any other tooling could come to depend on the old names. `cfg.fetch_task_parameters`/`fetch_sibling_target_sql_action` and every SQL string referencing the table were updated together with the schema change.
  - **`sql_actions.py`** wraps the closed action vocabulary CLAUDE.md names but never defined: `CREATE_TABLE`, `SETUP_TABLE`, `OVERWRITE_TABLE`, `SCD1_MERGE`, `SCD2_MERGE`, `DROP_TABLE`, `DELETE_ROWS`. **[ADDITION]** `CFG_TASK_PARAMETERS.PARAMETER_NAME` vocabulary (the module's own module docstring is the authoritative reference, mirrored in `schema.sql`'s `COMMENT ON TABLE`): `SQL_ACTION`, `TARGET_OBJECT` (`schema.table`, no database prefix), `SOURCE_SQL` (the bare read-only SELECT), `MERGE_KEY`/`MERGE_COMPARE_COLUMNS` (pipe-separated column lists), `HARD_DELETE`.
    - **Audit columns, per explicit instruction**: the author's own SELECT must never project an engine-managed column — the engine appends exactly the set each action calls for, positioned after the SELECT's own business columns. Every action always appends `PIPELINE_RUN_ID` (CLAUDE.md's pre-existing auto-stamp rule); `CREATE_TABLE` adds nothing else; `OVERWRITE_TABLE` adds `UPDATE_DATE`; `SCD1_MERGE` adds `CREATE_DATE`/`CREATED_BY`/`UPDATE_DATE`/`UPDATED_BY`/`DELETE_FLAG`; `SCD2_MERGE` adds those plus `ACTIVE_FLAG`. `SETUP_TABLE` — which only ever establishes a target's *shape* ahead of the real writer — infers its own audit-column set by finding whichever other active task in the same pipeline actually writes that `TARGET_OBJECT` (`cfg.fetch_sibling_target_sql_action`) and mirroring that writer's set; falls back to `CREATE_TABLE`'s (none) if no sibling is found. **[CHOICE]** `CREATED_BY`/`UPDATED_BY` are stamped with the active `[Warehouse]` profile's `user`, not a SQL `current_user()` call — the Data DB has no "one Postgres role per human" convention the way the Engine DB does, so a literal bind value is the only thing that means the same thing across every dialect.
    - **Schema check / evolution, per explicit instruction, with no separate schema-registry table**: the task's own `SOURCE_SQL` is materialized into a uniquely-named temp table (which also supplies row counts and drives every merge statement — the SELECT only ever executes once), then compared via `information_schema.columns` on both sides against the target's business columns (its full column set minus `PIPELINE_RUN_ID` and the action's own audit columns). A staged SELECT missing a column the target already has is always a hard failure, evolvable or not — this only ever adds columns, never drops one. A genuinely new column fails with a clear reason when `CFG_TASKS.SCHEMA_EVOLUTION` is false (the default); when true, the target is rebuilt with the column added at the position the SELECT's own column order implies, existing rows backfilled `NULL`. **[CHOICE]**: Postgres has no `CREATE OR REPLACE TABLE` (unlike some other dialects) — "use CREATE OR REPLACE" (as literally instructed) is implemented as the portable three-statement equivalent every ANSI-ish engine supports instead: `CREATE TABLE <new-shape> AS SELECT ... FROM <target>`, `DROP TABLE <target>`, `ALTER TABLE <new-shape> RENAME TO <target>`.
    - **`qualify()`**: `CFG_TASK_PARAMETERS.TARGET_OBJECT`/`CFG_BUSINESS_RULES.TARGET_TABLE` are always just `schema.table`, deliberately environment-agnostic per explicit instruction — the database/catalog prefix always comes from the active `[Warehouse]` profile, producing the literal ANSI `catalog.schema.table` form. The same `schema.table` pair means a different real object in dev/uat/prod without any `CFG_` row changing across a promotion.
    - **Row counts, per explicit instruction ("implement ways to easily get impacted row counts... we correct impact counts")**: never trusts a DBAPI cursor's own `rowcount` — every count is computed with its own `COUNT(*)`/`EXISTS` query, evaluated *before* the mutating statement it describes runs.
    - **`DROP_TABLE`** is refused unless some other active task in the same pipeline creates that exact `TARGET_OBJECT` via `SQL_ACTION=CREATE_TABLE` — "drop tables created by this pipeline only using create_table," read as a safety guard against pointing a drop at an unrelated table, not literal permission to drop everything the pipeline has ever created.
    - **`DELETE_ROWS`** takes a `HARD_DELETE` param (default false/soft): `true` issues a real `DELETE`; anything else instead does `UPDATE ... SET DELETE_FLAG = 'Y'`, per explicit instruction.
    - Atomicity/idempotency, per explicit instruction: every action's statements run inside the one Data DB transaction `handlers.py` already opens — a failure partway rolls back everything, so a retry always starts from the target's last genuinely-committed state. Idempotency follows for free from each action re-deriving its effect from current state every run.
    - **[Bug caught and fixed before shipping]**: the schema-evolution rebuild's final `ALTER TABLE <new> RENAME TO <name>` used the *new* table's own bare name as the rename target instead of the *original* target's name — Postgres correctly rejected renaming a table to its own already-existing name (`DuplicateTable`). Found by actually exercising the evolution path end-to-end against Docker Postgres (a real `new_col` insertion), not assumed from reading the code.
  - **`business_rules.py`** drives `CFG_BUSINESS_RULES` per explicit instruction: `SELECT DISTINCT t.<key_column> FROM <target_table> t WHERE <scope> AND EXISTS (<business_rule_sql>)` flags rows (a new `AUD_BUSINESS_RULES_RESULTS` row, unless already actively flagged); the same query with `NOT EXISTS` finds rows that stopped violating the rule and deactivates their existing active flag. **[ADDITION]** `BUSINESS_RULE_SQL` is a correlated condition, not standalone — the outer target row is aliased `t`; nowhere else pins this convention down, and there's no reliable way to validate a rule follows it without a real SQL parser (ruled out per Non-goals).
    - **[CHOICE]** "a manual br task execution will run for all data" is read as: `--force` (the CLI's existing "bypass normal gating" signal) switches the scope predicate from `t.PIPELINE_RUN_ID = <this run>` to unconditional `1=1`. A pipeline-triggered run always scopes to `PIPELINE_RUN_ID`, which for a FULL-refresh target still covers every row anyway, since a full refresh re-stamps that column on every row it rewrites.
    - **[ADDITION, post-signoff schema change]** `CFG_BUSINESS_RULES.BUSINESS_RULE_TYPE` gains a third value, `REPORT`, alongside `INCOMPLETE`/`REJECT` — "if the team wants reportability, they should be able to do it as well." `AUD_BUSINESS_RULES_RESULTS.STATUS` gets the same third value (it mirrors the flagged row's rule type). Execution is identical for all three types; only the vocabulary widened.
    - **Cross-database mechanics**: target data lives in the Data DB, `AUD_BUSINESS_RULES_RUN_LOG`/`AUD_BUSINESS_RULES_RESULTS` live in the Engine DB — no single SQL statement can join them, so flagged/passing keys are always pulled into Python first, then written to the Engine DB as a second step, the same shape `crosspipe.py` already uses for its own cross-connection comparisons.
    - **[Bug caught and fixed before shipping, not after]**: the first version shared one Engine DB transaction (`cfg_conn`, opened by `handlers.py`) across every rule in a task. When a later rule's Data DB query raised, the enclosing transaction rolled back on the way out of `handlers.py`'s `with engine.begin()` block — taking the "FAILED" status this module had just written for the *broken* rule down with it, along with any earlier rules' genuinely-succeeded results in the same task. Found by asserting `AUD_BUSINESS_RULES_RUN_LOG.STATUS == 'FAILED'` after a deliberately malformed rule and watching the row not exist at all. Fixed by having `business_rules.execute()` take the Engine DB `Engine` directly (not a shared `Connection`) and open its own independently-committed transaction per rule, so one rule's failure never erases another rule's — or its own — already-written outcome.
  - **`scripts.py`** (`HANDLER=PYTHON`) is the one piece of this round with no detailed spec to work from beyond CLAUDE.md's single existing rule ("the engine does not auto-inject the run id... the team's own script is responsible for fetching and including `pipeline_run_id`") — **[ADDITION]**, flagged for confirmation before a real script is written against it: `CFG_TASKS.SCRIPT_NAME` runs as `<python> <script>`, inheriting this process's environment plus `ETL_CRAFT_PIPELINE_CODE`/`ETL_CRAFT_TASK_CODE`, in the current working directory (so the script can resolve its own `pipeline_run_id`, e.g. via `etl-craft`'s own `craft-connector.yml` in the same directory). A nonzero exit becomes a `HandlerError` built from stderr (or stdout if stderr is empty). The script may optionally print one trailing JSON line reporting `ingestion_count` (→ `HandlerResult.source_count`) and/or `latest_offset_value`/`latest_offset_type` (→ upserts `AUD_TASK_OFFSET_TRACKER` for that task) — both offset keys must be present together or neither is applied; an unrecognized `latest_offset_type` is a clean `HandlerError`, not a silent write.
  - **`handlers.py` rewritten**: `dispatch(engine, ctx)` (was `dispatch(handler)`) now actually calls into the three real modules, builds the Data DB engine only for `SQL`/`BUSINESS_RULES` (disposed after the one task's use, same lifecycle as `validate.py`'s own pairing), and — since this all runs inside `runner.py`'s crash-detection fork, where anything that isn't a `HandlerError` crashes the child outright and loses its real message to the parent's generic "died unexpectedly" fallback — wraps `ConfigError`/`SQLAlchemyError` into a `HandlerError` with the original message preserved. **[Bug caught and fixed before shipping]**: without that wrapping, a missing `[Warehouse]` section (`ConnectionError_`, a `ConfigError` subtype, not a `HandlerError`) crashed the forked child instead of failing the task cleanly — caught by a test asserting the real message appears, not the generic crash fallback.
  - **4 pre-existing tests broke and were updated**, not regressions — they asserted the old "every `HANDLER` is a stub" behavior, now superseded: two `HANDLER='SQL'`-stub-message assertions became `[Warehouse]`-not-configured assertions (still true — `make_config()`'s test helper has no `[Warehouse]` section); the `dispatch` monkeypatch test's lambda signature updated from `(handler)` to `(engine, ctx)`; `test_dispatch_unknown_handler_rejected` updated to the new `dispatch(engine, ctx)` call shape.
  - 34 new tests (242 → 276): pure-logic unit tests for `substitute_pipeline_id`/`qualify`/`active_database`/`_parse_trailing_json`, plus real-Postgres integration tests (Postgres standing in as *both* the Engine DB and the Data DB, `make_config(warehouse=True)`, same spirit as `warehouse.py`'s own tests) covering every action's success and failure paths, `SETUP_TABLE`'s three-way audit-column inference, a full multi-run `SCD2_MERGE` history scenario (insert new → deactivate-and-reinsert a real value change → leave an unrelated row alone), schema evolution's before/after column shape (proving the new column lands at the right *position*, not just that it exists), `BUSINESS_RULES`' flag/deactivate/force-all-data paths, and `scripts.py`'s success/failure/offset-tracker paths. 100% coverage maintained, stable across repeated runs.
  - Manually verified beyond the automated suite, against Docker Postgres standing in as the warehouse: a 3-run `SCD2_MERGE` lifecycle (bootstrap via `SETUP_TABLE` → all-new insert → a genuine value change producing a deactivated-old/active-new pair → soft then hard `DELETE_ROWS`); `SCD1_MERGE` across two runs (one update, one no-op, one insert); `OVERWRITE_TABLE` truncate-and-replace across runs; `DROP_TABLE`'s refusal and success paths; schema evolution disabled (clean failure) vs. enabled (column added mid-schema, existing data preserved) on the same starting shape; a `BUSINESS_RULES` task with a `REPORT`-typed rule flagging and correctly classifying a row.
- **Execution engine refinements — hash-key change detection, smarter `$$pipeline_id`, universal lineage, parallel BR waves, `lineage`/`migrate` CLI — done (2026-09-20).** A second, very detailed round of explicit instruction on top of the execution engine above, refining several mechanisms and closing two more open items.
  - **[ADDITION] `HASH_KEY`, per explicit instruction ("scd tables should also have hashkey created by merge_compare columns")**: `SCD1_MERGE`/`SCD2_MERGE` gain `HASH_KEY` as a new engine-managed audit column (`sql_actions.AUDIT_COLUMNS`), an `MD5` hash of `MERGE_COMPARE_COLUMNS`, NULL-safely concatenated (`_hash_expression`). Change detection (`t.HASH_KEY IS DISTINCT FROM s.HASH_KEY`) replaces the old per-column `OR`-chained `IS DISTINCT FROM` comparison entirely. **[Bug caught and fixed]**: `_add_hash_key` computes it as a follow-up `ALTER TABLE ... ADD COLUMN` + `UPDATE`, deliberately *after* the schema-shape check, not baked into the staging `CREATE TABLE AS SELECT` itself — doing it earlier made the staged shape carry a column the target-side comparison (which correctly excludes engine-managed columns) never expects, breaking the "shapes agree" check on every single SCD run. Caught immediately by re-running the existing SCD tests, not left for later.
  - **`substitute_pipeline_id` gained two more cases, per explicit instruction**: (1) if `$$pipeline_id` is absent from a task's `SOURCE_SQL` and it has *no* `WHERE` clause at all, the engine now appends one itself (`WHERE 1=1` / `WHERE pipeline_run_id = X`) rather than requiring the author to always remember the token; (2) if `$$pipeline_id` is absent but a real `WHERE` clause already exists, the SQL is left **completely untouched** — "you may need to enforce it on static tables as well, which is wrong": a query against a small reference/lookup table with its own filter and no `PIPELINE_RUN_ID` column must never get one silently `AND`-ed on. **[CHOICE]** the "has a WHERE clause" check is a plain `\bWHERE\b` regex, not a real parse (ruled out elsewhere in this module for the same Non-goals reason) — a WHERE that exists only inside a subquery, none at the outer level, would be mis-detected; flagged, not solved further. This broke essentially every existing SQL-action test using inline `VALUES (...)` fixtures with no real `PIPELINE_RUN_ID` column — fixed by adding an explicit `WHERE 1=1` to each (correctly exercising the "leave a static-shaped query alone" case, not a workaround).
  - **`DROP_TABLE`'s ordering check strengthened, per explicit instruction ("created by this pipeline using create_table *before* this drop table step")**: previously only checked that a `CREATE_TABLE` sibling exists in `CFG_`; now also verifies (`runlog.fetch_task_run_status`) that sibling has genuinely reached `SUCCESS` under *this* `pipeline_run_id` before allowing the drop — a declared-but-not-yet-run `CREATE_TABLE` task no longer satisfies the guard. `cfg.fetch_sibling_target_sql_action` renamed to `fetch_sibling_target_writer`, now returning the sibling's `task_id` alongside its `SQL_ACTION` (needed for this new check).
  - **Universal lineage declaration + traceability, per explicit instruction ("every task should have atleast 1 source_table and target_table" / "one of our goals to maintain traceability")**: every active task, regardless of `HANDLER`, now declares `SOURCE_OBJECT`/`TARGET_OBJECT` in `CFG_TASK_PARAMETERS` (pipe-separated for more than one of either) — purely declarative bookkeeping for lineage, layered on top of (not replacing) `HANDLER=SQL`'s already-functional `TARGET_OBJECT` and `HANDLER=BUSINESS_RULES`' already-functional per-rule `CFG_BUSINESS_RULES.TARGET_TABLE`. **[CHOICE]** checked by `validate` (`validate_task_lineage_declarations`, a third check alongside the graph/PK ones), not hard-enforced at runtime inside the handler modules themselves — consistent with schema.sql's own "not enforceable as a constraint, check at validate time" precedent for `CFG_TASK_PARAMETERS` conventions in general. **[Bug caught and fixed before shipping]**: the first version of `cfg.fetch_tasks_missing_source_or_target` used a bare `bool_or(...)` in its `HAVING` clause — for a task with *zero* `CFG_TASK_PARAMETERS` rows at all, the `LEFT JOIN` produces one all-`NULL` row, and Postgres's `bool_or` over an all-`NULL` input returns `NULL`, not `FALSE`; `NOT NULL` is `NULL`, which `HAVING` silently treats as "excluded" — the exact tasks the check most needs to catch (declaring nothing) vanished from the result entirely. Reproduced directly (`SELECT bool_or(NULL::boolean)` → `NULL`) before fixing with `COALESCE(bool_or(...), FALSE)`.
  - **`lineage --table <schema.table>` — a new CLI command**, closing the "table-level lineage" read-only query verb CLAUDE.md's CLI surface section already anticipated but left unnamed/unbuilt. `cfg.fetch_table_lineage` reads only the `SOURCE_OBJECT`/`TARGET_OBJECT` convention above (not `CFG_BUSINESS_RULES.TARGET_TABLE` — **[CHOICE]** one canonical place lineage is read from, not two that could disagree for a multi-rule `BUSINESS_RULES` task), filtering pipe-separated values in Python rather than attempting a fragile SQL `LIKE`-based match against one element of a delimited list.
  - **`business_rules.py` gained genuine same-wave parallelism, per explicit re-instruction ("it sequence would be like a dense rank. run in waves. every rule sharing same number for a task can run parallel")**: rules are grouped by consecutive `SEQUENCE_NUMBER` (`itertools.groupby`, already ordered by `cfg.fetch_business_rules_for_task`) into waves; a wave with more than one rule runs them concurrently via a `ThreadPoolExecutor` (I/O-bound DB round trips, not CPU work — threads, not the fork-based approach `runner.py`'s crash detection uses for an unrelated reason), each thread opening its **own** Data DB connection (`data_engine.connect()`) since SQLAlchemy `Connection`s aren't safe to share across threads. One wave fully completes — every rule, success or failure — before the next wave starts; a same-wave failure doesn't cancel its wave-mates, only stops any *later* wave from starting. `execute()`'s signature changed to take `data_engine: Engine` instead of a shared `Connection`, for the same reason.
  - **[DEVIATION, applied retroactively] Pipe (`|`), not comma, is now the one multi-value separator convention project-wide**, per explicit instruction ("any column that has a need to store more than one value must use | as separator"). `CFG_TASKS.RETURN_VALUES` (added comma-separated earlier the same round) was switched to pipe-separated for consistency with `MERGE_KEY`/`MERGE_COMPARE_COLUMNS`/`SOURCE_OBJECT`/`TARGET_OBJECT`, all pipe-separated from the start — `ck_tasks_return_values`'s regex, `scripts._parse_return_values`, and every test fixture updated together.
  - **`scripts.py`'s return-variable contract fully specified, per explicit instruction** (superseding the earlier "no detailed spec to work from" placeholder): `HANDLER=PYTHON` must report at least `INGESTION_COUNT` (a plain number) and `LATEST_OFFSET_UPDATE` (a `value|datatype` pair, e.g. `"2023-01-01 00:00:00|timestamp"`) — both declared in `CFG_TASKS.RETURN_VALUES` and both actually present in the script's one trailing JSON line, or a clean `HandlerError` (not optional the way the rest of this file's docstring once implied). Any other declared name is logged if present, silently skipped if not. **"log all the variables... as rows with variable = value semantics"**: `execution.HandlerResult` gained a `variables: dict[str, object] | None` field and a shared `format_task_log()` helper — every declared-and-present script variable becomes one `"NAME = value"` `AUD_TASK_RUN_LOG.TASK_LOG` line (in `RETURN_VALUES`' own declared order); `sql_actions.py`/`business_rules.py` leave `variables` unset and get the same `"NAME = value"` shape for free, rendered from whichever typed count fields (`SOURCE_COUNT`, `INSERT_COUNT`, ...) are non-`None` — one shared formatter, not a bespoke one per handler. The old `HandlerResult.task_log` free-text field (raw stdout dump) is gone.
  - **Coverage gate relaxed from 100% to 80%, per explicit instruction** ("use only high quality tests... try to add only high quality tests if adding new") — existing tests untouched; new tests for this round were written for genuine new behavior (including two real bugs the tests themselves caught — the `HASH_KEY`-before-schema-check ordering and the `bool_or`/`NULL` lineage gap above) rather than chased line-by-line to a percentage. Actual coverage still lands at 100% as of this round, incidentally, not by design.
  - **Migration tooling — closes CLAUDE.md open question #7, per explicit permission ("you may implement the migration mechanism as well")**: a new `SCHEMA_MIGRATIONS(VERSION, APPLIED_AT)` bookkeeping table plus `src/etl_craft/migrate.py`/`etl-craft migrate`, deliberately a small Alembic-*lite* runner, not a full framework. **[CHOICE]** `sql/schema.sql` keeps its existing role as the single authoritative *full* definition for a brand-new install; `sql/migrations/*.sql` (see that directory's own `README.md`) is only for carrying an *already-deployed* database forward incrementally from here on — nothing already baked into `schema.sql` gets a retroactive migration file, since that would misstate when each change actually happened (every one is already flagged in `schema.sql`'s own "POST-SIGNOFF CHANGES" block). `sql/migrations/` starts genuinely empty, which is consistent by construction with a fresh `SCHEMA_MIGRATIONS` table having zero rows — no bootstrapping conflict, since no migration file has ever existed for anything already in `schema.sql`. Each pending file runs inside its own transaction together with its own bookkeeping row (a failure rolls both back and halts before any later file); a file's own statements are split on `;` and executed individually, since psycopg3's extended query protocol (what SQLAlchemy's `text()` normally goes through) doesn't reliably support more than one statement per call — a known, flagged limitation (a literal `;` inside a string literal would mis-split), not silently assumed safe.
  - 18 new tests (294 total, up from 276 at the end of the execution-engine round above — `pytest --collect-only` before/after, not estimated), plus several pre-existing SQL-action tests updated in place (not counted as new) to add an explicit `WHERE 1=1` to their inline `VALUES (...)` fixtures now that `substitute_pipeline_id` auto-scopes a bare query with no `WHERE` at all. 100% coverage maintained (naturally, not enforced), stable across repeated runs.
  - Manually verified via `make db-schema-test`/`make db-reset` against real Docker Postgres after every schema change this round (`SCHEMA_MIGRATIONS` table creation, `RETURN_VALUES`' pipe-separated regex, the two new `EXPECT SUCCEED`/`EXPECT FAIL` cases) — every failure still lands exactly on the line marked to produce it.
- **Data-modeling cleanup: CFG_TASKS/CFG_PIPELINES stripped down to what's true of every row — done (2026-09-20), per explicit instruction to "think like an data modelling architect."** Two real anti-patterns fixed: dedicated columns that only ever mattered for a *subset* of rows (`CFG_TASKS.SCRIPT_NAME`/`RETURN_VALUES` — `HANDLER='PYTHON'` alone; `SCHEMA_EVOLUTION` — `HANDLER='SQL'` alone), and seven separate typed columns on `CFG_PIPELINES` that were really one cohesive, optional settings bag.
  - **[DEVIATION, post-signoff, supersedes three earlier entries in this section] `CFG_TASKS.SCRIPT_NAME`/`RETURN_VALUES`/`SCHEMA_EVOLUTION` all removed as columns**, per explicit instruction ("why special treatment for ingestion task alone... make these as something we give as parameter values"). All three now live in `CFG_TASK_PARAMETERS` as ordinary `PARAMETER_NAME`/`PARAMETER_VALUE` rows (`'SCRIPT_NAME'`, `'RETURN_VALUES'`, `'SCHEMA_EVOLUTION'`), read at execution time via `ctx.task_params.get(...)` — the same mechanism `SQL_ACTION`/`TARGET_OBJECT`/`MERGE_KEY` already used. `CFG_TASKS` now holds only what's true of every task regardless of `HANDLER`. Their old `CHECK` constraints (`ck_tasks_script_required`, `ck_tasks_return_values`) are gone with them — the same "not a schema constraint once it's a `CFG_TASK_PARAMETERS` convention, enforce at the application layer" reasoning already established for every other parameter: `scripts.py` still enforces `SCRIPT_NAME` presence and `RETURN_VALUES`' mandatory tokens at runtime, just no longer backed by a `CHECK`. `execution.TaskExecutionContext` lost the three dedicated fields entirely (not just their source) — a direct, concrete expression of "no special treatment," not merely cosmetic.
  - **[DEVIATION, post-signoff] `CFG_PIPELINES`' 7 separate Airflow-facing columns (`CATCHUP`, `TAGS`, `RETRIES`, `RETRY_DELAY_MINUTES`, `DEPENDS_ON_PAST`, `EMAIL_ON_FAILURE`, `EMAIL_RECIPIENTS`) collapsed into one `PIPELINE_PARAMETERS JSONB` column**, per explicit instruction ("these all as one pipeline_parameters column"). Same three-tier resolution as before, unchanged in `generate_yml.py` itself — `cfg.fetch_pipeline_detail` now reads the JSONB blob (psycopg3 deserializes it into a plain `dict` automatically, confirmed directly — no `json.loads` needed) and pulls the same `UPPERCASE` keys back out into `PipelineDetail`'s own unchanged, individually-typed fields, so the only thing that changed is *where* the values are stored, not how any consumer reads them. **[CHOICE]**: JSONB keys match `CFG_TASK_PARAMETERS.PARAMETER_NAME`'s own `UPPERCASE` casing convention, not a second casing style. No `CHECK` constraint on shape (the removed `RETRIES >= 0` etc.) — same "enforce at the application layer" reasoning as above.
  - Both changes are additive-only from a *behavior* standpoint (every existing test passed once its own fixtures were updated to write through the new storage location) but real schema surgery: old columns are gone, not just deprecated. `sql/schema.sql`'s own "POST-SIGNOFF CHANGES" block keeps the three superseded entries in place rather than editing them away, plus a new entry explicitly marking them superseded — an accurate history of what happened and when, per this file's own established practice.
  - No new migration file was written for this in `sql/migrations/` (still genuinely empty) — this change landed as a direct `schema.sql` edit, same as every other post-signoff change so far; the migration mechanism exists for carrying an *already-deployed* database forward, which this repo's own dev/CI Postgres isn't.
  - Test fixtures updated, not test *counts* grown: `conftest.py`'s `insert_committed_task(..., schema_evolution=, script_name=, return_values=)` convenience kwargs kept their exact shape for ~30 existing call sites, but now write `CFG_TASK_PARAMETERS` rows under the hood instead of `CFG_TASKS` columns; one `CFG_PIPELINES` direct-`UPDATE` test fixture switched from setting individual columns to a JSONB literal. 294 tests, 100% coverage maintained, stable across repeated runs — no new tests were needed since existing ones already fully exercised every affected path once their fixtures spoke the new storage location.
  - Manually verified: a real `INSERT`/round-trip of `PIPELINE_PARAMETERS` and a `CFG_TASK_PARAMETERS`-based `SCRIPT_NAME` against Docker Postgres; `make db-schema-test` (8 `EXPECT FAIL` cases remain, down from 12 — the 4 removed were exactly the `CHECK` constraints deleted from `CFG_TASKS`/`CFG_PIPELINES` above, confirmed by name, not just count).
- **`$$pipeline_id` auto-append reverted; new audit-column-presence pre-flight check added — done (2026-09-20), per explicit correction ("this was a bad idea") plus a related new request.** No schema.sql changes — both are pure `sql_actions.py` application-layer fixes.
  - **[DEVIATION, post-signoff, supersedes the "gained two more cases" entry above] `substitute_pipeline_id` reverted to a strict two-case rule**: the token is present (substitute it) or it isn't (leave `sql` **completely untouched**, `WHERE` clause or not). The auto-append-a-`WHERE`-when-none-exists case (and its known regex blind spot for subqueries) is gone entirely, per explicit instruction. Reasoning walked through with the user, not just applied silently: auto-appending made a SELECT's real behavior depend on a hidden heuristic invisible to its own author, which is a bigger overreach than plain token substitution and inconsistent with CLAUDE.md's own model that the author supplies and owns "a bare, validated, read-only SELECT." The residual risk — a task author forgets the token on a genuinely incremental source and it over-scans — is accepted as a reviewable pipeline-definition mistake ("this will only result in failure in a pipeline where there is no pipeline id, which should not be the case for what we create"), not something the engine should silently rescue by guessing. The `WHERE 1=1` suffixes added to existing test fixtures in the prior round are now unnecessary (harmless either way — a real `WHERE` with no token was always left alone) and were left in place rather than churned back out.
  - **[ADDITION] New audit-column-presence pre-flight check, per explicit instruction ("fail the task if the target is already present and does not have the scoped audit columns for that sql action")**: `_check_or_evolve_schema` now verifies every one of an action's own engine-managed columns (`PIPELINE_RUN_ID` plus whatever `AUDIT_COLUMNS[action]` lists) is actually present on an already-existing target *before* comparing business-column shape at all — a stale or hand-built target missing e.g. `UPDATE_DATE`/`HASH_KEY`/`ACTIVE_FLAG` now fails with one clear `HandlerError` naming exactly what's missing, instead of blowing up later mid-statement with a confusing raw "column ... does not exist." Runs unconditionally, regardless of `SCHEMA_EVOLUTION` — that flag only ever governs new *business* columns the staged SELECT introduces, never repairing a target's own missing engine-managed columns.
  - **[ADDITION, scoped extension beyond the literal ask, stated to the user and not corrected]** `_delete_rows`' soft-delete path (`HARD_DELETE` not `"true"`) gets the equivalent check for `DELETE_FLAG` specifically — the same failure class (a target missing the one column that action's write path depends on), reached differently since `DELETE_ROWS` never goes through `_check_or_evolve_schema` at all (it only matches on `MERGE_KEY`, no shape comparison). **[CHOICE]** Presence-only, not column-*type* checking (e.g. not verifying `ACTIVE_FLAG` is really `VARCHAR(1)` and not, say, `BOOLEAN`) — deliberately out of scope per explicit instruction to address "highly possible data engineering possibilities," not "the infinite possibilities": a predates-this-convention/hand-edited target missing a whole column is a real, seen failure mode; a column present but subtly mistyped is a rarer one not worth chasing here.
  - **Confirmed during review**: the `DELETE_FLAG` check is scoped to the soft-delete branch only (`sql_actions.py`'s `_delete_rows`, inside `else: # not hard_delete`) — a `HARD_DELETE=true` task issues a real `DELETE FROM` and never touches `DELETE_FLAG` at all, so it correctly is never rejected for a target that lacks that column. Locked in with a dedicated regression test (`test_sql_delete_rows_hard_delete_ignores_missing_delete_flag`) rather than left as an implicit property of the `if hard_delete: / else:` branching, since it's exactly the kind of scoping a later refactor could accidentally widen.
  - 4 new integration tests against real Docker Postgres (`test_sql_overwrite_table_missing_audit_column_fails_clearly`, `test_sql_scd1_merge_missing_audit_column_fails_even_with_schema_evolution` — proving the check fires even with `SCHEMA_EVOLUTION=true` — `test_sql_delete_rows_soft_delete_missing_delete_flag_fails_clearly`, `test_sql_delete_rows_hard_delete_ignores_missing_delete_flag`), each hand-building a target table missing its required audit column(s) rather than going through `SETUP_TABLE` (which would never omit them). One existing unit test (`test_substitute_pipeline_id_appends_where_when_none_exists`) asserted the now-removed auto-append behavior and was rewritten to assert the new unconditional "leave alone" rule instead (renamed `..._leaves_a_tokenless_query_with_no_where_untouched`) — not just deleted, since the no-token/no-WHERE case is still worth a named regression test for the opposite behavior. 298 tests total (up from 294), 100% coverage maintained, `make check` and `make db-schema-test` both clean.
- **The remaining CLAUDE.md scope closed out in one large round — `steps`/`history` query verbs, `EMAIL_ALERT`+SMTP with an HTML status digest, interactive `configure`, `Cloning`'s copy mechanism, and `generate-docs` — done (2026-09-20), per explicit request to build everything still open.** No `sql/schema.sql` changes anywhere in this round — every piece is pure application code against the already-signed-off schema. 368 tests total (up from 298), 100% coverage maintained (save 4 lines in `cloning.py`'s own type-dispatch function, see its own entry below), `make check`/`make db-schema-test` both clean.
  - **`steps`/`history` — closing CLAUDE.md's last two "conceptually agreed but not yet named or built" read-only query verbs.** `cfg.fetch_pipeline_steps` (a pipeline's active tasks, each with its own `CFG_TASK_PARAMETERS` dict — deliberately generic, no per-`HANDLER` formatting in the read layer, same spirit as `fetch_table_lineage`) backs `etl-craft steps --pipeline_code X`. `cfg.fetch_pipeline_run_history`/`fetch_task_run_history` (newest-first, `--limit`, default 20) back `etl-craft history --pipeline_code X [--task_code Y] [--limit N]` — pipeline-level or task-level depending on whether `--task_code` is given. 10 new tests (3 `cfg.py`-direct, 7 CLI-level including both empty-result placeholders and both unknown-code error paths).
  - **`EMAIL_ALERT` + SMTP, closing CLAUDE.md's last open Handlers question — `src/etl_craft/email_alert.py`.** Transport resolved to SMTP per explicit instruction; gating stays entirely generic (a normal `CFG_TASK_DEPENDENCY`/`CFG_PIPELINE_DEPENDENCY` edge, same as any other task) — this module's only job is composing and sending the email once dispatch already decided the task runs.
    - **[ADDITION] New `craft-connector.yml` section, `[Email]`** (`config.EmailConfig`/`EmailProfile`) — shaped like `[Postgres]`/`[Warehouse]`'s own `Active_profile`/`Profiles` pattern (a team's dev/uat/prod relays can genuinely differ), not a single flat block. **[CHOICE]** its own smaller `auth_mode` vocabulary (`none`/`password` only — `VALID_EMAIL_AUTH_MODES`) since an OAuth2 SMTP flow is exactly as provider-specific as `db.py`'s existing `token`/`sso` stubs and no team has named one to build against; left out entirely rather than stubbed with a third `NotImplementedError` nothing currently asks for.
    - **CFG_TASK_PARAMETERS vocabulary**: `EMAIL_TO` (pipe-separated, required), `EMAIL_SUBJECT`/`EMAIL_BODY` (substitution tokens allowed; `EMAIL_BODY` required unless `EMAIL_PIPELINES` is set), `EMAIL_PIPELINES` (below). Substitution tokens — `$$pipeline_id`/`$$pipeline_code`/`$$task_code`/`$$error_message` — are plain-text, pre-driver replacements, the same spirit as `sql_actions.substitute_pipeline_id`, not a templating engine; `$$error_message` is derived from the task's own active `FAILURE`-typed `CFG_TASK_DEPENDENCY` edges (`cfg.fetch_failure_watch_messages`, latest logged `ERROR_MESSAGE` per watched task, same-or-cross-pipeline) rather than a second, parallel `WATCH_TASK_CODE` parameter that could drift out of sync with the real gating edge.
    - **[ADDITION, mid-round explicit follow-up request] Every email is HTML with an inline `<style>` block, and supports a status digest**: `EMAIL_PIPELINES` — "if it is all, send the status of all pipelines for its latest [run]. if the pipeline name is mentioned, then that alone. can contain 1, some, all. some = pipeline1|pipeline2" — parsed the same pipe-separated way as every other multi-value `CFG_TASK_PARAMETERS` value (`ALL` | one code | `CODE_A|CODE_B`). When set, the email gets an appended summary table (pipeline/status/run id/start/end) plus one collapsible `<details>`/`<summary>` section per pipeline (per explicit instruction — "each pipeline should have collapsible arrow") showing that pipeline's own latest run's per-task breakdown (`cfg.fetch_task_statuses_for_run`, `PENDING` for a task with no row yet under that run). **[CHOICE]** SUCCESS (green)/FAILED (red) are the two colors that matter most, per explicit instruction ("success, falire types"); `SKIPPED`/`IN-PROGRESS`/this module's own synthetic `PENDING`/`NEVER_RUN`/`UNKNOWN` get neutral grey. **[CHOICE]** "if pipeline should support any other type that too" is read as: never hard-fail the whole alert over one bad name in the list — an unresolvable pipeline code renders as its own `UNKNOWN` row instead of raising. **[CHOICE]** plain `<details>`/`<summary>`, no JavaScript — most clients render a real disclosure triangle; the rest just show it always-expanded, a safe fallback not a broken one. **[CHOICE]** "any other template, team should use airflow," per explicit instruction — this module's reporting shape stops at one intro paragraph plus this one digest shape; richer/bespoke reporting is out of scope by design, not an oversight.
    - 24 new tests (6 `config.py` `[Email]`-parsing, 2 pure substitution, 4 pure digest-rendering/status-color, 12 integration against real Postgres with `smtplib.SMTP` faked). **[Bug avoided, not caught after the fact]**: the fake SMTP client had to write its captured calls to a file, not an in-memory list — `runner.py`'s crash-detection fork means the actual `smtplib.SMTP(...)` call happens in a *forked child process*, and mutations to Python objects there are invisible back in the parent test process (fork's copy-on-write semantics, the same reason the project's own crash-detection test already reaches for a DB-visible effect instead of an in-memory one — see that test's own note in the "Crash detection" entry above).
  - **Interactive `configure` (no `--env`), closing the last "not implemented yet" CLI row — `configure.configure_interactive`.** Mirrors `configure_from_env`'s own section shape and merge semantics (`Execution`/`Source`/`Cloning` wholesale-replaced; the one `Postgres` profile entered merges in alongside any others already on disk), plus two `configure_from_env` never covered at all: optional `[Warehouse]` and `[Email]` blocks, each gated behind its own "configure this now? y/n" prompt so declining leaves any existing section for it untouched. **[CHOICE]** the 7 `[Orchestrator]` Airflow-facing global defaults are deliberately not prompted for — real edge-case tuning is better done by hand-editing the YAML than by walking through 7 more prompts most setups would just accept the fallback default for anyway. `input_fn`/`print_fn` are injectable (default: real `input`/`print`), the same "injectable I/O" testability convention `crosspipe.py`'s `sleep`/`now` already established — every prompt/re-prompt/merge path is tested with a canned answer list, no real terminal involved. 9 new tests (7 `configure_interactive`-direct, 2 CLI-wiring — mocking `configure_interactive` itself, the same "prove the CLI wires this in, the function's own behavior is proven elsewhere" pattern the `migrate` CLI tests already use).
  - **`Cloning`'s actual copy mechanism, closing the last piece of config plumbing that never did anything — `src/etl_craft/cloning.py`.** `tables_for_scope` is this module's own authoritative `cfg`/`aud`/`all` table list (CLAUDE.md names the scopes but never enumerated the tables). **[CHOICE]** "merge-style" is implemented as truncate-then-reinsert per table, not a genuine row-level upsert/delete-tracking merge — a real cross-dialect `MERGE` has no single portable ANSI form, and CFG_/AUD_ tables are metadata, not fact-table-scale data, so a current, correct mirror is what the stated goal ("so a team can query engine config/audit from inside their own warehouse") actually needs, not incremental change tracking; flagged as a real, deliberate scope limit. **[ADDITION]** the mirrored target table's own columns are generic (`BigInteger`/`Numeric`/`Boolean`/`DateTime`/`Text`), not a byte-for-byte copy of Postgres's own types — `PIPELINE_PARAMETERS` (`JSONB`) and any array column are JSON-serialized into `Text` at insert time instead, since neither has a portable equivalent across "any SQLAlchemy-supported relational engine."
    - **[ADDITION, a real safety guard, not a contrived one]** `run_cloning_if_enabled` refuses outright if `[Warehouse]` resolves to the same `(dialect, host, port, database)` as the Engine DB — since every mirrored table keeps its Engine DB name, cloning onto itself would mean truncating and reinserting the real `CFG_`/`AUD_` tables in place from their own reflection. **[Bug caught and fixed before shipping, via a real integration test built specifically to exercise this guard, not a review]**: the first version compared the *built* `Engine` objects' own `.url` attributes — always blank on both sides, since `db.build_engine`/`warehouse.build_data_engine` both deliberately use a blank-URL-plus-`creator` pattern (never rendering a secret into a logged engine URL) — so the guard silently never fired in practice and a genuinely same-database clone hit Postgres's own real `FeatureNotSupported: cannot truncate a table referenced in a foreign key constraint` error instead of being refused up front. Fixed by comparing the two profiles' own `jdbc_url` strings (config-level data, independent of how either `Engine` gets built) instead.
    - **[CHOICE, two real ClickHouse-specific gaps, verified directly against a real ClickHouse target, not assumed]**, the same "prove it for real" bar `warehouse.py`'s own ClickHouse test already set: (1) ClickHouse refuses any `CREATE TABLE` with no explicit table `ENGINE` clause — no other dialect touched so far needs this, and `clickhouse-sqlalchemy` only accepts it via its own `Engine` construct, which this module deliberately never imports (per Non-goals, no third-party dialect is a hard/direct dependency) — `_create_clickhouse_table` is a hand-built, literal `CREATE TABLE ... ENGINE = MergeTree() ORDER BY tuple()`, reached only via the dialect *name string* `data_engine.dialect.name == "clickhouse"`, never an import. (2) `clickhouse-sqlalchemy`'s own table-engine reflection resolves the target database from `connection.engine.url.database`, not the live DBAPI connection's real one — always blank for the same blank-URL-plus-`creator` reason above — so ClickHouse alone needs the real database name passed explicitly as `schema=` (resolved once via `warehouse.translate_jdbc_url`, the same parse `warehouse.py` itself already does). (3, found the same way) ClickHouse's own `DELETE` compiler refuses an unconditional `DELETE` outright ("WHERE clause is required") — switched the clear-a-table-for-a-full-rewrite step to `TRUNCATE TABLE`, this project's own established idiom for exactly that (`sql_actions.py`'s `OVERWRITE_TABLE`), which every dialect touched so far — Postgres included — supports.
    - Wired into `orchestrator.py`'s one shared finalize point (`_finalize_from_task_states`, called from both `run_pipeline()` and `finalize_active_run()` — the latter is Mode=orchestrator's *only* pipeline-level finalize path, so this is what makes Cloning actually reachable under both modes from one call site) via `_run_cloning_best_effort`, which deliberately catches every exception and prints a warning rather than letting a Data DB hiccup turn an otherwise-successful pipeline run into a reported failure — per CLAUDE.md's own framing of Cloning as "special-cased engine-internal machinery," secondary to the pipeline's own correctness. **[CHOICE]** deliberately *not* wired into the empty-pipeline early-return or either mode's SKIPPED-via-unmet-cross-pipeline-dependency short-circuit — one shared hook, not four, and neither path changes anything in the mirrored tables worth a clone.
    - 24 new tests: 3 pure (`tables_for_scope`, 3-way `_same_database`), 8 against real Postgres-as-Engine-DB + real ClickHouse-as-Data-DB (the actual copy mechanism — deliberately *not* tested with Postgres standing in for both roles, unlike every other dual-role test elsewhere in this project, since doing so for cloning specifically would mean truncating the real `CFG_`/`AUD_` tables — exactly what the same-database guard exists to refuse), 1 against a second, genuinely separate Postgres database on the same server (proving the *generic*, non-ClickHouse create-target-table path for real, not just in theory), 2 proving the `orchestrator.py` wiring (invoked-with-the-right-args, and a cloning failure not failing the pipeline).
  - **`generate-docs`, closing CLAUDE.md's planned documentation generator — `src/etl_craft/docs_generator.py`.** Reuses exactly the existing read-layer functions (`cfg.py`, `resolver.build_graph`) `list`/`graph`/`steps`/`lineage` already use — no new query logic, just assembling their results into pages, per CLAUDE.md's own "the same read-layer queries as above." Emits `index.html` (search box + every active pipeline) plus one `<PIPELINE_CODE>.html` per pipeline (task waves, steps, pipeline/cross-pipeline-task dependencies) plus `search-index.json` plus `style.css`/`search.js`, all to a local `--output` directory (default `./etl-craft-docs`) a team then serves or deploys however it likes. **[CHOICE]** the client-side search is a small, dependency-free vanilla-JS substring search over the generated JSON index, not a vendored copy of Fuse.js/Lunr.js — CLAUDE.md's own "Fuse.js/Lunr.js-class" phrasing reads as "a client-side index of this general shape," not a literal library requirement, and staying dependency-free keeps the site working even served from a network with no CDN access at all; flagged as a real trade-off (substring matching only, no fuzzy/ranked search) appropriate for a metadata site with at most a few hundred pipelines/tasks. 6 new tests (5 pure rendering/search-index in `test_unit.py`, 2 against real Postgres including the CLI wiring).

**Tooling (added 2026-09-19).** The package is uv-managed (`pyproject.toml` + `uv.lock`, src-layout under `src/etl_craft/`, Python ≥3.11). Dev tooling: `pytest`, `black` (formatter, line-length 100), `ruff` (lint-only — `E,F,I,UP,B,SIM`; deliberately excludes `D` so it never duplicates/conflicts with pydocstyle), and `pydocstyle` (`select=D` in `[tool.pydocstyle]`). One deviation worth knowing about: a bare `select=D` selects two internally contradictory rule pairs — `D203`/`D211` (blank line before a class docstring: required vs. forbidden) and `D212`/`D213` (multi-line summary on the first line vs. the second) — so *any* style choice leaves one side of each pair permanently violated. `add-ignore = "D203,D213"` resolves this the same way pydocstyle's own `pep257` convention does; the alternative (writing every docstring as a one-line summary with extended rationale kept as plain `#` comments underneath, as `resolver.py` now does) was adopted project-wide to sidestep the conflict rather than fight it file by file. `.github/workflows/ci.yml` runs `black --check`, `ruff check`, `pydocstyle`, and `pytest` via `uv sync --locked --all-groups` on every PR into `main`.

**Coverage gate: 80% (relaxed from 100% on 2026-09-20, per explicit instruction), added 2026-09-19.** `pytest-cov` + `[tool.coverage.run]`/`[tool.coverage.report]` in `pyproject.toml` (`fail_under = 80`, was `100`); CI's final step and `make check`/`make coverage` both run `pytest --cov=etl_craft --cov-report=term-missing`. **[DEVIATION]** the instruction accompanying the relaxation was explicit about *why*: "use only high quality tests... try to add only high quality tests if adding new" — the gate exists to catch real regressions, not to force line-by-line coverage-chasing on every future change; existing tests were left untouched, and actual coverage has stayed at 100% since incidentally (each new feature's own genuinely-new branches got real tests, not padding). Coverage is only meaningful against the **full** suite (unit + integration) — most of the source is exercised through the Postgres-backed tests, so measuring `test_unit.py` alone reports large, misleading gaps. The original push to 100% required real investigation, not just writing tests until a number hit it:
- `[tool.coverage.run] concurrency = ["thread"]` is required, or coverage.py silently doesn't trace background threads at all — without it, `test_integration.py`'s concurrency tests report 0% on the very race-handling code they exist to exercise, despite genuinely running it.
- Even with that fixed, the original 8-thread best-effort race test (`test_concurrent_find_or_create_active_run_converges_on_one_run`) turned out not to *reliably* force the `except IntegrityError` branch — depending on connection-pool/network timing, a "losing" thread's own SELECT can run after the winner already committed, so it takes the early-return path instead of ever attempting the conflicting INSERT. Fixed by adding two **deterministic** 2-thread race tests (`..._second_transaction_hits_integrity_error_path`, one per `find_or_create_*` function) that explicitly control the interleaving via a `threading.Event` and a timed hold, rather than relying on 8-way scheduling luck. The original best-effort test stays too, as a broader stress proof.
- A few branches are genuinely unreachable through any real path and are either exercised by directly calling the private function with hand-built invalid input (`resolver._edge_satisfied`, `db.build_engine` with a bad `auth_mode`, `config._load_dotenv_file(None)`, `handlers.dispatch("BOGUS")`) or, for `runlog.py`'s "unique_violation fired but the re-SELECT still finds nothing" defensive `RunLogError` (which *cannot* happen against a real Postgres — a unique_violation means a conflicting row exists by definition), simulated with a minimal fake `Connection` in `test_unit.py`. `runner.py`'s `SUCCESS`-finalizing path is unreachable through any real handler today (every `HANDLER` is still a stub that always raises) — exercised by monkeypatching `etl_craft.runner.dispatch` to return a real `HandlerResult` directly.
- `cli.py`'s final dispatch fallback (`return 2` after exhausting `run`/`list`/`graph`) is structurally unreachable — argparse's `required=True` on the subparsers already guarantees `args.command` is one of the three — and is the one line excluded via `# pragma: no cover`, with a comment explaining why.
- `runpy.run_module("etl_craft", run_name="__main__")` executes `__main__.py`'s `if __name__ == "__main__":` guard in-process (not a subprocess), which is what let it be measured at all — the orchestrator's own subprocess-spawning tests exercise the same line for real but in a child process, invisible to the parent's coverage collection.

109 tests total (up from 87).

**Local Postgres (Docker), added and verified end-to-end 2026-09-19.** `docker-compose.yml` runs Postgres 16 on host port `55432` (not `5432`, to avoid colliding with a locally-installed Postgres) with `sql/schema.sql` auto-applied via `docker-entrypoint-initdb.d` on first boot. `make db-up` / `make db-down` / `make db-reset` (tears down the volume too, since `schema.sql` is plain `CREATE TABLE` — not idempotent — by design, meant to run once against an empty DB) / `make db-schema-test` (runs `sql/schema_test.sql` inside the container) / `make test` / `make check` (mirrors CI). The Postgres-backed integration suite (`tests/test_integration.py`, via `tests/conftest.py`) skips itself with a clear message if nothing is reachable at `ETL_CRAFT_TEST_DATABASE_URL` (defaults to the compose file's own connection string), so `pytest -q` alone never requires Docker. CI runs the same integration suite for real, via GitHub Actions' native `services:` (no Compose needed there) rather than skipping.

**Real CI bug, found 2026-09-20 (first genuine GitHub Actions run) and fixed.** `.github/workflows/ci.yml`'s `psql -f sql/schema.sql` step failed with `connection to server on socket "/var/run/postgresql/.s.PGSQL.5432" failed` — it was passing `ETL_CRAFT_TEST_DATABASE_URL` (SQLAlchemy's `postgresql+psycopg://` dialect-qualified scheme) straight to `psql`, which speaks plain libpq and doesn't recognize `+psycopg`; rather than erroring on the bad scheme, libpq silently fell back to a local Unix socket that doesn't exist in that job. This had never been exercised: local testing always used `docker compose exec ... psql -U ... -d ...` (flag form, no URI at all), and there's no way to run GitHub Actions itself locally. Fixed by giving the `psql` steps libpq's own standard `PGHOST`/`PGPORT`/`PGUSER`/`PGPASSWORD`/`PGDATABASE` env vars instead, so they need no connection string; `ETL_CRAFT_TEST_DATABASE_URL` stays SQLAlchemy-flavored for the Python suite alone. Reproduced the exact failure and confirmed the fix with a throwaway `docker run --network host postgres:16 psql ...` against the local Docker Postgres before pushing, since that's the closest thing to CI's own environment available here.

Ran `make db-schema-test` and `make check` for real once Docker was available: `sql/schema_test.sql` passed line-for-line as documented against actual Postgres 16, and the integration suite immediately caught a real bug — `find_or_create_task_run` read a Row's `TASK_RUN_ID`/`STATUS` attributes by their as-written uppercase names, which works against the SQLite stand-in (preserves declared case) but not real Postgres (folds unquoted identifiers to lowercase), raising `NoSuchColumnError`. Fixed by adding explicit lowercase `AS` aliases to those `SELECT`s. **Lesson for any future raw-SQL code here**: always alias `SELECT` columns explicitly in lowercase (or another fixed casing) rather than relying on a Row's attribute-name matching an unquoted identifier's original casing — SQLite and Postgres disagree on what that casing even is.

## Core design principles

These aren't stylistic preferences — violating them undoes decisions made deliberately over a long design conversation:

- **Config vs. code, drawn by blast radius, not convenience.** A step that only reads/writes within the platform (a `CFG_` row) is config — low-friction, eventually CLI-driven. A new external connection, a new action type, or a destructive schema change is code — reviewed, deliberately harder to change. Creating a new pipeline is *always* manual (git-managed inserts/migrations), never a CLI verb.
- **Closed vocabulary of SQL actions.** `CREATE_TABLE`, `SETUP_TABLE`, `OVERWRITE_TABLE`, `SCD1_MERGE`, `SCD2_MERGE`, `DROP_TABLE`, `DELETE_ROWS` — implemented in `sql_actions.py` (see "Where things stand"), the authoritative reference for the exact vocabulary and mechanics now that it's built. Every SQL-handler task supplies a bare, validated, read-only `SELECT`; the engine wraps it in whatever statement the declared action calls for, and appends whichever engine-managed audit columns that action calls for — never present in the author's own SELECT. The engine owns every write — a step cannot touch the warehouse outside its declared action. Cover new needs by extending an existing action's metadata, not by adding a new action type casually.
- **Order comes from data.** `CFG_TASK_DEPENDENCY` / `CFG_PIPELINE_DEPENDENCY` rows are the DAG. Nothing enumerates order in code; the resolver just walks dependency rows. Same-pipeline order compiles into Airflow's native task chaining when generating YAML; cross-pipeline order has no structural equivalent in any orchestrator and is always resolved by self-check/polling at runtime.
- **Idempotent by construction, retry resumes.** Re-running a step must produce the same result whether it runs once or five times. A retry reads the log, skips what's already `SUCCESS` or `SKIPPED`, and only re-attempts what actually failed or never ran.
- **One run id, resolved, never passed.** `pipeline_run_id` is never handed between tasks — no XCom, no injected env var. Every task independently resolves the currently-active run by querying `AUD_PIPELINES_RUN_LOG`. See "Run-id resolution" below; this is probably the single most load-bearing mechanism in the whole design.
- **Full-refresh/derived state by default; incremental only where provably safe.** A table either accumulates every run (history) or holds exactly one run's truth (current state) — never a mix.

## Non-goals — things explicitly considered and rejected

Do not reintroduce these without a real reason; each was deliberately ruled out during design:

- REST API calls to any orchestrator, including Airflow, at execution time. The only hard runtime dependency, checked by every action, is a valid Engine DB connection (SQLite by default, PostgreSQL in production).
- XCom, or any other orchestrator-specific mechanism, for passing `pipeline_run_id` between tasks.
- Importing `dag-factory` or any other orchestrator-specific library. `generate-yml` is entirely hand-rolled — the YAML shape takes visual inspiration from how Airflow/dag-factory-style YAML looks, for familiarity, but is not required to match any external tool's schema, and no such library is a dependency.
- Support for more than one warehouse connection per deployment. One warehouse, full stop — the explicit reasoning given for this was "else we will become Informatica."
- Any runtime detection scheme for whether execution mode is being spoofed (credential-probing, install-time locks were both considered and dropped). Final stance: trust the developer. `--mode` is never a runtime flag typed by a human for orchestrator runs — it's baked in once, at setup time, via `set-execution-mode`, and persists in `craft-connector.yml` until the environment is rebuilt.
- Sourcing credentials from an orchestrator's own connection store (e.g. reading Airflow Connections directly). All connections resolve through `craft-connector.yml` only, to stay orchestrator-agnostic — this includes the warehouse connection, "even on the Airflow side."
- ~~A SQL parser dependency.~~ **Reversed 2026-09-20 by explicit instruction** ("sql parser to implement column level lineage. dont re-invent the wheel use metadata and any existing package that can do this"). `sqlglot` is now a hard dependency, used only by `column_lineage.py`. Writing our own was considered and rejected on the instruction's own terms ("if it is easy implement our own, else go with hard dependency") — it is not easy, and a hand-rolled parser would be subtly wrong in exactly the cases lineage is consulted for. Note `validate`'s read-only lint (E2-07) deliberately stays a string check: it is a lint, not a parse, and does not need one.
- Bundling third-party SQLAlchemy dialects (Snowflake, Databricks, BigQuery, Redshift, ...) as hard dependencies. They're optional extras a team installs itself; SQLAlchemy discovers an installed dialect automatically via setuptools entry points, so engine code never imports one directly. **[DEVIATION, 2026-09-21] One scoped exception: `duckdb-engine` is a hard dependency**, because DuckDB is embedded — there is no server to stand up, so shipping it is what lets `etl-craft setup` reach a working warehouse in one step, the same reason `psycopg` ships for the Engine DB. It does not reopen the rule for anything server-based. ClickHouse was briefly supported and is gone (see the dated entry under "Where things stand"): too far from ANSI for the SQL-action vocabulary to hold there.
- A heartbeat column plus an external "reaper" process for detecting dead tasks. Superseded by parent/child process monitoring — see "Crash detection" below.
- A staging/cross-connection copy path for ordinary tasks. Moot by construction once there's only one warehouse — `source_object`/`target_object` are always in the same place.

## Architecture at a glance

**Two databases, two very different rule sets:**
- **Engine DB** — **SQLite by default, PostgreSQL recommended for production** (changed 2026-09-24, per explicit instruction; it was "always Postgres, no exceptions" before). Holds every `CFG_`/`AUD_` table. The load-bearing guarantee — a partial unique index making run-id creation race-safe (see below) — holds on both: SQLite supports partial indexes and serializes writers besides. `schema_sqlite.sql` is `schema.sql`'s table-for-table translation; `locks.engine_lock` is a Postgres advisory lock or, on SQLite, an OS file lock beside the database file. SQLite's real limits are why Postgres stays the production answer: one machine (orchestrator workers elsewhere cannot open the file), serialized writes, and no database users for `CREATED_BY`. Every engine action checks for a valid Engine DB connection first, before anything else.
- **Atomicity on an Iceberg warehouse (2026-09-22, E2-67)**: there is none. Trino does not roll back, DML included — verified, not inferred. An action that fails partway leaves the target half-written, so **idempotency is what makes a retry safe, not atomicity**: every action re-derives its whole effect from current state, and converges. Plan recovery around that rather than around a rollback that will not happen. Postgres keeps the full transactional guarantee. `sql_actions.py`'s own module docstring carries the detail.
- **Warehouse** — exactly one per deployment. **PostgreSQL, or a SQL engine over Iceberg** (or, since 2026-09-22, that engine's own native format — `[Warehouse].Table_format`/`CFG_TASK_PARAMETERS.TABLE_FORMAT`, `iceberg` by default) (settled 2026-09-22: `engine: always postgres` / `warehouse: postgres (pg analytics), databricks with iceberg unity catalog, snowflake with iceberg, maybe trino with iceberg or any sql tool over plain iceberg`). Postgres stores its own tables natively and is the one with no caveats. On **every other warehouse, every table the engine creates is an Iceberg table** — per explicit instruction, "if the warehouse is not postgres, every table we create or operate should be iceberg compatible". Dialects are optional extras (`etl-craft[databricks|snowflake|trino]`), never imported by engine code. DuckDB remains supported for local development, and is the one warehouse that admits a single *writing process* at a time, so the engine serializes warehouse access for it through an Engine DB advisory lock (`warehouse.open_warehouse`, E2-61); no other supported warehouse needs that. Any other SQLAlchemy-supported relational engine will likely work and is an optional dialect a team installs itself, but nothing here tests it. Because there's only one, `TARGET_OBJECT`/`SOURCE_SQL` in `CFG_TASK_PARAMETERS` always resolve against that same database — there is no cross-connection staging path to build for ordinary tasks. `TARGET_OBJECT` is stored as bare `schema.table`, deliberately environment-agnostic — the database/catalog name is always supplied at runtime from the active `[Warehouse]` profile (`sql_actions.qualify()`), so the same CFG_ row means a different real object in dev/uat/prod.
  - **What's actually verified versus what's supported in principle, stated plainly (`docs/operations.md`, added 2026-09-22/23; updated 2026-09-23 — see "Where things stand" for the full detail):** the *controlled, supported* launch path for a real deployment is a PostgreSQL Engine DB with a PostgreSQL warehouse — "the one with no caveats" above is not a rhetorical flourish, it is the operational recommendation. DuckDB is verified for real (E2-61's queueing, a genuine multi-task-wave regression test) and is the local-development target, not a production one — it admits one writing process at a time. Trino-over-Iceberg is integration-tested against the repo's own local stack (real MinIO + Iceberg REST + Trino via `docker-compose.yml`), including a real two-run `SCD1_MERGE`. **Databricks is now verified live**, both `native` (managed Delta) and `iceberg` (managed Delta with UniForm enabled, not a literal `USING ICEBERG` clause — see "Where things stand"), through `CREATE_TABLE`/`OVERWRITE_TABLE`/`SCD1_MERGE`. **Snowflake is now verified live too, both `native` and Snowflake-managed Iceberg tables**, via the preferred (PAT) connection shape — Iceberg defaults to `EXTERNAL_VOLUME = 'SNOWFLAKE_MANAGED'` (Snowflake's own internal storage, not a customer bucket) when a task declares no external volume of its own, which is what makes it zero-config and what let it pass despite this particular account's one customer-owned external volume being on a different cloud than the account itself (see "Where things stand" for the full sequence). `docs/operations.md` says to treat each cloud warehouse as its own acceptance target before enabling it for a customer workload; treat that as a hard requirement, not caution for its own sake.

**Connections & auth** — all resolved through `craft-connector.yml` (below), never hardcoded, never sourced from an orchestrator. JDBC URL is the preferred storage format for a connection string wherever one is persisted; a small internal translator converts it to the right SQLAlchemy dialect URL at connection time. Auth is a small registry of functions keyed by each profile's `auth_mode` (`password`, `token`, `sso`, `key_file`), handed to `create_engine(creator=...)` — the `sso`/`token` modes mint or refresh short-lived credentials per connection. For those two specifically, `pool_recycle` should sit comfortably under the credential's real lifetime so a checked-out connection always has meaningful life left — that's a safety margin, not a guarantee; a connection dying mid-operation is just another failure the idempotent-retry design already absorbs. `pool_pre_ping` is a worthwhile optimization on top but not load-bearing.

**Execution — one primitive.** Everything runs through `etl-craft run --pipeline_code X [--task_code Y] [--force]`:
- `--task_code` given → runs exactly one task. This is the literal form Airflow's generated `BashOperator` tasks shell out to.
- `--task_code` omitted → runs the whole pipeline. The engine becomes its own tiny scheduler: resolve the ready wave from `CFG_TASK_DEPENDENCY` (the same resolver `graph` uses to walk the same data, just consumed differently), spawn one `run --task_code` subprocess per ready task, wait on the wave, repeat. Local runs are built to mimic exactly what an orchestrator-driven run does — sequential/parallel waves, subprocess-per-task, crash monitoring — unless `--force` is passed.
- `--force` bypasses all dependency/state checks entirely, for a pipeline or a single task. Only legal under `Mode = local`; refused outright under `Mode = orchestrator`.
- There is deliberately no separate `execute --local`/`execute --airflow` verb, and no REST-based trigger/status command — `run` is the only execution primitive, in both modes.

**Execution mode (`local` vs. `orchestrator`)** is set once per environment via `etl-craft set-execution-mode`, persisted in `craft-connector.yml`. It is **not** a per-invocation CLI flag — nothing in a generated DAG or a manual command line passes `--mode`. Changing it means re-running setup, which in a real deployment goes through the same versioned pipeline as any other environment change. This is intentionally a trust boundary, not a technical one: see the Non-goals entry above.

**Crash detection.** `run` forks the actual task logic as a child process and watches it. A child that exits normally — success, or a handled failure — writes its own final log row, as always. A child that dies unannounced (OOM-kill, segfault) gets `FAILED` written on its behalf by the still-alive parent. This is identical in local and Airflow modes, since Airflow-side execution is this same subprocess form — nothing orchestrator-specific to build. Known, accepted gap: if the *entire* process tree dies at once (container OOM-killed, pod evicted, node gone), nothing survives to write `FAILED`. Airflow's own zombie-task detection covers its UI in that scenario but never touches `AUD_TASK_RUN_LOG`, which is left permanently stale. Accepted as rare enough not to solve for now.

## Run-id resolution (read this before touching anything related to run state)

`pipeline_run_id` is never passed to a task — every task resolves it itself by querying `AUD_PIPELINES_RUN_LOG`:

1. The first task of a run does a **find-or-create**: reuse an existing `IN-PROGRESS` row for this pipeline if one exists; otherwise mint a new one.
2. This must be atomic at the database level — a trigger or application-level check alone cannot close the race between two near-simultaneous kickoffs of the same pipeline. The actual guarantee comes from a **partial unique index**: `UNIQUE (PIPELINE_ID) WHERE STATUS = 'IN-PROGRESS'`. The second concurrent insert fails outright rather than racing. This is implemented in `sql/schema.sql` as `ux_pipeline_run_one_active`.
3. Every later task queries for whatever run is currently active for its pipeline and binds itself to it (one `AUD_TASK_RUN_LOG` row per task per pipeline run, found-or-created and updated in place — never a new row per retry attempt).
4. If a task already has a binding under that run and it already shows `SUCCESS`, it returns success immediately without re-running. This is what makes retries — whether triggered locally or by an orchestrator — always *continue* a failed/in-progress run rather than starting fresh, and is why a run should never end in an intermediate state.
5. A single-task invocation with nothing currently active is a dev/ad-hoc convenience path: it binds to the latest logged run and updates its dates. Not an everyday scenario — don't over-engineer around it.

## Incremental / full-refresh mechanics

- `CFG_PIPELINES.REFRESH_TYPE` (`FULL`/`INCREMENTAL`) is a **pipeline-level** value — every task in one pipeline run shares one mode. Mixing an always-full dimension load with an incremental fact load means two pipelines, not one. **Note:** both pasted schema drafts still show `REFRESH_TYPE` on `CFG_TASKS` — that was superseded by a later, explicit decision to move it to the pipeline. `sql/schema.sql` implements the pipeline-level version; `CFG_TASKS` carries a comment noting the column is intentionally absent there.
- Every SQL step's `SELECT` may contain the literal token `$$pipeline_id` in its `WHERE` clause. The engine runs its own plain-text substitution pass *before* the query reaches the driver's own bind-variable handling, so `$$` tokens and real bind params (`:param`, `%s`) never collide.
- At substitution time: `$$pipeline_id` becomes `pipeline_run_id = <this run's id>` for incremental, or `1=1` for full refresh.
- **Refined 2026-09-20, per explicit instruction, to two further cases** (`sql_actions.substitute_pipeline_id`): if the token is absent and the `SELECT` has **no `WHERE` clause at all**, the engine appends one itself, so an author doesn't have to remember the token on every ordinary query; if the token is absent but a **real `WHERE` clause already exists**, the query is left completely untouched — "you may need to enforce it on static tables as well, which is wrong," since a small reference/lookup table with its own filter and no `PIPELINE_RUN_ID` column must never get one silently `AND`-ed on. The "has a `WHERE`" check is a plain regex, not a real parse — a `WHERE` that exists only inside a subquery would be mis-detected as present at the top level; a known, flagged heuristic limit, not solved further.
- **[SUPERSEDED, same day, 2026-09-20]** The auto-append bullet directly above was reverted per explicit correction ("this was a bad idea"). `substitute_pipeline_id` is back to a strict two-case rule: token present → substitute; token absent → leave `sql` **completely untouched**, `WHERE` clause or not — no heuristic `WHERE`-detection, no auto-append. A missing token on a genuinely incremental source is now treated as a reviewable pipeline-definition mistake, not something the engine tries to silently rescue. See "Where things stand"'s own dated entry for the full reasoning and the new audit-column-presence check added alongside it.
- Every table written by a SQL action carries a `pipeline_run_id` column that the engine auto-stamps. A step author writes `select col1, col2 from table where $$pipeline_id` — never manually selects or inserts the id.
- **`HANDLER = PYTHON` ingestion scripts are the one exception.** The engine does not auto-inject the run id into them; the team's own script is responsible for fetching and including `pipeline_run_id` in whatever it inserts.
- Runtime substitution, rather than trusting a stored value, matters specifically for full refresh: it's what lets a downstream table that maintains history, with no new data after change-detection, still correctly carry the *current* run's id instead of getting stuck showing some run from long ago.
- `HAS_DATA` (`TARGET_COUNT > 0`) is only a meaningful signal against an **incremental** upstream — there it genuinely reflects whether anything changed, and downstream tasks can legitimately skip on it. Against a full-refresh upstream, every run re-asserts the complete current state regardless of whether values actually moved, so `HAS_DATA` isn't a real change signal there — full refresh just proceeds and re-stamps.

## Dependency resolution & polling

- **Same-pipeline task deps**, when going through a generated DAG, compile into Airflow's own native task chaining (`>>`), built from `CFG_TASK_DEPENDENCY` — series, parallel, conditional shapes, as the data dictates.
- **Any path that bypasses the DAG's own ordering** — a manual single-task run, a backfill, a re-triggered task — has no structural ordering to lean on, so the engine still has to verify same-pipeline dependencies itself: a live query against `AUD_TASK_RUN_LOG` scoped by the shared `pipeline_run_id` (no staleness comparison needed, since both tasks already share one run).
- **Cross-pipeline dependencies** (`CFG_PIPELINE_DEPENDENCY`, and `CFG_TASK_DEPENDENCY` rows where `DEPENDS_ON_PIPELINE_ID` points elsewhere) have no DAG-native equivalent in Airflow or otherwise — one DAG can't natively depend on a task in a separate DAG. These are always resolved by a self-check/poll step inserted before step 1, alongside run-id minting.
- The naive version of this check — "look at the dependency's last logged run" — breaks the moment two pipelines run on different schedules (weekly vs. daily, say): most days the dependency isn't in-progress, so the check falls straight to a stale "last logged run" from days ago and wrongly proceeds. The fix: two watermark tables, `AUD_PIPELINE_DEPENDENCY_TRACKER` and `AUD_TASK_DEPENDENCY_TRACKER` (in `sql/schema.sql`, not in either pasted schema draft — designed later), recording the run last *consumed* for each dependency edge. A candidate run only satisfies the edge if it's newer than what was already consumed for that specific edge *and* matches that edge's own `DEPENDENCY_TYPE` — a `SUCCESS` edge tracks the last `SUCCESS` run, `FAILURE` tracks the last `FAILED` run, `HAS_DATA` tracks the last run with `TARGET_COUNT > 0`. Until a new qualifying run appears, the edge keeps resolving to "not yet satisfied," with no polling wasted on a pipeline that was never going to produce one today.
- The tracker only updates *after* the gated task/pipeline completes. Cross-pipeline task-level tracking only applies to actual cross-pipeline edges — same-pipeline task deps never touch it.
- Poll only while the dependency is genuinely `IN-PROGRESS`; if it isn't, go straight to the tracker comparison above rather than polling blindly.
- Poll cadence, as specified: a hard 1-hour timeout overall. The interval strategy is duration-aware rather than fixed — based on the dependency's average run duration and current elapsed time, first poll at 70% of that average, next at 80%, +10% each poll after, capped at 30 polls total. Whichever limit is hit first (poll count or the 1-hour wall clock) ends the wait. **Resolved (2026-09-19, open question #4): the percentage-based schedule alone governs.** The earlier "exponential backoff of 60 minutes" mention is stray/superseded, not a second real mechanism — confirmed directly, not guessed at. When no duration history exists yet for a new pipeline pairing (nothing to compute 70%/80%/etc. of), there's still no separate backoff fallback specified; see this build's own `[CHOICE]` on what fills that gap.

## Business rules

- `CFG_BUSINESS_RULES.BUSINESS_RULE_TYPE` (`INCOMPLETE`/`REJECT`) classifies the **row** a rule flags — it is not a pass/fail outcome. Rule *run* outcome (did the check itself execute successfully) is tracked separately, in `AUD_BUSINESS_RULES_RUN_LOG.STATUS`.
- Ordering is `SEQUENCE_NUMBER` (dense rank per task) directly on `CFG_BUSINESS_RULES` — same rank runs in parallel, different ranks run sequentially. There is no separate business-rule dependency table.
- Every target table is required to have a single-column primary key — an enforced framework convention, which is why `BUSINESS_RULE_KEY_COLUMN` can safely stay a single column rather than a list. This convention lives outside the Engine DB entirely (it constrains tables in the warehouse), so it cannot be checked by an Engine DB constraint — enforce it at `validate` time via schema introspection against the warehouse.
- A flagged row's classification is audit information, not a failure signal — a business-rule task that flags every row it checks is still a `SUCCESS` task run. Nothing here auto-blocks a pipeline; that would have to come from a separate, explicitly-declared task dependency on the BR task's own status.

## Handlers

Four, closed: `PYTHON` (ingestion scripts, `scripts.py`), `SQL` (engine-executed, wraps a bare `SELECT` per the closed action vocabulary, `sql_actions.py`), `BUSINESS_RULES` (drives `CFG_BUSINESS_RULES` rows for that task, `business_rules.py`), `EMAIL_ALERT` (added later than the other three — not in either pasted schema draft, but confirmed; SMTP send, `email_alert.py`, done 2026-09-20 — see "Where things stand" for the full design, including its HTML/CSS status-digest mode). An `EMAIL_ALERT` task is gated the same way any other task is, via a normal `CFG_TASK_DEPENDENCY`/`CFG_PIPELINE_DEPENDENCY` row — a `FAILURE` edge fires only when what it watches fails, `ALWAYS` fires regardless. No separate conditional mechanism needed.

**`EMAIL_ALERT` redesigned (2026-09-20, iteration 2 phase 3, `E2-43`) — it is now a pipeline-level completion alert, not a task-level one.** Per explicit interview decision: "task level emails are noise", and "once you exhaust retries and all of the tasks that can be run are ran and failed, then send one email considering all". One email per run, whose **flavour** — `SUCCESS` (green) / `COMPLETED_WITH_ERRORS` (amber) / `FAILED` (red) — is computed by `email_alert.run_flavour()` from every active task's own status under this `pipeline_run_id`, read from `AUD_TASK_RUN_LOG` rather than `AUD_PIPELINES_RUN_LOG` (which may not be finalized yet when the alert runs). Optional per-flavour `EMAIL_SUBJECT_<STATUS>`/`EMAIL_BODY_<STATUS>` templates each fall back to the plain `EMAIL_SUBJECT`/`EMAIL_BODY`, and optional `EMAIL_ON_STATUS` restricts which flavours actually send. The gating sentence above still holds — this changes what the task *does* once dispatched, not how it is dispatched.

## CLI surface

| Command | Purpose |
|---|---|
| `list` | List pipelines |
| `run --pipeline_code X [--task_code Y] [--force]` | The one execution verb (see Architecture above) |
| `setup` | **(2026-09-20, supersedes `configure` entirely)** One idempotent command: read settings from a `.env`-style file or the environment, write/update `craft-connector.yml`, then bring the Engine DB to current (`init-db` if empty, `migrate` if not). Run it to set up, run it again to update — `src/etl_craft/setup_command.py` |
| `init-db` | (added 2026-09-20) Apply the packaged schema to an empty Engine DB |
| `doctor` | (added 2026-09-20) Resolve config, every secret and every configured connection, reporting all checks |
| `docs-version` | (added 2026-09-20) Record a new documentation version for any task whose `DOCUMENTATION` text changed; with `--task_code`, show one task's full history |
| `set-execution-mode local\|orchestrator` | One-time-per-environment mode lock; persists in `craft-connector.yml` until rebuild/reconfigure |
| `graph --name <pipeline>` | Print dependency chain / lineage. Bare `graph` with no name errors. |
| `validate` | Config integrity check — including the cross-database checks (e.g. single-column PK on a `TARGET_TABLE`) that no Engine DB constraint can enforce, plus (added 2026-09-20) every active task declaring `SOURCE_OBJECT`/`TARGET_OBJECT` |
| `generate-yml --pipeline_code X [--output <path>]` | Emit YAML (replaces the earlier working name `generate-dag`) — hand-rolled, Airflow-YAML-inspired shape, consumable by Airflow, another orchestrator, or a custom script |
| `generate-yml --global` | (added 2026-09-19) Emit the optional cross-pipeline trigger DAG (`TriggerDagRunOperator`-shaped nodes) — refused unless `[Orchestrator].Global_dag`/`Dag_defaults.Global_dag: true`; mutually exclusive with `--pipeline_code` |
| `lineage --table <schema.table>` | (added 2026-09-20) List every active task that declares `<schema.table>` as a `SOURCE_OBJECT` or `TARGET_OBJECT`, across all pipelines — closes the "table-level lineage" read-only verb this section used to list as conceptually agreed but unbuilt |
| `lineage --column <schema.table.column>` | (added 2026-09-20) Column-level lineage, parsed from each SQL task's own `SOURCE_SQL` with sqlglot and cached in `AUD_COLUMN_LINEAGE` |
| `migrate` | (added 2026-09-20) Apply any pending `sql/migrations/*.sql` file to an already-deployed Engine DB — see `src/etl_craft/migrate.py` and `sql/migrations/README.md` |
| `steps --pipeline_code X` | (added 2026-09-20) List a pipeline's active tasks, each with its own `CFG_TASK_PARAMETERS` — closes the "steps-in-a-pipeline" read-only verb |
| `history --pipeline_code X [--task_code Y] [--limit N]` | (added 2026-09-20) Recent run history, pipeline- or task-level — closes the "run history" read-only verb |
| `generate-docs [--output <dir>]` | (added 2026-09-20) Emit the static, searchable documentation site described below — `src/etl_craft/docs_generator.py` |

Retired during design: `execute --local` / `execute --airflow` (collapsed entirely into `run`), any REST-based Airflow trigger/status commands, and a proposed `reap` command for stale-run cleanup (superseded by parent/child monitoring).

All of the "read-only query verbs conceptually agreed but not yet named or built" this section used to list — dependency graph (`graph`), table-level lineage (`lineage`), steps-in-a-pipeline (`steps`), run history (`history`) — are now built, closing that item out. Writing `CFG_` rows (registering a pipeline, adding a task, adding a dependency) stays outside the CLI — pipeline creation specifically is manual, git-managed migrations/inserts; how far *other* `CFG_` writes get their own verbs later is open ("most of it is fine," not fully enumerated).

The documentation generator (`generate-docs`, above) is also now built: the same read-layer queries as above (`cfg.py`, `resolver.build_graph`), plus each task's `DOCUMENTATION` parameter and its parsed column lineage, rendered as a static, searchable site to a local output directory (deployable to GitHub Pages or any endpoint) rather than returned as CLI text. **[DEVIATION, 2026-09-20]** "searchable" is **Fuse.js, vendored into the generated output** — this paragraph previously said the opposite ("a small, dependency-free vanilla-JS substring search ... not a vendored copy of Fuse.js/Lunr.js"), which round 3 caught as contradicting both the code and this file's own "Where things stand" entry. The original no-CDN reasoning is why Fuse is *vendored* rather than linked. **[DEVIATION, E2-56]** the generator is read-only: `docs-version` and `lineage --column` are the verbs that write. See `docs_generator.py`'s own module docstring.

## craft-connector.yml

**This section rewritten 2026-09-23 to match the config-manifest pivot of 2026-09-22/23 (commits
`578ab74`..`8e0f96f`), which had shipped without a matching update here — see "Where things stand"'s
dated 2026-09-23 entry for the full story of that gap and E3-01, the shippability bug found in it.
Updated again the same day (E3-12) to add the preferred-connection-shape fields commit `3915fd8`
added for Databricks/Snowflake — the same class of gap (a config-format change shipping without a
matching update here) recurring at smaller scale inside the very commit that fixed the first one.**

Versioned, no secrets stored directly in it — analogous to a dbt `profiles.yml`. `config.py` reads
**two** shapes losslessly into one `ConnectorConfig`, and every downstream reader is
format-agnostic:

- **Legacy** (`[Execution]`/`[Source]`/`[Postgres]`/`[Warehouse]`/`[Orchestrator]`/`[Email]`,
  `Active_profile`/`Profiles: {dev: {jdbc_url, user, auth_mode, ...}}`) — connection *values* live
  directly in the profile. Still fully supported for files already in this shape; `setup` updates
  one such file in place rather than rewriting it into the canonical shape.
- **Canonical** (current, what `setup` writes for a new file, and what both shipped worked
  examples — `docs/craft-connector.env-secrets.example.yml`,
  `docs/craft-connector.file-secrets.example.yml` — teach) — connection *values* never appear in
  the file at all; `Engine`/`Warehouse`/`Email` each carry a `Profile` label plus a `Variables`
  mapping from field name to the *name* of whatever env var or `.env` key holds the real value.

```yaml
Orchestration:
  Mode: local | remote             # remote = Mode=orchestrator internally; set via `set-execution-mode` or `setup`
  Orchestrator_name: <optional>    # informational only, engine doesn't act on it
  Task_timeout_seconds: 21600      # default; a task parameter can override, 0 disables
  Max_parallel_tasks: 8            # default; local wave size
  Enforce_sla: false               # opt in to engine-side comparison with CFG_PIPELINES.SLA_IN_HOURS

Secrets:
  Source_type: file | environment  # where the names below resolve against
  Source_path: <path>              # required when Source_type = file (a .env-style file)

Engine:                            # Engine DB — required; PostgreSQL shape shown. SQLite (the default):
                                   #   Engine: {Profile: dev, Jdbc_url: jdbc:sqlite:etl-craft-engine.db}
  Profile: dev                     # $ETL_CRAFT_ENGINE_PROFILE overrides at runtime
  Variables:
    jdbc_url: ENGINE_JDBC_URL
    user: ENGINE_USER
    auth_mode: ENGINE_AUTH_MODE    # password | key_file
    secret: ENGINE_SECRET          # a tier-scoped variant (ENGINE_DEV_SECRET) wins if it's set

Warehouse:                         # optional — see "Warehouse connector layer" above; resolved open question #1
  Name: Postgres | DuckDB | Databricks | Snowflake | Trino   # checked against what jdbc_url resolves to
  Table_format: iceberg | native   # default iceberg; a task can override with CFG_TASK_PARAMETERS.TABLE_FORMAT
  Profile: dev                     # $ETL_CRAFT_WAREHOUSE_PROFILE overrides at runtime
  Variables:
    jdbc_url: WAREHOUSE_JDBC_URL
    user: WAREHOUSE_USER           # omitted for auth_mode none/token (Databricks' user is the literal "token")
    auth_mode: WAREHOUSE_AUTH_MODE # none | password | key_file | token
    secret: WAREHOUSE_SECRET
    key_file: WAREHOUSE_KEY_FILE   # only for auth_mode=key_file (Snowflake key-pair)

# [ADDITION, 2026-09-23] The tested and recommended shape for Databricks/Snowflake specifically
# (warehouse.PREFERRED_CONNECTION_FIELDS) — separate connection fields instead of one packed JDBC
# URL. Both resolve to auth_mode: token automatically; there is no separate auth_mode variable to
# set for either; an explicit token mode is accepted, other modes are rejected. The engine
# assembles the full connection at connect time (warehouse.preferred_connection_url) — nothing here
# is a JDBC URL a human hand-builds with an embedded query string or, for Databricks, transport/
# auth parameters (AuthMech/UID/PWD) copied verbatim from the JDBC tab a workspace's own connection
# details page shows — preferred_connection_url strips those before this value is ever used or
# persisted (E3-08; a real, reproduced leak path into an existing legacy-shaped manifest before
# that fix, not a hypothetical one).
Warehouse:                         # Databricks, via the preferred shape
  Name: Databricks
  Table_format: iceberg | native   # iceberg = Delta with UniForm enabled; Databricks always reads/writes Delta either way
  Profile: dev
  Variables:
    jdbc_url: WAREHOUSE_JDBC_URL   # host, port, httpPath only — no ConnCatalog/ConnSchema, no UID/PWD
    catalog: WAREHOUSE_CATALOG     # Unity Catalog catalog
    schema: WAREHOUSE_SCHEMA
    token: WAREHOUSE_TOKEN         # a personal access token; no `user` — the username is the literal "token"

Warehouse:                         # Snowflake, via the preferred shape
  Name: Snowflake
  Table_format: iceberg | native   # iceberg defaults to EXTERNAL_VOLUME = 'SNOWFLAKE_MANAGED' (no bucket to provision)
  Profile: dev
  Variables:
    user: WAREHOUSE_USER
    account: WAREHOUSE_ACCOUNT     # the <org>-<account> identifier, not a hostname
    database: WAREHOUSE_DATABASE
    schema: WAREHOUSE_SCHEMA
    warehouse: WAREHOUSE_WAREHOUSE
    role: WAREHOUSE_ROLE
    token: WAREHOUSE_TOKEN         # a Programmatic Access Token (PAT) — needs a network policy on the account/user first

Cloning:
  Enabled: true | false            # opt-in, off by default — nothing is copied unless explicitly turned on
  Scope: cfg | aud | all           # which table groups mirror into the warehouse, merge-style
  External_volume: <name>          # Snowflake Iceberg mirrors only — see cloning.py
  Base_location: <path>            # Snowflake Iceberg mirrors only

Dag_defaults:                      # optional — global defaults/fallbacks for generate-yml's Airflow-facing fields
  Global_dag: true | false         # emit the optional cross-pipeline trigger DAG — defaults to false
  Catchup: true | false
  Tags: [tag1, tag2]
  Retries: <int>
  Retry_delay_minutes: <int>
  Depends_on_past: true | false
  Email_on_failure: true | false
  Email_recipients: [addr1, addr2]

Email:                             # optional — required only if any active task uses HANDLER=EMAIL_ALERT
  Profile: dev
  Variables:
    host: EMAIL_HOST
    port: EMAIL_PORT
    from_address: EMAIL_FROM
    auth_mode: EMAIL_AUTH_MODE     # none | password only
    user: EMAIL_USER
    use_tls: EMAIL_USE_TLS
    secret: EMAIL_SECRET
```

`auth_mode` per profile selects one of a small registry of connection-creator functions (`password`,
`token`, `sso` — Engine DB only, unimplemented, `key_file`, and `none` for a warehouse/email target
with nothing to authenticate) — see Architecture above. `[Email]`/`Email:` profiles use their own,
smaller `auth_mode` vocabulary (`none`, `password`) — see `email_alert.py`'s own module docstring
for why `token`/`sso` aren't included there.

**The `Variables` name-indirection rule, and its one deliberate exception.** Every value under a
canonical `Variables:` block is a variable *name*, never the value itself — that is what makes the
file safe to commit. `setup`'s bootstrap-input reads and the names it writes into `Variables` are
now, per E3-01, the *same* string (`ENGINE_JDBC_URL`, `WAREHOUSE_JDBC_URL`, ...) — a team that sets
`ENGINE_JDBC_URL` once has it serve both `setup`'s own bootstrap and every later `etl-craft run`.
Before that fix, `setup` wrote `ETL_CRAFT_POSTGRES_*`/`ETL_CRAFT_WAREHOUSE_*` pointers into the
manifest regardless of what a hand-authored file already had — re-running `setup` against a
manifest written by hand per the docs silently discarded its `Variables` mapping and pointed it at
names nothing in the environment had ever set. `docs/craft-connector.variables.env` is the full,
current list of every variable name the two shipped examples use.

**Cloning**, specifically: a merge-style copy of selected Engine DB tables into the warehouse, so a team can query engine config/audit from inside their own warehouse without a separate Postgres connection. Runs after each pipeline run, only when enabled. This is special-cased engine-internal machinery — it does not reopen the "one warehouse, no general cross-connection support" rule that applies to ordinary tasks. **Done (2026-09-20)** — `src/etl_craft/cloning.py`; see "Where things stand" for the full mechanism (truncate-then-reinsert per table, generic cross-dialect column types, and a same-database safety refusal). The ClickHouse-specific fixes it once carried were deleted on 2026-09-21 along with ClickHouse support itself. `External_volume`/`Base_location` are needed only when cloning onto a Snowflake Iceberg warehouse; `setup` writes them from `ETL_CRAFT_CLONING_EXTERNAL_VOLUME`/`ETL_CRAFT_CLONING_BASE_LOCATION` and preserves a hand-added value it wasn't given a bootstrap input for (E3-06).

**Documentation surface, for anyone new to this repo**: `README.md` (quickstart), `docs/configuration.md` (the canonical manifest, in full), `docs/craft-connector.*.example.yml` and `docs/craft-connector.variables.env` (worked examples), `docs/parameters.md` (`CFG_TASK_PARAMETERS` reference per handler), `docs/first-pipeline.md` (a runnable walkthrough), `docs/operations.md` (backup/upgrade sequence, and — see "Architecture at a glance" above — which warehouse targets are the controlled/supported launch path versus which need a team's own acceptance test), `docs/release-checklist.md`, `SECURITY.md`, and `CHANGELOG.md` (a pointer to this file plus `ITERATION_2.md`/`ITERATION_3.md` for full history).

## Database schema

Full DDL (complete first draft, needs review and sign-off) is at `sql/schema.sql`. It has been applied to a real local Postgres 16 and exercised against `sql/schema_test.sql`, which confirms — by actually triggering them, not just reading the DDL — that every load-bearing mechanism behaves as this file describes: the audit trigger stamps and preserves the right columns, the enum/conditional `CHECK` constraints reject what they should, the `DEPENDS_ON_PIPELINE_ID` default-from-sibling trigger fires, self-dependency is blocked, the partial unique indexes correctly allow a deactivated natural key to be reused while blocking a second active duplicate, and — the one that matters most — the `AUD_PIPELINES_RUN_LOG` partial unique index really does block a second concurrent `IN-PROGRESS` run for the same pipeline while leaving a second pipeline's own `IN-PROGRESS` row and any terminal-status row untouched. Re-run `sql/schema_test.sql` (instructions in its header) after any schema change that touches these mechanisms. Summary of tables:

**Config tables** — `CFG_PIPELINES`, `CFG_PIPELINE_DEPENDENCY`, `CFG_TASKS`, `CFG_TASK_DEPENDENCY`, `CFG_TASK_PARAMETERS`, `CFG_BUSINESS_RULES`.
**Audit tables** — `AUD_PIPELINES_RUN_LOG`, `AUD_TASK_RUN_LOG`, `AUD_BUSINESS_RULES_RUN_LOG`, `AUD_BUSINESS_RULES_RESULTS`, `AUD_TASK_OFFSET_TRACKER`, `AUD_PIPELINE_DEPENDENCY_TRACKER`, `AUD_TASK_DEPENDENCY_TRACKER`.
**Migration bookkeeping** — `SCHEMA_MIGRATIONS` (added 2026-09-20, see "Where things stand" and `sql/migrations/README.md`) — not a `CFG_`/`AUD_` table, purely for `etl-craft migrate`'s own use.

All primary keys use `GENERATED ALWAYS AS IDENTITY`. The pasted schema notes annotate almost every validated column as "(WILL BE ENFORCED BY THE ENGINE SETUP AS TRIGGER)" — `schema.sql` does **not** implement all of these as literal trigger functions; see its header and closing summary block for exactly which ones became real triggers (only where a `DEFAULT`/`CHECK` genuinely cannot do the job — namely `CREATED_BY`/`UPDATED_BY` capture, and `CFG_TASK_DEPENDENCY.DEPENDS_ON_PIPELINE_ID`'s default-from-sibling-column) versus which became plain `CHECK` constraints (all the enum-style value lists). This is a real, deliberate divergence from the literal pasted text — flagged prominently for sign-off, not silently decided.

## Open questions

Genuinely unresolved — don't guess silently on these, ask:

1. ~~What should the warehouse section in `craft-connector.yml` actually be called?~~ **Resolved (2026-09-19): `[Warehouse]`** — see "Warehouse connector layer" under "Where things stand" for the full reasoning; `"Data Db"`/`[Data Db 1]` stay rejected as too Informatica-shaped.
2. ~~`CFG_PIPELINES` needs a stable code/slug for the CLI's `--pipeline_code` to resolve against~~ **Resolved (2026-09-19, during the schema sign-off walkthrough)**: `PIPELINE_CODE` — confirmed as written, no change requested, per `sql/schema.sql`'s own closing summary ("every flag ... confirmed as written — none required a change").
3. ~~Email alert transport and `$$`-substitution-in-alert-bodies~~ **Resolved (2026-09-20)**: SMTP, via a new `[Email]` craft-connector.yml section; `$$`-substitution does apply, via a small closed token set — see "Handlers" above and `email_alert.py`'s own module docstring for the full design, including the later-added HTML/CSS status-digest mode.
4. ~~The dependency-poll backoff schedule~~ **Resolved (2026-09-19)**: the percentage-based schedule alone governs — see "Dependency resolution & polling" above.
5. ~~Schema drift detection~~ **Resolved (2026-09-20), per explicit instruction ("we are using information schema for that")**: the user identified `sql_actions.py`'s existing `_check_or_evolve_schema` mechanism (staged-SELECT-vs.-target column comparison via `information_schema`, with an opt-in evolution path — see the "execution engine" entries under "Where things stand") as already satisfying this, rather than a separate declared-column-list-diffed-each-run mechanism as originally envisioned. **Scope note, for anyone revisiting this**: the built mechanism runs at *write* time (when a SQL-action task actually executes), scoped to that one target, not as an independent per-run drift check across every table regardless of whether anything writes to it that run — a real, narrower shape than the open question's original literal wording, accepted as sufficient rather than building the broader version.
6. Every `[DEVIATION]`, `[ADDITION]`, and `[CHOICE]` flagged in `sql/schema.sql` — that file's closing summary block is the authoritative, current list; this file's "Database schema" section above is a pointer to it, not a duplicate.
7. ~~No migration tooling (Alembic or otherwise) has been discussed~~ **Resolved (2026-09-20)**: a small Alembic-*lite* runner (`src/etl_craft/migrate.py`, `etl-craft migrate`, the `SCHEMA_MIGRATIONS` table) — see "Where things stand"'s "Execution engine refinements" entry and `sql/migrations/README.md` for scope and the fresh-install/empty-migrations-dir consistency argument. `schema.sql` keeps its existing role as the full definition for a brand-new install; this only carries an *already-deployed* database forward incrementally from here on.
