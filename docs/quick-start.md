# Quick Start

!!! note "Planned"
    The quick start is written once `setup` and `run` exist in the new code base. It will take
    you from `pip install` to a pipeline running against a local SQLite Engine DB and DuckDB
    warehouse, and it is run as part of the release tests so it stays correct.
