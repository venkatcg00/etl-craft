# Cloning: mirror Engine DB tables into the warehouse after each pipeline run.
#
# So a team can query run history and config from inside its own warehouse.
# Off by default. Scope cfg copies the CFG_ tables, aud the AUD_ tables, all
# both, and none nothing (a profile can turn it off without deleting it).
#
# Each setting is marked `# variable` or `# value`. A variable is looked up in
# the secrets source (here: the process environment); when it is not set, the
# text itself is used, so `Profile: dev` is simply dev. A secret must always
# be a variable that is set. `etl-craft doctor` lists every value used as
# written that looks like a variable name.
#
# Complete and loadable as written: tests/test_config_examples.py loads every
# file in this directory, for every profile it declares.

Secrets:
  Source_type: environment             # value — variables below are read from the process environment
  Profile: ETL_CRAFT_PROFILE           # variable — dev | sit | uat | prod, set in each environment

Orchestration:
  Mode: local                          # value
  Task_timeout_seconds: 21600          # value
  Max_parallel_tasks: 8                # value
  Enforce_sla: false                   # value

Engine:
  Name: Postgres                       # value
  dev:
    jdbc_url: ENGINE_JDBC_URL          # variable
    user: ENGINE_USER                  # variable
    auth_mode: ENGINE_AUTH_MODE        # variable
    secret: ENGINE_SECRET              # variable, required: a secret is never used as written
  sit:
    jdbc_url: ENGINE_JDBC_URL          # variable
    user: ENGINE_USER                  # variable
    auth_mode: ENGINE_AUTH_MODE        # variable
    secret: ENGINE_SECRET              # variable, required: a secret is never used as written
  uat:
    jdbc_url: ENGINE_JDBC_URL          # variable
    user: ENGINE_USER                  # variable
    auth_mode: ENGINE_AUTH_MODE        # variable
    secret: ENGINE_SECRET              # variable, required: a secret is never used as written
  prod:
    jdbc_url: ENGINE_JDBC_URL          # variable
    user: ENGINE_USER                  # variable
    auth_mode: ENGINE_AUTH_MODE        # variable
    secret: ENGINE_SECRET              # variable, required: a secret is never used as written

Warehouse:
  Name: Postgres                       # value
  Table_format: native                 # value
  dev:
    jdbc_url: WAREHOUSE_JDBC_URL       # variable
    user: WAREHOUSE_USER               # variable
    auth_mode: WAREHOUSE_AUTH_MODE     # variable
    secret: WAREHOUSE_SECRET           # variable, required: a secret is never used as written
  sit:
    jdbc_url: WAREHOUSE_JDBC_URL       # variable
    user: WAREHOUSE_USER               # variable
    auth_mode: WAREHOUSE_AUTH_MODE     # variable
    secret: WAREHOUSE_SECRET           # variable, required: a secret is never used as written
  uat:
    jdbc_url: WAREHOUSE_JDBC_URL       # variable
    user: WAREHOUSE_USER               # variable
    auth_mode: WAREHOUSE_AUTH_MODE     # variable
    secret: WAREHOUSE_SECRET           # variable, required: a secret is never used as written
  prod:
    jdbc_url: WAREHOUSE_JDBC_URL       # variable
    user: WAREHOUSE_USER               # variable
    auth_mode: WAREHOUSE_AUTH_MODE     # variable
    secret: WAREHOUSE_SECRET           # variable, required: a secret is never used as written

Cloning:
  dev:
    Enabled: false                     # value — default false
    Scope: cfg                         # value — cfg | aud | all | none
  sit:
    Enabled: true                      # value
    Scope: none                        # value
  uat:
    Enabled: true                      # value
    Scope: cfg                         # value
  prod:
    Enabled: true                      # value
    Scope: all                         # value
